(ns metabase.slides.init
  "Init namespace for the slides module — required by `metabase.core.init` so the
   model namespace is loaded on startup."
  (:require
   [metabase.slides.models.slides]))

(comment metabase.slides.models.slides/keep-me)
