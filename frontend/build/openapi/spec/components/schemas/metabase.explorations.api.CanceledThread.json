{
  "description": "Schema for the cancel endpoint response — just the state-bearing fields the FE needs to\n  reflect the cancellation. EQ status changes are picked up via the existing `/queries` poll.",
  "properties": {
    "canceled_at": {
      "oneOf": [
        {},
        {
          "type": "null"
        }
      ]
    },
    "completed_at": {
      "oneOf": [
        {},
        {
          "type": "null"
        }
      ]
    },
    "id": {
      "description": "value must be an integer greater than zero.",
      "minimum": 1,
      "type": "integer"
    }
  },
  "required": [
    "id",
    "canceled_at",
    "completed_at"
  ],
  "type": "object"
}
