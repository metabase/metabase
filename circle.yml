machine:
  java:
    version:
      oraclejdk8
test:
  override:
    - |
      case $CIRCLE_NODE_INDEX in
        0) lein eastwood ;;
        1) ./lint_js.sh && lein bikeshed --max-line-length 240 ;;
        2) lein test ;;
        3) lein uberjar ;;
      esac:
        parallel: true
