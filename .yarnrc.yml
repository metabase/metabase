# Yarn v4 configuration - compatibility mode (uses node_modules)
nodeLinker: node-modules

# Disable global cache - each project has its own cache
enableGlobalCache: false

# Network settings
httpTimeout: 600000
