{
  "name": "Revenue by month",
  "description": "Amount paid per month.",
  "type": "question",
  "display": "line",
  "collection_id": "{{c:pagila}}",
  "visualization_settings": {
    "graph.dimensions": [
      "payment_date"
    ],
    "graph.metrics": [
      "sum"
    ]
  },
  "dataset_query": {
    "lib/type": "mbql/query",
    "database": "{{db}}",
    "stages": [
      {
        "lib/type": "mbql.stage/mbql",
        "source-table": "{{t:payments}}",
        "aggregation": [
          [
            "sum",
            {},
            [
              "field",
              {},
              "{{f:payments.amount}}"
            ]
          ]
        ],
        "breakout": [
          [
            "field",
            {
              "temporal-unit": "month"
            },
            "{{f:payments.payment_date}}"
          ]
        ]
      }
    ]
  }
}
