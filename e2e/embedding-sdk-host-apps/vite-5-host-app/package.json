{
  "name": "vite-5-host-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "npm run docker:up",
    "docker:up": "docker compose up",
    "docker:down": "docker compose down --rmi all --volumes"
  }
}
