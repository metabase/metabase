issue: 30235
issue_url: https://github.com/metabase/metabase/issues/30235
fix_commits: ["5c1b2af3b14495a6199b23dda52667a15a701e8b"]
fix_subject: "Fully hydrate PUT-ed collections, which the FE depends on"
area: collections-rest/api (was api/collection)
fe_be_boundary: false

mutation:
  kind: semantic
  patch_file: reconstruction.patch
  report_file: reconstruction-report.md
  base_sha: "56879decb0aac2ad61b5bc5737ebd4afc56e5f81"
  applies_cleanly: true
  file_relocated: true
  reconstructed_by: worktree_subagent
  oracle_source: modified_deftest
  reconstruction_notes: >
    Reverted the collection-detail hydration wrapping of the PUT /collection/:id response back to raw t2/hydrate :parent_id.

validation:
  oracle: {kind: be_unit, test: "metabase.collections-rest.api-test/update-collection-test", clean_baseline: pass, reconstructed_result: "1/4 fail (response missing :effective_ancestors / :effective_location)"}
  repro_confirmed: true
  confidence: high

failure_shape:
  observed: true
  source: observed_unit
  symptom: >
    PUT /collection/:id returns a partially-hydrated response missing :effective_ancestors and :effective_location that the FE depends on.
  repro_failure: >
    update-collection-test: partial= fails, response missing :effective_ancestors/:effective_location.
  not_setup_crash: true

coverage:
  be_unit: {status: kill, killed_by: ["metabase.collections-rest.api-test/update-collection-test"], clean_baseline: pass}
  fe_unit: {status: untested, killed_by: []}
  notes: "Semantic-reconstruction batch-5 wave-2 (no_added_deftest / modified-deftest oracle). Killed by shipped BE test => e2e repro cullable."
