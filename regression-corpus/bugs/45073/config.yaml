issue: 45073
issue_url: https://github.com/metabase/metabase/issues/45073
fix_commits: ["bc32ba9e15730b009b3b40ca2a33849e332e9517"]
fix_subject: "Add admin-only user-local setting for updated-permission modal/banner"
area: permissions/settings (was api/permissions)
fe_be_boundary: false

mutation:
  kind: semantic
  patch_file: reconstruction.patch
  report_file: reconstruction-report.md
  base_sha: "56879decb0aac2ad61b5bc5737ebd4afc56e5f81"
  applies_cleanly: true
  file_relocated: true
  reconstructed_by: worktree_subagent
  oracle_source: modified_deftest
  reconstruction_notes: >
    Removed the new-admin? guard from show-updated-permission-modal and -banner getters (returns raw setting value). NOTE: the detector mis-named the oracle (the fix APPENDED two NEW deftests right after delete-group-membership-test, fooling the enclosing-deftest scan). Correct oracle = the fix-added settings-test deftests.

validation:
  oracle: {kind: be_unit, test: "metabase.permissions.settings-test/show-updated-permission-modal-test", clean_baseline: pass, reconstructed_result: "2 fail (new-admin?=true rows: expected false, actual true)"}
  repro_confirmed: true
  confidence: high

failure_shape:
  observed: true
  source: observed_unit
  symptom: >
    Admins who joined after the v50 migration are shown the updated-permission modal/banner (guard bypassed).
  repro_failure: >
    show-updated-permission-modal-test / -banner-test: expected false, actual true.
  not_setup_crash: true

coverage:
  be_unit: {status: kill, killed_by: ["metabase.permissions.settings-test/show-updated-permission-modal-test"], clean_baseline: pass}
  fe_unit: {status: untested, killed_by: []}
  notes: "Semantic-reconstruction batch-5 wave-2 (no_added_deftest / modified-deftest oracle). Killed by shipped BE test => e2e repro cullable."
