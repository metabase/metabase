// Directory name is historical — when this barrel was first written the
// page had a "Chat | Add data" outer tab container called
// `NewExplorationLeftTabs` that lived here. That wrapper was deleted
// when the page moved to a three-column layout (Chat | Research content
// | Data palette); the Browse picker + its three sub-panels still live
// in this directory.
export { NewExplorationBrowse } from "./NewExplorationBrowse";
