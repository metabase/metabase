{
  "anyOf": [
    {
      "minLength": 1,
      "type": "string"
    },
    {
      "type": "boolean"
    }
  ],
  "description": "A single attribute value in a PATCH operation. The strings clients send for booleans are parsed by the handler."
}
