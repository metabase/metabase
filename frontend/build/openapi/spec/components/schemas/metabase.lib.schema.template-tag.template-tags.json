{
  "allOf": [
    {
      "items": {
        "$ref": "./metabase.lib.schema.template-tag.template-tag.json"
      },
      "type": "array"
    },
    {
      "$ref": "./metabase.lib.schema.template-tag.distinct-template-tags.json"
    }
  ],
  "description": "Prior to 63, this was a map, but was changed to a list to preserve order with more than 8 template tags.\n\n  Each tag must have a distinct `:name`."
}
