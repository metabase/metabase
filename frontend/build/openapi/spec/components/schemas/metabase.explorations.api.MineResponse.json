{
  "description": "Paginated envelope for `GET /mine`, mirroring the collection-items index shape. `:total` is the\n  count after the membership, permission, and archived filters; `:limit`/`:offset` echo the\n  `offset-paging` middleware (both nil when the request is unpaged).",
  "properties": {
    "data": {
      "items": {
        "$ref": "./metabase.explorations.api.ExplorationSummary.json"
      },
      "type": "array"
    },
    "limit": {
      "oneOf": [
        {
          "description": "value must be an integer greater or equal to than zero.",
          "minimum": 0,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ]
    },
    "offset": {
      "oneOf": [
        {
          "description": "value must be an integer greater or equal to than zero.",
          "minimum": 0,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ]
    },
    "total": {
      "description": "value must be an integer greater or equal to than zero.",
      "minimum": 0,
      "type": "integer"
    }
  },
  "required": [
    "total",
    "limit",
    "offset",
    "data"
  ],
  "type": "object"
}
