import type { IconName } from "metabase/ui";

export type ListItemTitleInfo = {
  label: string;
  icon: IconName;
};

export type ListItemSubtitleInfo = {
  label: string;
  icon: IconName;
  link: string;
};
