name: Codespell

on:
  push:
    branches:
      - 'master'
      - 'release-**'
  pull_request:

jobs:
  codespell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: codespell-project/actions-codespell@v1
        with:
          # keep this list in sync with the list in `mage/src/mage/codespell.yml`
          path: >-
            docs
