# Papercuts from Claude transcripts: index

116 papercuts. Mined 2026-09-23 with Jev classification and Claude drill-down. Pipeline and calibration notes: [`pipeline/`](../../pipeline/), [calibration.claude.md](../../pipeline/calibration.claude.md).

Kinds: codebase-trap (the Metabase code invites the mistake), test-harness, misleading-signal (output that lies), doc-gap, tool-quirk (CLI/tool behaviour), env-friction (shell/worktree/env), agent-behaviour (recurring agent habit, usually despite a memory note).

## codebase-trap (27)

| severity | occ | status | papercut | area |
|---|---|---|---|---|
| high | 6 | open* | [Search-index DDL (with-temp-index-table's CREATE/DROP TABLE, delete-obsolete-tables!) runs on the ambient connection; nested inside a rollback-only mt/with-temp it implicitly commits on H2/MySQL, destroys the savepoint and leaks every row into later tests (one leaked Transform broke four serialization tests); nested with-temp-index-table also silently reuses the outer table](chris.claude.search-temp-index-ddl-commits-enclosing-with-temp.md) | src/metabase/app_db/connection.clj (rollback-only transactions), src/metabase/search/appdb/index.clj (drop-table!, create-table!, with-temp-index-table, delete-obsolete-tables!), src/metabase/search/db.clj, test/metabase/search/test_util.clj, search scoring and transform tests |
| high | 4 | documented-still-hit | [mt/user-http-request is an in-process mock, but its name, the client docstring examples and copied comments say "real HTTP on a Jetty thread](chris.claude.user-http-request-named-like-real-http.md) | test/metabase/test/http_client.clj, test/metabase/test/data/users.clj, with-redefs -> with-dynamic-fn-redefs conversions |
| high | 2 | fixed | [Test-data helpers (user->id, username->token, mt/id, personal collections) create or cache durable state inside the ambient with-temp transaction; under rollback-only it replays, races, leaks tokens and bumps last_login](chris.claude.test-helpers-create-durable-state-inside-rollback-scope.md) | test harness -- test/metabase/test/data/users.clj, test/metabase/test/data/impl.clj, test/metabase/test/redefs.clj |
| high | 2 | fixed | [mt/user-http-request given a User id/object wrapped the whole in-process request in a rollback-only with-temp Session, silently discarding every app-DB write the endpoint made](chris.claude.user-http-request-non-keyword-user-runs-in-rollback-scope.md) | test harness -- test/metabase/test/data/users.clj (user-request), test/metabase/test/redefs.clj (with-temp rollback-only) |
| high | 1 | open | [The evals bucket's appdb/stats_copy_dwhs_adjusted.dump sits outside every Store key scheme and looks like an orphan; it is the registered source of dwhs-adjusted, and an agent deleted its local copy as "obsolete](chris.claude.evals-root-dump-looks-orphaned.md) | metabase/evals repo — appdb/artifacts.py (INPUT_KIND dwhs-adjusted), stats/fetch.py (split_location/object_key), stats/source.py, S3 evals bucket appdb/ prefix, .appdb/ local cache |
| high | 1 | fixed | [A separate `\"!path\"` entry in .github/file-paths.yaml doesn't exclude that path. dorny/paths-filter ORs the patterns, so it matches every *other* path and switched the backend CI matrix on for docs- and frontend-only PRs; green CI on a backend PR can't reveal it](chris.claude.paths-filter-negation-entry-inverts-filter.md) | .github/file-paths.yaml (dorny/paths-filter@v4.0.1 filters used by run-tests.yml / backend.yml), CI path gating |
| high | 1 | open | [Toucan 2 runs before-update hooks before it opens its transaction, so do-after-commit in a before-update hook fires before the UPDATE; and t2/changes in after-update is a TransientRow whose contains? answers for every column](chris.claude.toucan-before-update-hook-runs-outside-transaction.md) | toucan2 1.0.519 tools/before_update.clj vs tools/before_delete.clj; src/metabase/app_db/connection.clj do-after-commit; src/metabase/osi/models/osi_ai_context.clj |
| medium | 2 | open | [with-dynamic-fn-redefs proxy breaks on replacements it can't `apply` (reify IFn without applyTo) and on primitive-hinted fns](chris.claude.dynamic-redefs-proxy-cant-wrap-some-replacements.md) | test/metabase/test/util/dynamic_redefs.clj, test/metabase/pulse/test_util.clj (wrap-function), kondo hook :metabase/prefer-with-dynamic-fn-redefs |
| medium | 2 | open | [Pre-existing "why with-redefs" comments name thread mechanisms that don't exist, and agents copy them as evidence](chris.claude.false-why-comments-propagate-as-precedent.md) | test/metabase/transforms/jobs_test.clj (and any :metabase/prefer-with-dynamic-fn-redefs suppression comment) |
| medium | 2 | documented-still-hit | [The module name → namespace prefix → source directory rule is copied into many places (mage, dev.deps-graph, module explorer, test runner, sort order), so a new module kind needs a hunt](chris.claude.module-path-rules-duplicated.md) | mage/src/mage/modules.clj, dev/src/dev/deps_graph.clj, dev/src/dev/module_explorer.clj, test/metabase/test_runner.clj, dev/src/dev/modules_config.clj, dev/test/metabase/core/modules_test.clj, .clj-kondo/src/hooks/common/modules.clj |
| medium | 2 | open | [Metabase general-search ingestion drains through an in-process DelayQueue, so external harnesses have no durable "indexing is done" signal; index rotation also leaves retired tables and several active rows](chris.claude.search-ingestion-no-durable-drain-signal.md) | metabase src/metabase/search/ingestion.clj, search/models/search_index_metadata.clj, search/appdb/index.clj; consumers in evals stats/build.py |
| medium | 1 | fixed | [App-DB queries only in <module>/db.clj" convention was unlinted and invisible; agent put raw t2 calls back into specialization namespaces during a restack](chris.claude.app-db-queries-only-in-module-db-namespace.md) | src/metabase/*/db.clj convention; search module (search.db, appdb/index, specialization/h2|postgres, lease) |
| medium | 1 | fixed | [dlt mutates column-hint dicts in place, so a hint dict shared between resources leaks one table's columns (and NOT NULL flags) into another](chris.claude.dlt-mutates-shared-column-hints.md) | metabase/data-stack — pipeline/src/ingest_runtime/runtime.py (column_hints), sources/*/extension.py, dlt resource `columns=` |
| medium | 1 | open | [The mage "modules triggering driver tests" cap counts nested modules, so carving a child out of a triggering module fails CI](chris.claude.driver-test-cap-counts-nested-modules.md) | mage/test/mage/modules_test.clj (module-graph-may-not-become-more-connected), mage/src/mage/modules.clj (driver-affecting-overrides) |
| medium | 1 | open | [Quartz secure-delegate gen-class is compiled into one fixed java.io.tmpdir folder shared by every checkout; moving the namespace loads a stale class](chris.claude.gen-class-delegate-shared-tmpdir-cache.md) | src/metabase/task/secure_delegate.clj (dev-compile-delegate!), Quartz DriverDelegate shims, dev/test classpath |
| medium | 1 | open | [`semantic.embedding/get-configured-model` returns an unresolved model with no `:embedding-space-id`, but the entity-retrieval index writes that key into a NOT NULL column; only `resolve-model` supplies it](chris.claude.get-configured-model-lacks-embedding-space-id.md) | enterprise/backend/src/metabase_enterprise/semantic_search/embedding.clj (get-configured-model, resolve-model); entity_retrieval/index_table.clj |
| medium | 1 | open | [search/appdb/index_test.clj contains raw NUL and control bytes, so grep treats it as binary and prints nothing, and awk editing truncates it](chris.claude.index-test-nul-bytes-hide-from-grep.md) | test/metabase/search/appdb/index_test.clj (strip-junk-chars-test); Claude Code grep shim (-I), awk/sed edits |
| medium | 1 | fixed | [Running the kondo ratchet tests locally silently rewrote .clj-kondo/ratchets.edn (test fixture called fix!)](chris.claude.kondo-ratchet-test-rewrites-ratchets-file.md) | dev/test/metabase/core/kondo_ratchet_test.clj, dev/src/dev/kondo_ratchet.clj, CLAUDE.md "Kondo Ignore Ratchets", mage project-tests |
| medium | 1 | fixed | [search's table-not-found-exception? matched any PSQLException, so the missing-index-table test passed on Postgres for the wrong reason (upsert never reached the table)](chris.claude.search-missing-table-predicate-matched-any-psqlexception.md) | src/metabase/search/appdb/index.clj (safe-batch-upsert!, delete!), test/metabase/search/appdb/index_test.clj |
| medium | 1 | open | [A defsetting write inside t2/with-transaction updates the in-memory settings cache immediately, so a rollback leaves the node serving a value the DB no longer has](chris.claude.setting-write-cache-not-rolled-back-by-transaction.md) | src/metabase/settings/models/setting.clj (set-value-of-type! :string, set-many!), settings cache; enterprise metabot permissions mode switch |
| medium | 1 | documented-still-hit | [Toucan 2 treats a trailing map in t2/delete!/exists? as a Honey SQL query, and a test's (with-redefs [t2/query identity]) breaks nil-conn delegation](chris.claude.toucan2-trailing-map-and-redef-arity.md) | toucan2 API; src/metabase/search/db.clj; test/metabase/search/appdb/specializations/postgres_test.clj |
| medium | 1 | open | [`with-model-cleanup` silently runs a full in-place search reindex at scope exit (not in its docstring), so hundreds of tests acquire the search lease inside ambient transactions](chris.claude.with-model-cleanup-hidden-search-reindex.md) | test/metabase/test/util.clj (do-with-model-cleanup / reindex-search-index!), search lease (src/metabase/search/lease.clj ambient-transactions-allowed?) |
| low | 1 | unknown | [`./bin/mage cljfmt-files bb.edn` reindents unrelated comments, because master's bb.edn is not cljfmt-clean and CI's cljfmt-all never checks .edn](chris.claude.cljfmt-files-reformats-bb-edn.md) | bb.edn, mage/src/mage/format.clj (cljfmt-files / cljfmt-all scope), .github/workflows/backend.yml be-cljfmt |
| low | 1 | open | [Agents read EDN data with clojure.core/read-string instead of clojure.edn/read-string; nothing lints it, and mage has pre-existing examples to copy](chris.claude.clojure-core-read-string-for-edn.md) | mage/ (bb tooling), dev/src, test helpers that parse .edn files; .clj-kondo/config.edn :discouraged-var |
| low | 1 | documented-still-hit | [data-stack: editing sources/<name>/source.yml without `make regen` leaves sources/manifest.json stale, and terraform reads the manifest, so a tier change merges and does nothing](chris.claude.data-stack-manifest-regen-after-source-yml.md) | metabase/data-stack — sources/*/source.yml, sources/manifest.json (generated), Makefile `regen`, pipeline/tests/test_validate.py |
| low | 1 | open | [Normalization/validation placed in a defsetting `:setter` never applies to env-var values, so moving a trim from the getter into the setter silently regressed `MB_*` handling](chris.claude.defsetting-setter-normalization-bypassed-by-env.md) | src/metabase/settings/models/setting.clj (defsetting :setter docs); enterprise osi_generation/settings.clj; metabot/settings.clj |
| low | 1 | open | [`(io/resource "metabase/metabot/self")` on a package directory resolves to the test tree, because src and test share package dirs on the classpath](chris.claude.io-resource-directory-resolves-test-tree.md) | classpath layout (src/ and test/ both contain metabase/metabot/self/); benchmark_test.clj in the OSI stack |

## test-harness (10)

| severity | occ | status | papercut | area |
|---|---|---|---|---|
| high | 3 | documented-still-hit | [The local test app DB silently depends on a gitignored .lein-env (Postgres, hosted token, encryption) in the main checkout and on H2 in fresh worktrees, and the test banner names only the warehouse driver](chris.claude.local-test-appdb-differs-per-worktree-lein-env.md) | .lein-env (gitignored), ./bin/test-agent, test initialization; worktrees under ~/workspace/metabase |
| medium | 2 | open | [Tests gated on drivers or app-DB features pass with 0 assertions in a default local run, so "green locally" and "fails without the fix" checks prove nothing](chris.claude.driver-gated-tests-silently-skip-locally.md) | mt/test-drivers + mt/normal-drivers-with-feature, (when (not= :h2 (mdb/db-type)) ...), (when (search/supports-index?) ...) guards; ./bin/test-agent default DRIVERS=h2 |
| medium | 1 | documented-still-hit | [A green `eastwood` run over the backend source says nothing about CI's "Eastwood (changed tests only)" job, which lints test namespaces through a separate `:eastwood/test` alias; the agent reported "Eastwood clean backend-wide" and CI failed on a reflection warning in a test](chris.claude.eastwood-source-alias-skips-changed-tests-gate.md) | .github/workflows/backend.yml be-linter-eastwood-test-changed; deps.edn :eastwood vs :eastwood/test aliases; memory reference_eastwood_reflection_ci_gate.md |
| medium | 1 | fixed | [The global token checker's circuit breaker carries state across the test JVM; adding unrelated test namespaces reshuffled CI partitions and broke token-check-test](chris.claude.global-token-checker-circuit-breaker-leaks-across-tests.md) | src/metabase/premium_features/token_check.clj (token-checker, circuit-breaker-token-checker), test/metabase/premium_features/token_check_test.clj, CI App DB Tests partitioning |
| medium | 1 | documented-still-hit | [Tests for dev/src/dev/kondo_ratchet.clj live in two namespaces (kondo-ratchet-test and kondo-ratchet-check-test); running only the obvious one missed an output-pinning test, and CI caught the regression](chris.claude.kondo-ratchet-tests-split-across-two-namespaces.md) | dev/src/dev/kondo_ratchet.clj, dev/test/metabase/core/kondo_ratchet_test.clj, dev/test/metabase/core/kondo_ratchet_check_test.clj, mage/src/mage/project_tests.clj |
| medium | 1 | documented-still-hit | [A test namespace without an :once initialize fixture triggers lazy `initialize :db` mid-test (via mt/user->id), which recreates the H2 app DB underneath rows the test already wrote -- "Table ... not found (this database is empty)](chris.claude.lazy-test-db-init-mid-test-recreates-h2-app-db.md) | test harness -- metabase.test.initialize, metabase.test.fixtures; enterprise/backend/test/metabase_enterprise/search/scoring_test.clj |
| medium | 1 | open | [test-agent :only runs a dev.* test namespace green, but CI's discovery (namespace-pattern ^metabase|hooks, dev dirs excluded) never runs it](chris.claude.test-agent-only-runs-namespaces-ci-never-discovers.md) | test/metabase/test_runner.clj, bin/test-agent, dev/src, dev/test |
| medium | 1 | unknown | [A ./bin/test-agent run that failed app-db init (Liquibase checksum) left its test JVM running for ~8 hours](chris.claude.test-jvm-orphaned-after-liquibase-checksum-failure.md) | ./bin/test-agent, test app-db init, `timeout N ./bin/test-agent ...` wrappers |
| low | 1 | open | [evals `test_cli_cases_cover_every_command_in_the_pinned_manifest` fails in any fresh worktree (it needs the installed pinned `mb` CLI) with no skip guard, so every full local run has one "known" red test](chris.claude.evals-pinned-cli-manifest-test-fails-in-fresh-worktree.md) | metabase/evals tests/test_surface_scenarios.py:71; CliSurface().command_names() |
| low | 1 | documented-still-hit | [A regex expected value in mb.hawk's `=?` is a full match (re-matches), so a "contains" pattern like #"environment variable" fails against a longer message](chris.claude.hawk-approx-regex-is-full-match.md) | mb.hawk =? (approximately-equal) in Metabase tests |

## misleading-signal (15)

| severity | occ | status | papercut | area |
|---|---|---|---|---|
| high | 1 | open* | [Test console log appender is FATAL-only, so "zero warnings in the test output" looks like proof that a warn path never fired](chris.claude.test-log-console-fatal-hides-warnings.md) | test_config/log4j2-test.xml; any debugging that greps ./bin/test-agent output for log/warn strings |
| medium | 2 | open* | [GitHub's collaborator-permission API returns `read` for any account on a public repo; two team.json audits built on it passed departed members, and a memory note told agents to use it as a membership check](chris.claude.gh-collaborator-permission-read-means-nothing.md) | gh api repos/metabase/metabase/collaborators/<user>/permission; .github/team.json; GitHub org teams (metabot, core-backend-metabot); release/github-slack-map.json; memory reference_team_json_stale_members.md |
| medium | 1 | documented-still-hit | [A new .github/scripts/*.unit.spec.ts passed jest locally (swc, no type check) but broke CI fe-type-check, and pulled FE + e2e CI onto a backend-only PR](chris.claude.jest-swc-passes-but-tsc-fails-github-scripts-spec.md) | .github/scripts/*.unit.spec.ts, jest.base.conf.js (@swc/jest), tsconfig.json include, .github/file-paths.yaml ci_scripts |
| medium | 1 | documented-still-hit* | [Kondo ratchet scanner walked gitignored build output (modules/drivers/*/target), producing phantom over-budget failures locally](chris.claude.kondo-ratchet-scanner-counts-gitignored-files.md) | dev/src/dev/kondo_ratchet.clj (scan / source-roots), mage kondo-ratchets, project-tests ratchets |
| medium | 1 | documented-still-hit | [`(require 'x-test :reload)` in a warm nREPL keeps deleted or renamed deftests, so run-tests reports failures from tests that no longer exist](chris.claude.nrepl-reload-keeps-deleted-deftests.md) | .claude/skills/clojure-eval/SKILL.md (tells agents to always use :reload), clj-nrepl-eval workflows, clojure.test |
| medium | 1 | open | [A rebase silently auto-merged a duplicate `defn` into a non-conflicting part of util.cljc; `git checkout --ours` on the conflicted files and kondo on only the edited files both looked clean](chris.claude.rebase-automerge-duplicate-defn-unlinted.md) | git rebase/restack workflow; src/metabase/util.cljc; post-rebase verification (kondo :redefined-var) |
| medium | 1 | documented-still-hit | [Single-test / test-pair "branch vs master" controls give confident false verdicts -- fresh-JVM effects (never-logged-in users, FieldValues not synced) fail on both sides](chris.claude.standalone-test-controls-mislead-about-branch-regressions.md) | backend test methodology -- ./bin/test-agent :only runs, CI order-dependent failures |
| medium | 1 | open | [After squash merges, "is this backup tag or branch superseded?" can't be answered by ancestry or merge-tree; the agent first recommended deleting 62 backup tags it had not verified](chris.claude.superseded-check-after-squash-merge.md) | git (squash-merge repos: metabase/evals, metabase/metabase), backup tags / local branches cleanup |
| low | 2 | documented-still-hit | [Every clj-kondo run prints "WARNING: error while reading .clj-kondo/inline-configs/metabase.lib.expression.cljc/config.edn (Invalid token ...)" — a corrupt cache that keeps regenerating](chris.claude.kondo-inline-config-invalid-token-warning.md) | .clj-kondo/inline-configs (gitignored kondo cache); src/metabase/lib/expression.cljc:366 ignore annotation; any `clj-kondo --lint` / `./bin/mage kondo` |
| low | 1 | open | [The first app-db test in a fresh worktree JVM took ~94s (Liquibase + class loading); the agent reported it as the test's CI cost and argued against a backport on that basis](chris.claude.first-test-timing-includes-cold-start.md) | bin/test-agent timing output, metabase.cmd.copy-test (setup-db! on H2), fresh worktrees |
| low | 1 | fixed | [A test that asserts "left the conflict untouched" by comparing `git status --porcelain` before and after proves nothing, because a conflicted path stays `UU` however its contents change](chris.claude.git-status-uu-hides-content-changes.md) | mage/test/mage/merge_kondo_ratchets_test.clj (refused helper); any test or script using git status to check a conflicted file is unchanged |
| low | 1 | fixed | [bin/merge-kondo-ratchets resolves mage via the script's own path; review bots flag it as a bug and the "fix" breaks the merge-driver tests](chris.claude.merge-driver-bin-dir-looks-wrong.md) | bin/merge-kondo-ratchets, mage/test/mage/merge_kondo_ratchets_test.clj |
| low | 1 | open | [When the local pgvector container has exited, entity-retrieval tests fail with c3p0 "attempt ... to checkout a Connection has timed out", which reads as pool exhaustion rather than "server not running](chris.claude.pgvector-down-surfaces-as-pool-checkout-timeout.md) | enterprise/backend/src/metabase_enterprise/semantic_search/db/datasource.clj (c3p0, checkoutTimeout 10000); local Docker container pgvector-semantic-test on :55432; semantic_search test_util |
| low | 1 | open | [In the evals repo, `uv run pyright scripts/configure-local-metabase` reports type errors that the project-wide `uv run pyright` (what CI runs) does not](chris.claude.pyright-single-file-run-reports-false-errors.md) | metabase/evals -- pyright (mise.toml ci task), extension-less scripts under scripts/ |
| low | 1 | open | [`roborev list` silently filters to the current branch, so reviews enqueued for other branches look missing](chris.claude.roborev-list-filters-current-branch.md) | roborev CLI (host daemon) |

## doc-gap (4)

| severity | occ | status | papercut | area |
|---|---|---|---|---|
| high | 1 | open | [roborev review --since <baseline> reviews every master commit a merged-in base brought along; the pr-rereview skill says the opposite](chris.claude.roborev-since-includes-merged-base-commits.md) | roborev CLI (`review --since`), ~/.claude/skills/pr-rereview/SKILL.md |
| low | 1 | open | [The no-frontend memory's "relaxed" clause uses the #80394 dialog-copy edit as its example, but the user rolled that exact edit back the same day, so the memory now points agents the wrong way](chris.claude.no-frontend-memory-cites-reverted-exception.md) | ~/.claude/projects/-Users-christruter-workspace-metabase-metabase/memory/feedback_no_frontend.md; ai-controls EnableAdvancedModal.tsx / DisableAdvancedModal.tsx |
| low | 1 | open | [Copy a Slack message to my clipboard": plain text with mrkdwn `<url|text>` pastes literally; Slack needs public.html on the pasteboard](chris.claude.slack-clipboard-needs-rich-html-not-mrkdwn.md) | macOS pasteboard + Slack composer; agent drafting under Chris's name |
| low | 1 | open | [CLAUDE.md says to run tests with ./bin/test-agent, but mage (babashka) tests aren't on its classpath; they need ./bin/mage -test [filter]](chris.claude.test-agent-cannot-run-mage-tests.md) | CLAUDE.md "Running Backend Tests", bin/test-agent, bin/mage -test, mage/test/** |

## tool-quirk (28)

| severity | occ | status | papercut | area |
|---|---|---|---|---|
| high | 1 | documented-still-hit | [Backgrounded `codex exec "<prompt>"` with stdin left open hangs forever on "Reading additional input from stdin...](chris.claude.codex-exec-blocks-on-open-stdin.md) | codex CLI (`codex exec`) invoked from Claude Code background Bash; memory reference_codex_cli_second_opinion.md |
| high | 1 | documented-still-hit | [git-spice restack replays zero commits (exit 0) when a branch's recorded base hash is stale, collapsing it onto its base](chris.claude.git-spice-stale-base-hash-silently-drops-commits.md) | git-spice (branch base.hash in refs/spice/data), ~/bin/restack-branches |
| medium | 3 | documented-still-hit | [The clj-paren-repair Edit hook silently "repairs" an intentionally unbalanced edit (open a wrapper form now, close it later) by closing forms early and corrupting distant forms, while Edit reports success; the recommended perl workaround leads to paren-counting fumbles](chris.claude.clj-paren-repair-hook-silently-restructures-edits.md) | ~/.claude/settings.json:15-23 PreToolUse/PostToolUse "clj-paren-repair-claude-hook --cljfmt" on Edit|Write; any .clj edit; memory reference_edit_hook_reindents_style_indent_macros.md |
| medium | 3 | unknown | [/code-review run as a background subagent stalls for 75 min because its finder subagents' completion notifications go to the root session, not to the orchestrator that spawned them](chris.claude.code-review-orchestrator-stalls-on-nested-subagent-notification.md) | Claude Code /code-review skill (orchestrator + finder/verifier subagents), task-notification routing, SendMessage, ReportFindings schema |
| medium | 2 | fixed | [Release-branch and backport automation hits silent git/GHA quirks - `git rm --ignore-unmatch` outside the sparse cone removes nothing, `cherry-pick --continue` exits 1 on an emptied pick under `bash -eo pipefail`, `echo` leaves `\n` literal, and a new `.clj-kondo/` file is gitignored](chris.claude.backport-cherry-pick-sparse-checkout-quirks.md) | .github/actions/create-backport/action.yml, .github/actions/create-backport/kondo-ratchets.sh, .github/workflows/cut-release-branch.yml, .gitignore (.clj-kondo allowlist) |
| medium | 2 | fixed | [Restacking the kondo-ratchet stack conflicts on the generated .clj-kondo/ratchets.edn at almost every commit; hand-rolled regen and rebase loops dropped keys, resurrected deleted files, falsely reported "done" in a worktree, and amended the wrong commit](chris.claude.ratchets-edn-restack-regen-conflicts.md) | .clj-kondo/ratchets.edn (generated budgets); dev/src/dev/kondo_ratchet.clj; gh stack / git-spice restack; bin/merge-kondo-ratchets |
| medium | 2 | open | [restack-branches --onto-trunk uses the local trunk ref, so a stale local master reports "Already restacked" / restacks onto an old base](chris.claude.restack-branches-stale-local-trunk.md) | ~/bin/restack-branches (dotfiles), git-spice trunk |
| medium | 1 | fixed | [ClickHouse resolves a same-SELECT alias before the source column, so `sum(cost_usd) AS cost_usd` followed by `countIf(cost_usd IS NULL)` becomes a nested aggregate (ILLEGAL_AGGREGATION) -- agent-written DDL shipped with it three times](chris.claude.clickhouse-select-alias-shadows-source-column.md) | ClickHouse SQL authoring (evals data stack schema design, ~/planning/evals/clickhouse-schema.md) |
| medium | 1 | unknown | [/code-review with no target, run from a clean master worktree, silently reviewed the last merged commit instead of the branch](chris.claude.code-review-no-target-reviews-head-commit.md) | Claude Code /code-review skill, multi-worktree workflow |
| medium | 1 | open | [`gh pr comment --body - <<EOF` posts a one-character comment \"-\"; stdin needs `--body-file -`, and the agent's comment-count check read it as success](chris.claude.gh-pr-comment-body-dash-posts-literal.md) | gh CLI (gh pr comment / gh pr create / gh issue comment), GitHub PR replies written by agents |
| medium | 1 | documented-still-hit | [Applying a hand-selected subset of hunks with `git apply --unidiff-zero` put pure-insertion hunks in the wrong place and broke a kondo hook](chris.claude.git-apply-unidiff-zero-misplaces-pure-insertions.md) | git apply -U0 / --unidiff-zero, scripted hunk selection |
| medium | 1 | open | [git log --since on master stops early (committer dates aren't monotonic), and a bare --since=YYYY-MM-DD means that day at the current time of day](chris.claude.git-log-since-window-on-squash-master.md) | git date windows in repo tooling (mage/src/mage/kondo_ratchet_report.clj, any "last week on master" report) |
| medium | 1 | open | [Rebasing a branch that merged master in silently drops edits that lived only in the merge commit's conflict resolution](chris.claude.git-rebase-drops-merge-commit-resolutions.md) | git rebase (default drops merges), rebasing other people's PR branches |
| medium | 1 | unknown | [Worktree-isolated forked subagent got a worktree on an old master commit, not the parent's branch; plus "too complex to verify" refusals for ordinary git loops](chris.claude.isolated-worktree-agent-starts-on-stale-base.md) | Claude Code Agent tool (fork, isolation: worktree) |
| medium | 1 | open | [`linear issue list` is an alias of `issue mine` filtered to state "unstarted", so "No issues found" hides open In Progress issues; there is no `issue search`](chris.claude.linear-issue-list-is-mine-unstarted-only.md) | linear CLI 2.5.0 (~/.claude/skills/linear-cli) |
| low | 3 | open | [gh CI inspection traps - `gh run list --commit <short sha>` returns nothing silently; `gh run view --log`/`--log-failed` refuses (or, with `2>/dev/null`, prints nothing) while any job in the run is still going, and ci-failures then says "No test failure details found](chris.claude.gh-ci-inspection-silent-empty-and-in-progress-logs.md) | gh CLI (run list / run view --log), ~/bin/ci-failures (dotfiles ci_common.clj), metabase CI job logs |
| low | 1 | documented-still-hit | [Running `clj-kondo --lint <file>` directly (not via ./bin/mage kondo) fails to load repo hooks and prints dozens of bogus errors](chris.claude.bare-clj-kondo-lint-misses-repo-hooks.md) | .clj-kondo config/hooks, ./bin/mage kondo, CLAUDE.md |
| low | 1 | open | [In the Bash tool, `grep` is a Claude Code shell function that runs embedded ugrep, so GNU/BSD BRE patterns like `^\+` fail with a ugrep syntax error](chris.claude.bash-grep-is-ugrep-shim.md) | Claude Code Bash tool shell snapshot (~/.claude/shell-snapshots/snapshot-zsh-*.sh, "Shadow find/grep with embedded bfs/ugrep") |
| low | 1 | open | [bb.edn is read with the EDN reader, so `@var` in a task expression fails with an EdnReader stack trace, and `./bin/mage ... | tail` shows only JVM frames](chris.claude.bb-edn-is-plain-edn-no-reader-macros.md) | bb.edn (mage task definitions), bin/mage |
| low | 1 | open | [~/bin/ci-retry exits on the first transient GitHub API error mid-poll, and piping it to tail reports exit 0, so CI goes unwatched while the task reads "completed](chris.claude.ci-retry-dies-on-transient-gh-error.md) | ~/bin/ci-retry (poll-until-done), background Bash tasks piped through tail |
| low | 1 | documented | [`codex exec resume` rejects flags placed after `resume`, and piping codex output through `tail` drops its worst-first top finding](chris.claude.codex-exec-resume-flag-order.md) | codex CLI 0.153.x (`codex exec`, `codex exec resume`), second-opinion workflow |
| low | 1 | documented-still-hit | [The post-Edit formatter hook reindented all of src/metabase/util/log.clj (macros/case forms), inflating a 32-line change to 83 lines of diff](chris.claude.edit-hook-reindents-log-clj-case-macros.md) | Claude Code post-Edit hook (cljfmt), src/metabase/util/log.clj, :style/indent macros |
| low | 1 | open | [`gh pr view --json body --jq .body` prints one more trailing newline than the file given to `gh pr edit --body-file`, so a diff-based concurrent-edit check always fails](chris.claude.gh-pr-body-roundtrip-extra-trailing-newline.md) | gh CLI (pr view --jq / pr edit --body-file); memory "Re-read PR desc before gh pr edit |
| low | 1 | unknown | [git-spice `--restack none` (space-separated) is parsed as a branch named "none"; the flag needs `--restack=none`](chris.claude.git-spice-optional-value-flag-needs-equals.md) | git-spice CLI (kong-style flags), `git-spice branch delete` |
| low | 1 | unknown | [kata in a worktree with no bound project -- `create` succeeds but `label/edit/show` fail, and the error's suggested fix (`kata#abc4`) uses a literal project name "kata" that does not exist](chris.claude.kata-unbound-workspace-qualified-ref-hint-misleads.md) | kata CLI (workspace/project binding, ref resolution); ~/.claude/skills/kata |
| low | 1 | unknown | [`roborev close` takes exactly one job id and 404s on canceled jobs; bulk-closing superseded reviews silently did nothing, then half-failed](chris.claude.roborev-close-single-id-and-canceled-jobs.md) | roborev CLI (close/address, comment, list --open) |
| low | 1 | open | [roborev's post-commit hook queues a review for every local commit, including commits later reset away, and --no-verify does not stop it](chris.claude.roborev-postcommit-fires-per-local-commit.md) | .husky/post-commit (roborev post-commit, installed by worktrunk; gitignored), roborev daemon queue |
| low | 1 | fixed | [Worktree-isolation guard refused git-free `python - <<PY > file` and `psql ... < file` commands as "too complex to verify that it stays inside the worktree](chris.claude.worktree-isolation-guard-refuses-heredoc-redirects.md) | Claude Code worktree-isolated sessions (Bash guard) |

## env-friction (13)

| severity | occ | status | papercut | area |
|---|---|---|---|---|
| high | 3 | open | [Parallel subagents share one scratchpad directory and one worktree; they read and clobber each other's working files and edit each other's batches](chris.claude.parallel-subagents-share-scratchpad.md) | Claude Code Agent tool (session scratchpad inherited by subagents), multi-agent batch refactors in one git worktree |
| high | 2 | fixed | [Stow per-file symlinks silently replaced by real files (write-temp-then-mv), so edits to skills/voice-samples and shell config landed on untracked shadow copies for months](chris.claude.stow-symlink-replaced-by-mv-shadow-copy.md) | ~/dotfiles stow packages (agents, bin, zsh, zed) -> ~/.claude/skills/*, ~/bin, ~/.zshrc, ~/.zshenv; sound-less-like-an-ai skill "append to voice-samples.md in this directory |
| high | 1 | open | [GNU-only flags fail on macOS BSD tools mid-script, and `git add -A && git-spice rebase continue` commits the half-written file (conflict markers included)](chris.claude.bsd-tools-silent-failure-commits-conflict-markers.md) | macOS userland (head, sed, grep \b) + git-spice rebase continue + ~/bin/restack-branches |
| high | 1 | open | [Agents `cat .lein-env` to learn which test app DB is in use; the file holds private keys and tokens, and ad-hoc `sed` redaction of "password" does not catch them](chris.claude.lein-env-cat-leaks-secrets.md) | gitignored .lein-env / .env in the metabase main checkout; worktree bootstrap (copying .lein-env, .env, .clj-kondo lib configs) |
| high | 1 | open | [In the zsh Bash tool, `while read -r path ...` overwrites $PATH (zsh ties `path` to PATH), so later commands in the loop are "not found" and `2>/dev/null` turns that into plausible empty results](chris.claude.zsh-path-variable-clobbers-path.md) | Claude Code Bash tool on macOS (commands run in zsh); any shell loop that names a variable `path` |
| medium | 36 | open | [Bash tool runs zsh on macOS: `echo ===` errors, unmatched globs abort commands, unquoted vars don't word-split, `$var:x` is a history modifier, BSD sed/cat/head flags fail -- often silently or as a misleading "0](chris.claude.zsh-and-bsd-shell-quirks.md) | Claude Code Bash tool on macOS (zsh via shell snapshot), BSD coreutils |
| medium | 3 | open | [Bash tool cwd resets after `cd` into a repo worktree but persists after `cd` into the scratchpad, so the next relative git command runs in the wrong place](chris.claude.bash-cwd-persists-in-scratchpad-but-resets-in-worktrees.md) | Claude Code Bash tool cwd handling (allowed working dirs vs. others) |
| medium | 2 | open | [Long Bash commands are auto-backgrounded -- including the agent's own "until ...; sleep" wait loops -- so agents poll output files, read half-written logs, spin on `true`, and lose track of which worktree a backgrounded `cd` ran in](chris.claude.auto-backgrounded-bash-polling-and-cwd.md) | Claude Code Bash tool (auto-background, blocked foreground sleep, Monitor), long test runs (./bin/test-agent) |
| medium | 2 | unknown | [evals worktrees symlink .env files and caches into .evals-worktree-shared, where resolved secrets and snapshot caches go stale and a second, real cache lives elsewhere](chris.claude.evals-shared-worktree-local-state-stale.md) | evals repo local setup: .env, .env-warehouses, .env-snapshots, .stats-encryption-secret-key symlinks; snapshots/home.py local_root; stats build --warehouse-root |
| medium | 1 | documented-still-hit | [Fresh worktrees have no node_modules, so husky's `bun run precommit` (lint-staged) exits 127 and agents habitually commit with --no-verify](chris.claude.husky-precommit-needs-node-modules-in-worktree.md) | .husky/pre-commit, package.json "precommit": "lint-staged", worktree setup (worktrunk) |
| low | 1 | open | [metabase/metabase rejects a push that updates more than 5 branches (GH013 ruleset), and rejects the whole push](chris.claude.github-push-five-ref-limit.md) | GitHub repository rulesets on metabase/metabase; pushing restacked stacks (git-spice, restack-branches) |
| low | 1 | open | [Artifact HTML previewed via `python3 -m http.server` (or a mermaid render page) shows mojibake for · and — because the page has no <meta charset> and the server sends none](chris.claude.local-html-preview-mojibake-no-charset.md) | artifact authoring workflow (scratchpad HTML + local http.server + claude-in-chrome screenshots) |
| low | 1 | open | [Validating data-stack terraform with the hashicorp/terraform Docker image: the entrypoint is terraform (so `sh -c` fails), 1.9 is below the modules' required_version, and init in the repo rewrites tracked lock files](chris.claude.terraform-docker-image-quirks.md) | metabase/data-stack terraform/modules/* (versions.tf required_version >= 1.10, .terraform.lock.hcl tracked); local validation with Docker |

## agent-behaviour (19)

| severity | occ | status | papercut | area |
|---|---|---|---|---|
| high | 4 | open | [Review-driven fix loops keep adding new defects, because each fix is checked only on the path the agent expected and its tests pass with the fix reverted](chris.claude.review-fix-churn-fixes-introduce-bugs.md) | evals repo stats app-DB build (stats/sql.py, stats/build.py, tests/test_stats_sanitize.py); review loops that pair roborev with /code-review |
| medium | 3 | open | [The agent tells the user, and writes in PRs, Linear, and review replies, things it has not checked ("next hourly run will create them", "checked against production", "confirmed, deterministic"), often right after raising the doubt itself](chris.claude.asserts-unverified-claims-as-fact.md) | agent reporting in chat, PR descriptions, Linear issues, and GitHub review replies (seen across stats-remote-sync / data-stack / evals) |
| medium | 2 | open | [Agent's `git add -A` in a worktree the user was editing concurrently committed the user's edits under the agent's commit message (and pushed)](chris.claude.git-add-all-sweeps-concurrent-user-edits.md) | git workflow in shared worktrees; branch moved under the agent |
| medium | 2 | open | [Agents hand-write the tool-generated `[kondo-keep]` marker, faking "verified false positive" provenance](chris.claude.hand-written-kondo-keep-markers.md) | mage/src/mage/kondo_ratchet.clj (keep-marker), inline :clj-kondo/ignore comments |
| medium | 2 | documented-still-hit | [Memory note recommending `--no-verify` led agents to bypass pre-commit hooks preemptively, without observing a hook failure](chris.claude.memory-no-verify-workaround-invites-preemptive-hook-bypass.md) | ~/.claude/.../memory/project_fix_unused_requires_pod_crash.md, MEMORY.md index line, git commit hooks (husky + lint-staged + mage fix-unused-requires) |
| medium | 2 | open | [Scripted `python3 - <<PY ... s.replace(old, new, 1)` edits hit the wrong anchor, abort half-way, or over-apply, and the agent then misreads the result](chris.claude.python-heredoc-str-replace-edits-misfire.md) | any repo; heavy in evals (stats/build.py, tests/test_stats_sanitize.py) and metabase kondo-ratchet branches |
| medium | 1 | open | [Repeated adversarial review-and-fix rounds on a design doc grow it monotonically (1,200 lines) and each round's fixes introduce the next round's HIGHs; user had to redirect to "converge on good enough](chris.claude.adversarial-review-loops-bloat-design-docs.md) | agent workflow -- design docs with review subagents (Codex/Fable), ~/planning/evals/clickhouse-schema.md + HTML artifact |
| medium | 1 | documented-still-hit | [Agent amends and force-pushes commits already on an open PR, despite a feedback memory that says to add fixup commits](chris.claude.agent-amends-pushed-commits.md) | git workflow on review branches; memory feedback_fixup_commits.md |
| medium | 1 | open | [Agent calls driver-job CI failures "infra/flake" from job names alone, while the same tests fail in every run](chris.claude.agent-labels-ci-failures-infra-from-job-names.md) | CI triage (ci-failures, gh pr checks), driver test jobs that run the whole backend suite |
| medium | 1 | unknown | [Refactors that move code between namespaces condense or drop the "why" comments that went with it (c3p0 deadlock note, driver-implementation caveats)](chris.claude.comments-dropped-when-moving-code.md) | cross-namespace moves in metabase src (PR #82011 break-requiring-resolve-cycles: analytics/prometheus.clj, driver/sql_jdbc/connection/pool_lock.clj, query_processor/util/persisted_cache.clj, pulse/task/send_pulses_trigger.clj, parameters/params.clj) |
| medium | 1 | open | [Agent verified a refactor with a hand-picked test namespace list that omitted the test for the file it changed most, committed and pushed a broken kondo hook test](chris.claude.handpicked-test-namespaces-miss-the-covering-test.md) | .clj-kondo/src/hooks/common/modules.clj, .clj-kondo/test/hooks/common/modules_test.clj, bin/test-agent :only |
| medium | 1 | open | [`linear issue update --description-file` replaces the whole description, overwriting the user's edits made since the agent last wrote it](chris.claude.linear-description-file-overwrites-user-edits.md) | linear CLI (v2.5.0), Linear issue descriptions; analogous to gh pr edit --body |
| medium | 1 | open | [The agent re-created work that already sat in an open PR (evals#113) in a different repo without opening that PR, and copied in the defect its reviewer had already flagged](chris.claude.reimplemented-open-pr-without-reading-its-reviews.md) | cross-repo work (stats-remote-sync data app, metabase/data-stack DDL, metabase/evals#113) |
| low | 3 | documented-still-hit | [Agents keep using Python (string replace / regex) to parse EDN and edit .clj source despite the no-Python-for-Clojure memory](chris.claude.python-used-on-clojure-edn-despite-memory.md) | memory feedback_no_python.md; .clj-kondo/ratchets.edn; src/metabase/util/queue.clj; src/metabase/search/lease.clj |
| low | 2 | documented-still-hit | [Drafted prose hedges, concedes, and uses stock phrases ("is the gate", "expect to", overly broad claims) even with the voice skill loaded; user corrects line by line](chris.claude.prose-concessions-and-slop-despite-voice-skill.md) | sound-less-like-an-ai / precis skills; CLAUDE.md and skill prose; argument docs |
| low | 1 | open | [The agent inferred that PR #125 belonged to someone else from its `codex/` branch prefix, and built a stacking recommendation on that; the branch was the user's own, created through the codex CLI](chris.claude.codex-branch-prefix-misread-as-other-author.md) | metabase/evals PRs #125/#126; codex CLI branch naming (`codex/<slug>`) |
| low | 1 | documented-still-hit | [Agent rewrites a PR description wholesale in its own long style instead of treating the user's hand-edited body (and sibling PR) as the template](chris.claude.pr-body-rewrite-ignores-user-edited-style.md) | PR descriptions (gh pr edit --body), pr-writing / sound-less-like-an-ai skills |
| low | 1 | open | [When resolving rebase conflicts, the agent kept the branch's wider API because the branch's own test exercised it, even though the only real caller had been deleted](chris.claude.rebase-resolution-keeps-api-for-its-own-test.md) | rebase conflict resolution; test/metabase/test/server/handler.clj (PR #77954) |
| low | 1 | open | [The "revert the fix and confirm the test fails" probe, done with `git checkout -- <file>`, wipes the other uncommitted fixes in the same file](chris.claude.revert-probe-git-checkout-discards-uncommitted-fix.md) | agent verification workflow (mutation/revert probes); evals core/capture.py; also metabase worktrees |
_* status has a qualifying note in the file._
