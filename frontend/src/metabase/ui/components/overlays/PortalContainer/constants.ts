// Kept dependency-free so overlay configs (e.g. Tooltip.config.tsx) can import
// the id without pulling in the component and creating an import cycle.
export const PORTAL_CONTAINER_ID = "metabase-portal-root";
