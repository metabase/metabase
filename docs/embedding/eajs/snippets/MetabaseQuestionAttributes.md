Attributes for the `<metabase-question>` web component.

Embeds a Metabase question (chart). Provide either `question-id` (for SSO embeds)
or `token` (for guest embeds), plus optional display configuration.
Use `question-id="new"` to embed the query builder exploration interface.

## Properties

<!-- [<snippet properties>] -->

| Property                                                      | Type                       | Default value                                    | Description                                                                                                                                                                                                                                                                   |
| :------------------------------------------------------------ | :------------------------- | :----------------------------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <a id="drills"></a> `drills?`                                 | `boolean`                  | `true`                                           | Whether to enable drill-through on the question. **Remarks** Pro/Enterprise                                                                                                                                                                                                   |
| <a id="entity-types"></a> `entity-types?`                     | (`"model"` \| `"table"`)[] | `undefined`                                      | Which entity types to show in the question's data picker, e.g. `["model", "table"]`. **Remarks** Pro/Enterprise, Guest embed                                                                                                                                                  |
| <a id="hidden-parameters"></a> `hidden-parameters?`           | `string`[]                 | `undefined`                                      | List of parameter names to hide from the question. **Remarks** Pro/Enterprise                                                                                                                                                                                                 |
| <a id="initial-sql-parameters"></a> `initial-sql-parameters?` | `object`                   | `undefined`                                      | Default values for SQL parameters, only applicable to native SQL questions, e.g. `{ "productId": "42" }`. **Remarks** Pro/Enterprise, Guest embed                                                                                                                             |
| <a id="is-save-enabled"></a> `is-save-enabled?`               | `boolean`                  | `false`                                          | Whether the save button is enabled. **Remarks** Pro/Enterprise                                                                                                                                                                                                                |
| <a id="question-id"></a> `question-id`                        | `string` \| `number`       | `undefined`                                      | The ID of the question to embed. Can be a regular ID or an [entity ID](https://www.metabase.com/docs/latest/installation-and-operation/serialization#entity-ids-work-with-embedding). Use `"new"` to embed the query builder. Only for SSO embeds — guest embeds use `token`. |
| <a id="target-collection"></a> `target-collection?`           | `string` \| `number`       | `undefined`                                      | The collection to save a question to. Values: regular ID, entity ID, `"personal"`, `"root"`. **Remarks** Pro/Enterprise                                                                                                                                                       |
| <a id="token"></a> `token?`                                   | `string`                   | `undefined`                                      | The token for guest embeds. Set automatically by the guest embed flow. **Remarks** Guest embed                                                                                                                                                                                |
| <a id="with-alerts"></a> `with-alerts?`                       | `boolean`                  | `false`                                          | Whether to show the alerts button. **Remarks** Pro/Enterprise                                                                                                                                                                                                                 |
| <a id="with-downloads"></a> `with-downloads?`                 | `boolean`                  | `true` on OSS/Starter, `false` on Pro/Enterprise | Whether to show download buttons for question results. **Remarks** Guest embed                                                                                                                                                                                                |
| <a id="with-title"></a> `with-title?`                         | `boolean`                  | `true`                                           | Whether to show the question title in the embed. **Remarks** Guest embed                                                                                                                                                                                                      |

<!-- [<endsnippet properties>] -->
