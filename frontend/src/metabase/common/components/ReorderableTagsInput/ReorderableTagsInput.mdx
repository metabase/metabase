import { Canvas, Meta } from "@storybook/addon-docs/blocks";
import * as Stories from "./ReorderableTagsInput.stories";

<Meta of={Stories} />

# ReorderableTagsInput

An input composed of sortable pills with optional external drag-and-drop between multiple inputs. Built on top of dnd-kit and Mantine `PillsInput`/`Combobox`.

## Examples

<Canvas of={Stories.Default} />

### Drag and drop between two inputs

<Canvas of={Stories.DragBetweenTwoInputs} />
