name: Prepare Release Scripts
description: Set up yarn and build release scripts

runs:
  using: composite
  steps:
    - name: Install Yarn v4
      run: npm install -g yarn@4.9.1
      shell: bash
    - name: Build release scripts
      shell: bash
      run: cd ${{ github.workspace }}/release && yarn install --immutable && yarn build
