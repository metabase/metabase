name: Prepare Release Scripts
description: Set up pnpm and build release scripts

runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v4
      with:
        version: 10
    - name: Build release scripts
      shell: bash
      run: cd ${{ github.workspace }}/release && pnpm install --frozen-lockfile && pnpm build
