{
  "name": "invite-user",
  "description": "Invite a user to Metabase. Can only be used with valid and complete email address (username + @ + domain + TLD).",
  "parameters": {
      "type": "object",
      "properties": {
          "email": {
              "type": "string",
              "description": "A valid and complete email address (username + @ + domain + TLD). If the email address is incomplete or missing a part like the TLD, ask the user for clarification before proceeding."
          }
      },
      "required": ["email"],
      "additionalProperties": false
  }
}
