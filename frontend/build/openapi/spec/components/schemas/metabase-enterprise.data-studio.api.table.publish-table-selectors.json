{
  "properties": {
    "collection_id": {
      "description": "value must be an integer greater than zero.",
      "minimum": 1,
      "type": "integer"
    },
    "database_ids": {
      "items": {
        "description": "value must be an integer greater than zero.",
        "minimum": 1,
        "type": "integer"
      },
      "type": "array"
    },
    "schema_ids": {
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "table_ids": {
      "items": {
        "description": "value must be an integer greater than zero.",
        "minimum": 1,
        "type": "integer"
      },
      "type": "array"
    }
  },
  "required": [
    "collection_id"
  ],
  "type": "object"
}
