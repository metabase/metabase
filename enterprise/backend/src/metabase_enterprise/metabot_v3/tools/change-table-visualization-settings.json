{
    "name": "change-table-visualization-settings",
    "description": "Use this tool to change viz settings in a table (e.g. show or hide columns). Make sure that the visualization type is `table` before calling this tool.",
    "parameters": {
        "type": "object",
        "properties": {
            "visible_columns": {
                "type": [
                    "array"
                ],
                "description": "List of columns to show in the table. To hide columns, include all but the ones you want to hide in the list.",
                "items": {
                    "type": "string"
                }
            }
        },
        "required": [
            "visible_columns"
        ],
        "additionalProperties": false
    }
}
