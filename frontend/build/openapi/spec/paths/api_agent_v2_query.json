{
  "post": {
    "description": "Execute a portable MBQL 5 representations JSON query and stream the results, with\n  continuation-token pagination.\n\n  Accepts either a JSON body (same shape as /v2/construct-query) or a `continuation_token`\n  from a previous response. Returns results with column metadata and an optional\n  `continuation_token` for fetching the next page.",
    "operationId": "post-api-agent-v2-query",
    "parameters": [],
    "requestBody": {
      "content": {
        "application/json": {
          "schema": {
            "$ref": "../components/schemas/metabase.agent-api.api.query-request.json"
          }
        }
      }
    },
    "responses": {
      "2XX": {
        "content": {
          "application/json": {
            "schema": {
              "$ref": "../components/schemas/metabase.agent-api.api.query-response.json"
            }
          }
        },
        "description": "Successful response"
      },
      "4XX": {
        "description": "Client error response"
      },
      "5XX": {
        "description": "Server error response"
      }
    },
    "summary": "POST /api/agent/v2/query",
    "tags": [
      "/api/agent"
    ]
  }
}
