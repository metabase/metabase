import type { ReactNode } from "react";

type ModelsSidebarLayoutProps = {
  children?: ReactNode;
};

export function ModelsSidebarLayout({ children }: ModelsSidebarLayoutProps) {
  return <>{children}</>;
}
