{
  "description": "Request schema for /v1/execute. Accepts a base64-encoded MBQL query.",
  "properties": {
    "query": {
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "query"
  ],
  "type": "object"
}