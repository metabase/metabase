describe("scenarios > admin > spinner", () => {
    it("should not spin forever when a save fails", () => {

    });
    it("should not spin forever if API request returns an error", () => {

    });
});