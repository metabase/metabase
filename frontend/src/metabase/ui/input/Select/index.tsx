export { Select } from "@mantine/core";
/*
import styled from "@emotion/styled";
Didn't find this way of doing it to be enjoyable. It works, sort of but I think we'll want to use the theme file primarily.
const Select = styled(MantineSelect)`
  .mantine-Select-label {
    font-weight: bold;
    color: ${({ theme }) => theme.colors.orion[6]};
    margin-bottom: 0.12rem;
  }
  .mantine-Select-input {
    &:focus {
      outline: 2px solid ${({ theme }) => theme.colors.ocean[1]}};
    }
  }

  .mantine-Select-item {
    color: ${({ theme }) => theme.colors.orion[7]};
    color: red !important;

    &[data-hovered="true"] {
      color: red !important;
    }
  }
`;
export { Select };
*/
