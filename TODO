1.  https://github.com/metabase/metabase/pull/4490

https://github.com/metabase/metabase/issues/4664
Речь идёт о „perCard Cache TTL-Settigng“. Cached Questions уже сделано и вроде бы работает.
Мне нужно  что бы кэш TTL можно было выставить на каждой карте (и так же на мануальных SQL-запросах) – по идее то что описано во второй ссылке
И тут важно (не уверен сделано ли оно уже или нет) – что бы перезагрузка результата производилась на «фоне» (in background) с помощью крона.

Frontend - ExtendedOptions.jsx, in actions create new Metabase Action

2.       https://github.com/metabase/metabase/issues/4308 - тут разрабатывается LDAP, но мне нужно на много легче.
нужно autoAuth – как у них уже сделано для гугла. Т.е. либо SAML либо какой ни будь другой простой метод SSO
При этом, пользователи будут уже заранее созданы в метабэйз с помощью Metabase-API

3.       Не большое дополнение на дэшборде – если использовать „multi charts“ на дэшборде, то нужны процессуальные соотношения
http://i.imgur.com/Tl10dwR.png



4. Return crate dependency before pull
