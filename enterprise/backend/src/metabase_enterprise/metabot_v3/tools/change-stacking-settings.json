{
  "name": "change-stacking-settings",
  "description": "Change the stacking settings for a bar or area chart. Make sure that there are at least two series in series_settings with display set to `bar` or `area` before calling this tool.",
  "parameters": {
    "type": "object",
    "properties": {
      "stack_type": {
        "type": ["string", "null"],
        "description": "The new stacking setting. Set to `null` to remove the stacking setting.",
        "enum": ["stacked", "normalized", null],
        "nullable": true
      }
    },
    "required": ["stack_type"],
    "additionalProperties": false
  }
}
