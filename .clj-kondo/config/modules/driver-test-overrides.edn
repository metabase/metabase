;; Modules that do NOT trigger driver tests in CI when changed, even though the dependency graph says
;; `driver`/`transforms` transitively depend on them (see `mage.modules/driver-affecting-overrides`).
;;
;; Every entry is an unverified bet that the coupling is not real enough to break a driver test. The
;; set should only shrink: its size is a module-boundary ratchet (`:driver-test-exempt-modules`), and
;; `metabase.core.modules-test` fails any entry the graph no longer justifies (not a declared module,
;; or no longer upstream of a driver-triggering module).
{:exempt-modules
 #{analytics
   analytics.interface
   analytics.internal-stats
   api
   api-keys
   api-scope
   api.eid-translation
   appearance
   audit-app
   audit-app.view-log
   auth-provider
   batch-processing
   channel
   collections
   content-verification
   core.initialization-status
   core.version
   dashboards
   documents
   embedding
   events
   formatter
   geojson
   interestingness
   lib.types
   mcp
   mcp.oauth-server
   metabot
   metabot.llm
   metabot.slackbot
   notification
   permissions
   premium-features
   public-sharing
   pulse
   remote-sync
   request
   sample-data
   search
   search.entity-retrieval
   server
   session
   session.login-history
   settings
   setup
   sso
   sso.auth-identity
   staleness
   startup
   system
   task
   task.history
   tiles
   timeline
   tracing
   users
   util
   util.classloader
   util.config
   warehouses.schema
   warehouses.secrets
   workspaces
   enterprise/api
   enterprise/serialization
   enterprise/sso
   enterprise/transforms
   enterprise/transforms.inspector
   enterprise/sso.scim}}
