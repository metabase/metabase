name: Build Uberjar

# This workflow builds and containerizes an uberjar and pushes it to dockerhub.

concurrency:
  group: uberjar-${{ inputs.commit || github.event.pull_request.head.sha || github.sha }}
  cancel-in-progress: false

on:
  workflow_dispatch:
    inputs:
      commit:
        description: "Optional full-length commit SHA-1 hash"
        type: string
        default: ''
        required: false
      containerize:
        description: "Set if the uberjar should be containerized."
        type: boolean
        default: false
        required: false
      registry:
        description: "Registry the containerized uberjar is pushed to. Ignored if containerize is false."
        type: choice
        options:
          - docker
          - ecr
        default: docker
        required: false
      repo:
        description: "Repository name the containerized uberjar is pushed to, without the registry host or the Docker Hub owner. Ignored if containerize is false."
        type: string
        default: ''
        required: false
      tag:
        description: "Tag for containerized uberjar. Ignored if containerize is false."
        type: string
        default: ''
        required: false
  workflow_call:
    inputs:
      commit:
        description: "Optional full-length commit SHA-1 hash"
        type: string
        default: ''
        required: false
      containerize:
        description: "Set if the uberjar should be containerized."
        type: boolean
        default: false
        required: false
      registry:
        description: "Registry the containerized uberjar is pushed to. Ignored if containerize is false."
        type: string
        default: docker
        required: false
      repo:
        description: "Repository name the containerized uberjar is pushed to, without the registry host or the Docker Hub owner. Ignored if containerize is false."
        type: string
        default: ''
        required: false
      tag:
        description: "Tag for containerized uberjar. Ignored if containerize is false."
        type: string
        default: ''
        required: false

jobs:
  setup:
    runs-on: ${{ vars.DEFAULT_RUNNER_KEY }}
    timeout-minutes: 5
    outputs:
      build_editions: ${{ steps.matrix.outputs.build_editions }}
      containerize_editions: ${{ steps.matrix.outputs.containerize_editions }}
      java_version: ${{ steps.dependencies.outputs.java_version }}
      node_version: ${{ steps.dependencies.outputs.node_version }}
    steps:
      - uses: actions/checkout@v6

      - name: Check for existing EE uberjar
        id: check-ee
        uses: ./.github/actions/fetch-artifact
        with:
          commit: ${{ inputs.commit || github.event.pull_request.head.sha || github.sha }}
          edition: ee
          fail-on-fetch-miss: false
          download: false

      - name: Check for existing OSS uberjar
        id: check-oss
        uses: ./.github/actions/fetch-artifact
        with:
          commit: ${{ inputs.commit || github.event.pull_request.head.sha || github.sha }}
          edition: oss
          fail-on-fetch-miss: false
          download: false

      - name: Create Matrices
        id: matrix
        uses: actions/github-script@v9
        with:
          script: | # js
            const hit = {
              ee: "${{ steps.check-ee.outputs.fetch-hit }}" === "true",
              oss: "${{ steps.check-oss.outputs.fetch-hit }}" === "true",
            };

            // Don't rebuild an edition whose uberjar already exists for this commit.
            const editions = ['ee', 'oss'].filter((edition) => !hit[edition]);

            core.info(`build: ${editions.join(', ') || '(nothing, both artifacts already exist)'}`);

            // If both repo and tag is set, only containerize and push the ee version since we don't want a 
            // race condition between the oss and ee versions pushed to the same repo and tag.
            const repo = '${{ inputs.repo }}';
            const tag = '${{ inputs.tag }}';
            const containerize_editions = repo && tag ? ['ee'] : ['ee', 'oss'];

            core.info(`containerize: ${containerize_editions.join(', ')}`);

            core.setOutput('build_editions', JSON.stringify(editions));
            core.setOutput('containerize_editions', JSON.stringify(containerize_editions));

      - name: Prepare release scripts
        if: ${{ contains(github.ref, 'release-x') }}
        uses: ./.github/actions/prepare-release-scripts

      - name: Get build dependencies
        if: ${{ contains(github.ref, 'release-x') }}
        uses: actions/github-script@v9
        id: dependencies
        with:
          script: | # js
            const { getBuildRequirements, getVersionFromReleaseBranch } = require('${{ github.workspace }}/release/dist/index.cjs');

            const version = getVersionFromReleaseBranch('${{ github.ref }}');
            const requirements = getBuildRequirements(version);

            core.setOutput('java_version', requirements.java);
            core.setOutput('node_version', requirements.node);

  build:
    needs: [setup]
    if: ${{ needs.setup.outputs.build_editions != '[]' }}
    name: build (${{ matrix.edition }})
    runs-on: ${{ vars.DEFAULT_RUNNER_KEY }}
    timeout-minutes: 15
    strategy:
      matrix:
        edition: ${{ fromJson(needs.setup.outputs.build_editions) }}
    env:
      MB_EDITION: ${{ matrix.edition }}
      INTERACTIVE: false
      SKIP_LICENSES: ${{ github.event_name == 'pull_request' && !inputs.containerize }}  # faster builds for dev
      # Emit bundle stats on trunk/release builds (tracked over time) and on PRs
      # (the bundle-size check reads them for both the PR head and its base ref).
      EMIT_BUNDLE_STATS: ${{ (github.ref_name == 'master' || startsWith(github.ref_name, 'release-') || github.event_name == 'pull_request') && 'true' || 'false' }}
    steps:
      - name: Check out the code
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.commit }}
      - name: Prepare front-end environment
        uses: ./.github/actions/prepare-frontend
        with:
          node-version: "${{ needs.setup.outputs.node_version || '22' }}"
      - name: Prepare back-end environment
        uses: ./.github/actions/prepare-backend
        with:
          java-version: "${{ needs.setup.outputs.java_version || 25 }}"
      - name: Build
        run: ./bin/build.sh
      - name: Upload bundle stats
        if: ${{ matrix.edition == 'ee' && env.EMIT_BUNDLE_STATS == 'true' }}
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: metabase-bundle-stats-${{ matrix.edition }}-${{ inputs.commit || github.event.pull_request.head.sha || github.sha }}
          path: target/bundle-stats
          if-no-files-found: warn
      - name: Mark with the commit hash
        run: git rev-parse --short HEAD > COMMIT-ID
        shell: bash
      - name: Calculate SHA256 checksum
        run: sha256sum ./target/uberjar/metabase.jar > SHA256.sum
        shell: bash
      - name: Upload JARs as artifact
        uses: actions/upload-artifact@v7
        with:
          # Same key the artifact lookups use, so a build for an explicitly dispatched commit
          # uploads where `setup` and `jar-healthcheck` go looking for it.
          name: metabase-${{ matrix.edition }}-${{ inputs.commit || github.event.pull_request.head.sha || github.sha }}-uberjar
          path: |
            ./target/uberjar/metabase.jar
            ./COMMIT-ID
            ./SHA256.sum

  jar-healthcheck:
    needs: [setup, build]
    # Health-check whatever we are about to containerize
    if: ${{ !failure() && !cancelled() && (inputs.containerize || github.ref_name == 'master' || startsWith(github.ref_name, 'release-')) }}
    name:  jar-healthcheck (${{ matrix.edition }})
    runs-on: ${{ vars.DEFAULT_RUNNER_KEY }}
    timeout-minutes: 10
    strategy:
      matrix:
        edition: ${{ fromJson(needs.setup.outputs.containerize_editions) }}
    steps:
      - name: Check out the code
        uses: actions/checkout@v6
      - name: Prepare JRE (Java Run-time Environment)
        uses: actions/setup-java@v5.2.0
        with:
          java-package: jre
          java-version: ${{ needs.setup.outputs.java_version || 25 }}
          distribution: "temurin"
      - run: java -version
      - name: Retrieve uberjar artifact
        uses: ./.github/actions/fetch-artifact
        with:
          commit: ${{ inputs.commit || github.event.pull_request.head.sha || github.sha }}
          edition: ${{ matrix.edition }}
      - name: Verify license files are present in uberjar
        run: |
          echo "Checking for license files in uberjar..."
          unzip -l ./metabase.jar | grep -q "license-backend-third-party.txt" || (echo "ERROR: license-backend-third-party.txt not found in uberjar" && exit 1)
          unzip -l ./metabase.jar | grep -q "license-frontend-third-party.txt" || (echo "ERROR: license-frontend-third-party.txt not found in uberjar" && exit 1)
          echo "Both license files found in uberjar"
      - name: Launch uberjar
        run: >-
          java --add-opens java.base/java.nio=ALL-UNNAMED -jar ./metabase.jar &
      - name: Wait for Metabase to start
        run: while ! curl 'http://localhost:3000/api/health' | grep '{"status":"ok"}'; do sleep 1; done

  containerize:
    needs: [setup, build, jar-healthcheck]
    if: ${{ inputs.containerize && !failure() && !cancelled() }}
    name: containerize (${{ matrix.edition }})
    strategy:
      matrix:
        edition: ${{ fromJson(needs.setup.outputs.containerize_editions) }}
    uses: ./.github/workflows/containerize-jar.yml
    secrets: inherit
    with:
      build-args: |
        GIT_COMMIT_SHA=${{ inputs.commit || github.event.pull_request.head.sha || github.sha }}
      commit: ${{ inputs.commit || github.event.pull_request.head.sha || github.sha }}
      edition: ${{ matrix.edition }}
      release: false
      registry: ${{ inputs.registry }}
      repo: ${{ inputs.repo }}
      tag: ${{ inputs.tag }}
