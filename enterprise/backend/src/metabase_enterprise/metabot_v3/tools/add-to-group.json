{
    "name": "add-to-group",
    "description": "Add a user to a group, identified by the group name",
    "parameters": {
        "type": "object",
        "properties": {
            "user_id": {
                "type": "integer",
                "description": "The ID of the user to add to the group. Can be obtained with the `get-user-id` tool if necessary."
            },
            "group_name": {
                "type": "string",
                "description": "The name of the group to add the user to."
            }
        },
        "required": ["user_id", "group_name"],
        "additionalProperties": false
    }
}
