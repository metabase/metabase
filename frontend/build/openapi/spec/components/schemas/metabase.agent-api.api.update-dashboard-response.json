{
  "description": "Returned by `update_dashboard`. `:dashcard_ids` is the post-mutation list of dashcard\n  ids in row/col order so the LLM can confirm what landed on the dashboard. `:tabs` lists\n  the dashboard's tabs in display order — the ids a mutation's `tab_id` accepts.",
  "properties": {
    "archived": {
      "type": "boolean"
    },
    "collection_id": {
      "oneOf": [
        {
          "description": "value must be an integer greater than zero.",
          "minimum": 1,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ]
    },
    "collection_path": {
      "type": "string"
    },
    "dashcard_ids": {
      "items": {
        "description": "value must be an integer greater than zero.",
        "minimum": 1,
        "type": "integer"
      },
      "type": "array"
    },
    "description": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "id": {
      "description": "value must be an integer greater than zero.",
      "minimum": 1,
      "type": "integer"
    },
    "name": {
      "minLength": 1,
      "type": "string"
    },
    "tabs": {
      "items": {
        "$ref": "./metabase.agent-api.api.dashboard-tab.json"
      },
      "type": "array"
    }
  },
  "required": [
    "id",
    "name",
    "collection_id",
    "collection_path",
    "description",
    "archived",
    "dashcard_ids",
    "tabs"
  ],
  "type": "object"
}
