## Track per-assistant-turn outcome on metabot_message: was the turn aborted (finished=false) or did it error (error text set).

databaseChangeLog:
  - logicalFilePath: migrations/062_update_migrations.yaml

  - changeSet:
      id: v62.2026-05-08T10:00:00
      author: sloansparger
      comment: Add finished column to metabot_message to flag client-aborted assistant turns
      changes:
        - addColumn:
            tableName: metabot_message
            columns:
              - column:
                  name: finished
                  type: ${boolean.type}
                  defaultValueBoolean: true
                  remarks: True for completed and errored assistant turns; false only when the client aborted mid-stream.
                  constraints:
                    nullable: false

  - changeSet:
      id: v62.2026-05-08T10:00:01
      author: sloansparger
      comment: Add error column to metabot_message capturing the failure reason for errored assistant turns
      changes:
        - addColumn:
            tableName: metabot_message
            columns:
              - column:
                  name: error
                  type: ${text.type}
                  remarks: JSON-encoded error payload from the agent's error stream part (message/type/data). NULL for healthy or aborted turns.
                  constraints:
                    nullable: true
