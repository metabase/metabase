diff --git a/node_modules/eslint-plugin-depend/lib/rules/ban-dependencies.js b/node_modules/eslint-plugin-depend/lib/rules/ban-dependencies.js
--- a/node_modules/eslint-plugin-depend/lib/rules/ban-dependencies.js
+++ b/node_modules/eslint-plugin-depend/lib/rules/ban-dependencies.js
@@ -6,6 +6,7 @@
     native: nativeReplacements.moduleReplacements,
     preferred: preferredReplacements.moduleReplacements
 };
+const replacementLists = new Map();
 const defaultPresets = ['microutilities', 'native', 'preferred'];
 export const rule = {
     meta: {
@@ -52,6 +53,10 @@
     },
     create: (context) => {
         const options = context.options[0];
+        const cacheKey = JSON.stringify(options ?? {});
+        if (replacementLists.has(cacheKey)) {
+            return createReplacementListener(context, replacementLists.get(cacheKey));
+        }
         const replacements = [];
         const presets = options?.presets ?? defaultPresets;
         const modules = options?.modules;
@@ -76,6 +81,7 @@
                 }
             }
         }
+        replacementLists.set(cacheKey, replacements);
         return {
             ...createReplacementListener(context, replacements)
         };
diff --git a/node_modules/eslint-plugin-depend/lib/util/imports.js b/node_modules/eslint-plugin-depend/lib/util/imports.js
--- a/node_modules/eslint-plugin-depend/lib/util/imports.js
+++ b/node_modules/eslint-plugin-depend/lib/util/imports.js
@@ -53,8 +53,25 @@
  * @param {string} source Module being imported
  * @return {void}
  */
+const replacementIndexes = new WeakMap();
 function replacementListenerCallback(context, replacements, node, source) {
-    const replacement = replacements.find((rep) => rep.moduleName === source || source.startsWith(`${rep.moduleName}/`));
+    let index = replacementIndexes.get(replacements);
+    if (!index) {
+        index = new Map();
+        replacements.forEach((replacement, position) => {
+            if (!index.has(replacement.moduleName)) index.set(replacement.moduleName, { replacement, position });
+        });
+        replacementIndexes.set(replacements, index);
+    }
+    let match;
+    let end = source.length;
+    while (end >= 0) {
+        const candidate = index.get(source.slice(0, end));
+        if (candidate && (!match || candidate.position < match.position)) match = candidate;
+        end = source.lastIndexOf('/', end - 1);
+        if (end === 0) break;
+    }
+    const replacement = match?.replacement;
     if (!replacement) {
         return;
     }
