(ns metabase.python-runner.init
  "Initialization namespace for the Python runner module.")

;; This namespace is intentionally left mostly empty. 
;; It serves as an initialization point for the python-runner module 
;; and ensures the module loads correctly when required.