issue: 57381
issue_url: https://github.com/metabase/metabase/issues/57381
fix_commits: ["e73e604d4c25cada6f907e5198b2e5679a70982c"]
fix_subject: "fix narrow columns widths in dashcards"
area: data-grid/column-widths
fe_be_boundary: false

mutation:
  kind: semantic
  layer: frontend
  patch_file: reconstruction.patch
  report_file: reconstruction-report.md
  base_sha: "56879decb0aac2ad61b5bc5737ebd4afc56e5f81"
  applies_cleanly: true
  fe_bucket: partial
  oracle_source: "surviving_pure_util_spec"
  reconstructed_by: worktree_subagent
  reconstruction_notes: >
    Removed the '* factor' proportional-expansion multiplier in maybeExpandColumnWidths, so narrow columns aren't widened to minGridWidth. A LAYOUT bug that is unit-killable because the width DECISION is an extracted pure util (validates FE-TEST-GUIDANCE).

validation:
  oracle: {kind: fe_unit, test: "frontend/src/metabase/data-grid/utils/maybe-expand-column-widths.unit.spec.ts", clean_baseline: pass, reconstructed_result: "1/4 fail (columns not proportionally expanded)"}
  repro_confirmed: true
  confidence: high

failure_shape:
  observed: true
  source: observed_unit
  symptom: >
    Narrow table columns in dashcards stay at content width instead of expanding to fill the grid, leaving whitespace.
  repro_failure: >
    'proportionally expands non-fixed columns': expected {colA:100,colB:150,colC:150}, received {100,100,100}.
  not_setup_crash: true

coverage:
  be_unit: {status: untested, killed_by: []}
  fe_unit: {status: kill, killed_by: ["frontend/src/metabase/data-grid/utils/maybe-expand-column-widths.unit.spec.ts"], clean_baseline: pass}
  notes: "FE reconstruction from partial bucket; oracle via surviving_pure_util_spec. Killed by FE unit (jest) => e2e repro cullable."
