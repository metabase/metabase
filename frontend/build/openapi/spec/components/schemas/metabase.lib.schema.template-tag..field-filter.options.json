{
  "description": "Options appended to the filter clause a Field Filter template tag generates. These get merged into the parameter\n  value the QP builds for the tag, so they are the same options as `:metabase.lib.schema.parameter/parameter.options`.",
  "properties": {
    "case-sensitive": {
      "type": "boolean"
    },
    "include-current": {
      "type": "boolean"
    }
  },
  "type": "object"
}
