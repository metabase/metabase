export type AdminPaneProps = {
  title?: React.ReactNode;
  description?: string;
  buttonText?: string;
  buttonAction?: () => void;
  buttonDisabled?: boolean;
  buttonLink?: string;
  headingContent?: React.ReactNode;
};
