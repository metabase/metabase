{
  "description": "Request shape for /v1/execute-sql. The LLM passes a raw SQL string against a target database.",
  "properties": {
    "database_id": {
      "description": "value must be an integer greater than zero.",
      "minimum": 1,
      "type": "integer"
    },
    "sql": {
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "database_id",
    "sql"
  ],
  "type": "object"
}
