import * as ML from "cljs/metabase.lib_metric.js";

// Ensure the CLJS module is imported
void ML;

// Wrapper functions will be added here as the CLJS module is implemented
// Example:
// export function createMetricDefinition(opts: MetricDefinitionOptions): MetricDefinition {
//   return ML.create_metric_definition(opts);
// }
