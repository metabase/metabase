{
  "additionalProperties": false,
  "description": "Request body for /v1/construct-native-query: a target database and a raw SQL string.",
  "properties": {
    "database_id": {
      "description": "value must be an integer greater than zero.",
      "minimum": 1,
      "type": "integer"
    },
    "sql": {
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "database_id",
    "sql"
  ],
  "type": "object"
}
