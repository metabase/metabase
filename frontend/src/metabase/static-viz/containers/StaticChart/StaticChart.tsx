import { TrendChart } from "metabase/static-viz/components/TrendChart";

const StaticChart = () => {
  return <TrendChart />;
};

// eslint-disable-next-line import/no-default-export -- deprecated usage
export default StaticChart;
