issue: 24018
issue_url: https://github.com/metabase/metabase/issues/24018
fix_commits: ["70c8d96c7c4cbc5adcc50b2b986ab49bfa7bae81"]
fix_pr: 33602
fix_subject: "Hide read-only items in the Archive (#33602)"
area: search/impl (was api/search)
fe_be_boundary: false

mutation:
  kind: semantic
  patch_file: reconstruction.patch
  base_sha: "56879decb0aac2ad61b5bc5737ebd4afc56e5f81"
  applies_cleanly: true      # RECONSTRUCTION patch applies; original fix diff does NOT (conflict)
  file_relocated: true
  reconstructed_by: worktree_subagent
  reconstruction_notes: >
    Stripped the (:archived? search-ctx) -> can-write? guards from check-permissions-for-model methods (:default/:metric/:segment/:database), reverting to unconditional can-read?/true. File relocated api/search.clj -> search/impl.clj. Left :document/:measure (added post-fix) untouched = correct scope.

validation:
  oracle:
    kind: be_unit
    test: "metabase.search.api-test/archived-search-results-with-no-write-perms-test"
    clean_baseline: pass
    reconstructed_result: "1/3 fail (read-only archived card leaks into search?archived=true)"
  repro_confirmed: true
  confidence: high

failure_shape:
  observed: true
  source: observed_unit
  symptom: >
    A user with only read (not write) access to an archived collection sees its items in Archive search results, which should require write access.
  repro_failure: >
    expected ['card test card is returned'], actual ('card test card' 'card test card is returned').
  not_setup_crash: true

coverage:
  be_unit: {status: kill, killed_by: ["metabase.search.api-test/archived-search-results-with-no-write-perms-test"], clean_baseline: pass}
  fe_unit: {status: untested, killed_by: []}
  notes: "Semantic-reconstruction batch-2. Conflict target reconstructed into current code + killed by shipped BE test => e2e repro cullable."
