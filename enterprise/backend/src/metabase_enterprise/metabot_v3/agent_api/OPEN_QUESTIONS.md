# Agent API - Open Questions

- Should `sub` claim be required to be an email, or support other user identifiers?
- Should we support API keys as an alternative to JWT?
- How should we handle rate limiting?
- How do we version breaking changes?
