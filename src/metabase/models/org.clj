(ns metabase.models.org
  (:use korma.core
        ))

(defentity Org
  (table :core_organization)
;  (entity-fields
;    :id
;    :name
;    :description
;    :logo_url
;    :inherits
;    :slug)
  )
