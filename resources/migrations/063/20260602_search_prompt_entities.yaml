## Create search_prompt_entities table for storing search prompts and their associated entities.
##
## NOTE: this was briefly authored in 062/ with v62 IDs; it now lives cleanly in 063 (the target
## release). A preview env that already ran the old v62 version will need its appdb reset — prod
## never ran it.

databaseChangeLog:
  - logicalFilePath: migrations/063_update_migrations.yaml

  - changeSet:
      id: v63.2026-06-02T00:00:00
      author: lbrdnk
      comment: Create search_prompt_entities table
      changes:
        - createTable:
            tableName: search_prompt_entities
            remarks: Stores search prompts paired with their associated entities
            columns:
              - column:
                  name: id
                  type: int
                  remarks: Unique ID
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: prompt
                  type: ${text.type}
                  remarks: The search prompt text
                  constraints:
                    nullable: false
              - column:
                  name: entities
                  type: ${text.type}
                  remarks: JSON array of associated entities
                  constraints:
                    nullable: false
              - column:
                  name: verified
                  type: ${boolean.type}
                  remarks: Whether this prompt-entity mapping has been verified
                  defaultValueBoolean: false
                  constraints:
                    nullable: false
      rollback:
        - dropTable:
            tableName: search_prompt_entities

  - changeSet:
      id: v63.2026-06-02T00:00:01
      author: christruter
      comment: Add created_at to search_prompt_entities (backfilled, not null)
      changes:
        - addColumn:
            tableName: search_prompt_entities
            columns:
              # defaultValueComputed backfills existing rows with the current timestamp so NOT NULL holds.
              - column:
                  name: created_at
                  type: ${timestamp_type}
                  defaultValueComputed: current_timestamp
                  remarks: When the prompt-entity mapping was created
                  constraints:
                    nullable: false

  - changeSet:
      id: v63.2026-06-02T00:00:01.1
      author: christruter
      comment: Add updated_at to search_prompt_entities (backfilled, not null)
      changes:
        - addColumn:
            tableName: search_prompt_entities
            columns:
              - column:
                  name: updated_at
                  type: ${timestamp_type}
                  defaultValueComputed: current_timestamp
                  remarks: When the prompt-entity mapping was last updated
                  constraints:
                    nullable: false

  - changeSet:
      id: v63.2026-06-02T00:00:02
      author: lbrdnk
      comment: Add type column to search_prompt_entities (canonical or sources)
      changes:
        - addColumn:
            tableName: search_prompt_entities
            columns:
              - column:
                  name: type
                  type: varchar(50)
                  remarks: Discriminates the shape of entities — canonical (single entity) or sources (list of sources)
                  defaultValue: canonical
                  constraints:
                    nullable: false

  - changeSet:
      id: v63.2026-06-02T00:00:03
      author: christruter
      comment: Add entity_id to search_prompt_entities for serialization
      changes:
        - addColumn:
            tableName: search_prompt_entities
            columns:
              - column:
                  name: entity_id
                  type: char(21)
                  remarks: Random NanoID tag for unique identity (serialization)
                  constraints:
                    nullable: true
                    unique: true
