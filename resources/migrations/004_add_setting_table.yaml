databaseChangeLog:
- changeSet:
    author: cammsaul
    changes:
    - createTable:
        columns:
        - column:
            constraints:
              nullable: false
              primaryKey: true
            name: key
            type: varchar(254)
        - column:
            constraints:
              nullable: false
            name: value
            type: varchar(254)
        tableName: setting
    id: '4'
