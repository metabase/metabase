{
  "description": "Schema for a metric in the /dimensions response: dimensions referenced by id only.",
  "properties": {
    "collection": {
      "oneOf": [
        {
          "properties": {
            "id": {
              "oneOf": [
                {
                  "description": "value must be an integer greater than zero.",
                  "minimum": 1,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ]
            },
            "name": {
              "type": "string"
            }
          },
          "required": [
            "id",
            "name"
          ],
          "type": "object"
        },
        {
          "type": "null"
        }
      ]
    },
    "collection_id": {
      "oneOf": [
        {
          "description": "value must be an integer greater than zero.",
          "minimum": 1,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ]
    },
    "database_id": {
      "oneOf": [
        {
          "description": "value must be an integer greater than zero.",
          "minimum": 1,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ]
    },
    "description": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "dimension_ids": {
      "items": {},
      "type": "array"
    },
    "dimension_mappings": {
      "oneOf": [
        {
          "items": {
            "$ref": "./metabase.metrics.core.dimension-mapping.json"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ]
    },
    "id": {
      "description": "value must be an integer greater than zero.",
      "minimum": 1,
      "type": "integer"
    },
    "in_library": {
      "type": "boolean"
    },
    "name": {
      "type": "string"
    },
    "result_column_name": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "required": [
    "id",
    "name",
    "description",
    "collection_id",
    "dimension_ids"
  ],
  "type": "object"
}
