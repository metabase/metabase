issue: 47951
issue_url: https://github.com/metabase/metabase/issues/47951
fix_commits: ["2b82fc5785cad82cf32464861048e3541575a017"]
fix_subject: "Support remapping for multiple fields (#59804)"
area: metabase-lib/Field
fe_be_boundary: false

mutation:
  kind: semantic
  layer: frontend
  patch_file: reconstruction.patch
  report_file: reconstruction-report.md
  base_sha: "56879decb0aac2ad61b5bc5737ebd4afc56e5f81"
  applies_cleanly: true
  reconstructed_by: worktree_subagent
  reconstruction_notes: >
    Reverted Field.remappedField static to single-field-only logic (fields.length===1), dropping the fix's multi-field-with-identical-target support.

validation:
  oracle: {kind: fe_unit, test: "frontend/src/metabase-lib/v1/metadata/Field.unit.spec.ts", clean_baseline: pass, reconstructed_result: "1/32 fail (multi-field remap -> null)"}
  repro_confirmed: true
  confidence: high

failure_shape:
  observed: true
  source: observed_unit
  symptom: >
    A parameter targeting multiple fields that share one identical remap target no longer remaps (returns null).
  repro_failure: >
    remappedField([fkField1,fkField2]) expected pkField, received null.
  not_setup_crash: true

coverage:
  be_unit: {status: untested, killed_by: []}
  fe_unit: {status: kill, killed_by: ["frontend/src/metabase-lib/v1/metadata/Field.unit.spec.ts"], clean_baseline: pass}
  notes: "FE semantic-reconstruction batch-2. Killed by shipped FE unit (jest) test => e2e repro cullable."
