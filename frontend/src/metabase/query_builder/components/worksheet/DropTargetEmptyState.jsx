import React from "react";

const DropTargetEmptyState = ({ message }) => (
  <div className="flex-full flex layout-centered">
    <span>{message}</span>
  </div>
);

export default DropTargetEmptyState;
