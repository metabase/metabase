name: Serialization

on:
  push:
    branches:
      - 'master'
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref && github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  should-run:
    uses: ./.github/workflows/util.should-run.yml
    with:
      any-of: serialization_baseline

  validate-serialization-baseline:
    needs: should-run
    if: needs.should-run.outputs.changed == 'true'
    runs-on: ${{ vars.DEFAULT_RUNNER_KEY }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - name: Prepare front-end environment
        uses: ./.github/actions/prepare-frontend
      - name: Validate serialization baseline schema
        run: bunx @metabase/representations validate-schema --folder test_resources/serialization_baseline
