issue: 59984
issue_url: https://github.com/metabase/metabase/issues/59984
fix_commits: ["0fe24156a36066b50291e34af837dc4d8287654c"]
fix_subject: "fix: issues generating valid urls for map tiles"
area: viz/map
fe_be_boundary: false

mutation:
  kind: semantic
  layer: frontend
  patch_file: reconstruction.patch
  report_file: reconstruction-report.md
  base_sha: "56879decb0aac2ad61b5bc5737ebd4afc56e5f81"
  applies_cleanly: true
  reconstructed_by: worktree_subagent
  reconstruction_notes: >
    Removed the x-ray auto-dashboard handling block in getTileUrl (map.ts), so a string dashboardId like '/auto/dashboard/...' falls through to dashboardTileUrl and produces a malformed URL.

validation:
  oracle: {kind: fe_unit, test: "frontend/src/metabase/visualizations/lib/map.unit.spec.ts", clean_baseline: pass, reconstructed_result: "1/18 fail (x-ray dashboard tile URL)"}
  repro_confirmed: true
  confidence: high

failure_shape:
  observed: true
  source: observed_unit
  symptom: >
    Map tile URLs for x-ray auto-dashboards are malformed (the '/auto/...' dashboardId is interpolated into the path).
  repro_failure: >
    'should handle an x-ray dashboard': toBe expected /api/tiles/... received /api/tiles//auto/dashboard/table/5/...
  not_setup_crash: true

coverage:
  be_unit: {status: untested, killed_by: []}
  fe_unit: {status: kill, killed_by: ["frontend/src/metabase/visualizations/lib/map.unit.spec.ts"], clean_baseline: pass}
  notes: "FE semantic-reconstruction batch-2. Killed by shipped FE unit (jest) test => e2e repro cullable."
