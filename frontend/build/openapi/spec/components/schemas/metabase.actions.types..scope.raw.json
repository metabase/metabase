{
  "anyOf": [
    {
      "properties": {
        "dashboard-id": {
          "description": "value must be an integer greater than zero.",
          "minimum": 1,
          "type": "integer"
        },
        "type": {
          "type": "string"
        }
      },
      "required": [
        "dashboard-id"
      ],
      "type": "object"
    },
    {
      "properties": {
        "dashcard-id": {
          "description": "value must be an integer greater than zero.",
          "minimum": 1,
          "type": "integer"
        },
        "type": {
          "type": "string"
        }
      },
      "required": [
        "dashcard-id"
      ],
      "type": "object"
    },
    {
      "properties": {
        "card-id": {
          "description": "value must be an integer greater than zero.",
          "minimum": 1,
          "type": "integer"
        },
        "type": {
          "type": "string"
        }
      },
      "required": [
        "card-id"
      ],
      "type": "object"
    },
    {
      "properties": {
        "model-id": {
          "description": "value must be an integer greater than zero.",
          "minimum": 1,
          "type": "integer"
        },
        "type": {
          "type": "string"
        }
      },
      "required": [
        "model-id"
      ],
      "type": "object"
    },
    {
      "properties": {
        "table-id": {
          "description": "value must be an integer greater than zero.",
          "minimum": 1,
          "type": "integer"
        },
        "type": {
          "type": "string"
        }
      },
      "required": [
        "table-id"
      ],
      "type": "object"
    },
    {
      "properties": {
        "type": {
          "type": "string"
        },
        "webhook-id": {
          "description": "value must be an integer greater than zero.",
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "webhook-id"
      ],
      "type": "object"
    },
    {
      "properties": {
        "type": {
          "type": "string"
        },
        "unknown": {
          "enum": [
            "model-action"
          ],
          "type": "string"
        }
      },
      "required": [
        "unknown"
      ],
      "type": "object"
    }
  ]
}
