## The perms/workspaces data permission was removed; workspace management is now superuser-only.
## Delete any rows for it left in data_permissions.

databaseChangeLog:

  - changeSet:
      id: v63.2026-06-10T00:00:00
      author: ranquild
      comment: Remove perms/workspaces rows from data_permissions
      changes:
        - sql:
            sql: >-
              DELETE FROM data_permissions WHERE perm_type = 'perms/workspaces'
      rollback: # nothing to do; the permission type no longer exists
