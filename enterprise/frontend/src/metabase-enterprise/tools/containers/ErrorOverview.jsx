import React, { Component } from "react";
import PropTypes from "prop-types";
import { t } from "ttag";

export default class ErrorOverview extends Component {
  render() {
    return (
      <div>blah blah blah</div>
    );
  }
}
