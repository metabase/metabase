{
  "name": "show-results-to-user",
  "description": "Show the results of a query you ran to the user.",
  "parameters": {
    "type": "object",
    "properties": {
      "query_id": {
        "type": "string",
        "description": "The ID of the query you want to display to the user."
      }
    },
    "required": ["query_id"],
    "additionalProperties": false
  }
}
