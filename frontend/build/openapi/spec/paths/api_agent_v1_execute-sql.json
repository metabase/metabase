{
  "post": {
    "description": "Execute a raw SQL query against a database. Returns rows + column metadata.\n\n  Requires the user to have native-query permission on the target database; the QP\n  middleware enforces this. The instance-level `mcp-execute-sql-enabled` setting\n  must also be on (it is by default).",
    "operationId": "post-api-agent-v1-execute-sql",
    "parameters": [],
    "requestBody": {
      "content": {
        "application/json": {
          "schema": {
            "$ref": "../components/schemas/metabase.agent-api.api.execute-sql-request.json"
          }
        }
      }
    },
    "responses": {
      "2XX": {
        "content": {
          "application/json": {
            "schema": {
              "$ref": "../components/schemas/metabase.agent-api.api.execute-query-response.json"
            }
          }
        },
        "description": "Successful response"
      },
      "4XX": {
        "description": "Client error response"
      },
      "5XX": {
        "description": "Server error response"
      }
    },
    "summary": "POST /api/agent/v1/execute-sql",
    "tags": [
      "/api/agent"
    ]
  }
}
