issue: 47887
issue_url: https://github.com/metabase/metabase/issues/47887
fix_commits: ["63a3b4b3f5e18754bd7d7ef902c02a8cac6d3635"]
fix_subject: "Use case expression type inference logic similar to coalesce"
area: lib/schema/conditional
fe_be_boundary: false

mutation:
  kind: semantic
  patch_file: reconstruction.patch
  report_file: reconstruction-report.md
  base_sha: "56879decb0aac2ad61b5bc5737ebd4afc56e5f81"
  applies_cleanly: true
  file_relocated: false
  reconstructed_by: worktree_subagent
  oracle_source: modified_deftest    # fix ADDED assertions to an existing deftest
  reconstruction_notes: >
    Removed the unknown-type propagation branch in best-return-type (if either arg is ::type.unknown -> unknown). The fix's semantics originally lived in type-of-method :case but MIGRATED to best-return-type via a later refactor; reverted the invariant at its current home.

validation:
  oracle: {kind: be_unit, test: "metabase.lib.schema.expression.conditional-test/best-return-type-test", clean_baseline: pass, reconstructed_result: "1/1 fail (expected ::type.unknown, actual :type/*)"}
  repro_confirmed: true
  confidence: high

failure_shape:
  observed: true
  source: observed_unit
  symptom: >
    A case/coalesce expression whose arg type is unknown collapses to a wrong concrete type (:type/*) instead of propagating unknown.
  repro_failure: >
    (best-return-type :type/Integer ::type.unknown) expected ::type.unknown, actual :type/*.
  not_setup_crash: true

coverage:
  be_unit: {status: kill, killed_by: ["metabase.lib.schema.expression.conditional-test/best-return-type-test"], clean_baseline: pass}
  fe_unit: {status: untested, killed_by: []}
  notes: "Semantic-reconstruction batch-5 (no_added_deftest bucket; modified-deftest oracle). Killed by shipped BE test => e2e repro cullable."
