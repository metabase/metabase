{
  "name": "change-goal-line",
  "description": "Change the goal line settings for a chart. A chart may have only one goal line. Make sure that the visualization type is `line`, `bar`, `area`, `combo`, `scatter`, `waterfall`, or `row` before calling this tool.",
  "parameters": {
    "type": "object",
    "properties": {
      "goal_value": {
        "type": ["number", "null"],
        "description": "The goal value.",
        "nullable": true
      },
      "show_goal": {
        "type": ["boolean", "null"],
        "description": "Whether to show the goal line.",
        "nullable": true
      },
      "goal_label": {
        "type": ["string", "null"],
        "description": "The goal line label.",
        "nullable": true
      }
    },
    "required": ["goal_value", "show_goal", "goal_label"],
    "additionalProperties": false
  }
}
