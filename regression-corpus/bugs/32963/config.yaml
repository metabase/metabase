issue: 32963
issue_url: https://github.com/metabase/metabase/issues/32963
fix_commits: ["1ce8e2c2cfa0e7bc7c0a89dd11814b5e0255c79b"]
fix_subject: "Don't lock display for models (#33047)"
area: qb/initializeQB
fe_be_boundary: false

mutation:
  kind: semantic
  layer: frontend
  patch_file: reconstruction.patch
  report_file: reconstruction-report.md
  base_sha: "56879decb0aac2ad61b5bc5737ebd4afc56e5f81"
  applies_cleanly: true
  file_relocated: false
  reconstructed_by: worktree_subagent
  reconstruction_notes: >
    Removed the `type === 'question'` guard so lockDisplay() applies to all saved questions (bug: models get their display locked).

validation:
  oracle: {kind: fe_unit, test: "frontend/src/metabase/query_builder/actions/core/initializeQB.unit.spec.ts", clean_baseline: pass, reconstructed_result: "2/135 fail (models lock display)"}
  repro_confirmed: true
  confidence: high

failure_shape:
  observed: true
  source: observed_unit
  symptom: >
    Opening a model (dataset) wrongly locks its display type instead of letting it auto-select.
  repro_failure: >
    'models > doesn't lock display': expected falsy, received true.
  not_setup_crash: true

coverage:
  be_unit: {status: untested, killed_by: []}
  fe_unit: {status: kill, killed_by: ["frontend/src/metabase/query_builder/actions/core/initializeQB.unit.spec.ts"], clean_baseline: pass}
  notes: "FE semantic-reconstruction batch-3. Killed by shipped FE unit (jest) test => e2e repro cullable."
