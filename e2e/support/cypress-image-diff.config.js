const config = {
  ROOT_DIR: "../../cypress",
  SCREENSHOTS_DIR: "snapshots",
  NAME_TEMPLATE: "[specName]-[givenName]",
  REPORT_DIR: "report",
  JSON_REPORT: {
    FILENAME: "report",
    OVERWRITE: true,
  },
};

module.exports = config;
