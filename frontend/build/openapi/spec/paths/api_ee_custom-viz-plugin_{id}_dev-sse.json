{
  "get": {
    "description": "Proxy Server-Sent Events from the plugin's dev server.\n   Connects to `{dev_bundle_url}/__sse` and forwards events to the browser.\n   This avoids the need for a CSP exception for the dev server origin.\n   Requires custom viz plugin dev mode to be enabled.",
    "operationId": "get-api-ee-custom-viz-plugin-id-dev-sse",
    "parameters": [
      {
        "description": "value must be an integer greater than zero.",
        "in": "path",
        "name": "id",
        "required": true,
        "schema": {
          "minimum": 1,
          "type": "integer"
        }
      }
    ],
    "responses": {
      "2XX": {
        "content": {
          "application/json": {
            "schema": {}
          }
        },
        "description": "Successful response"
      },
      "4XX": {
        "description": "Client error response"
      },
      "5XX": {
        "description": "Server error response"
      }
    },
    "summary": "GET /api/ee/custom-viz-plugin/{id}/dev-sse",
    "tags": [
      "/api/ee/custom-viz-plugin"
    ]
  }
}
