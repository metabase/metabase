{
  "name": "sort-query",
  "description": "Sort the results of the current query.",
  "parameters": {
    "type": "object",
    "properties": {
      "column": {
        "type": "string",
        "description": "The column to apply sorting for. Choose an appropriate column name based on user input from orderable_columns list."
      },
      "direction": {
        "type": "string",
        "description": "Optional direction of sorting. Can be ascending or descending. Ascending by default.",
        "enum": ["asc", "desc"]
      }
    },
    "additionalProperties": false,
    "required": ["column", "direction"]
  }
}
