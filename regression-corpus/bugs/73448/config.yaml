issue: 73448
issue_url: https://github.com/metabase/metabase/issues/73448
fix_commits: ["258dbb310b409e2e79e642ae410ab51b5afd8300"]
fix_subject: "Fix bar chart dimension click behavior on dashboards"
area: viz/click-actions
fe_be_boundary: false

mutation:
  kind: semantic
  layer: frontend
  patch_file: reconstruction.patch
  report_file: reconstruction-report.md
  base_sha: "56879decb0aac2ad61b5bc5737ebd4afc56e5f81"
  applies_cleanly: true
  reconstructed_by: worktree_subagent
  reconstruction_notes: >
    Removed the dimension-fallback branch in getClickBehavior (dashboard-click-drill.js), so a clicked bar no longer falls back to its dimension column's configured click behavior.

validation:
  oracle: {kind: fe_unit, test: "frontend/src/metabase/dashboard/utils/dashboard-click-drill.unit.spec.js", clean_baseline: pass, reconstructed_result: "2/6 fail (the #73448 assertions)"}
  repro_confirmed: true
  confidence: high

failure_shape:
  observed: true
  source: observed_unit
  symptom: >
    Clicking a bar on a dashboard bar chart doesn't use the click behavior configured on the dimension column.
  repro_failure: >
    'falls back to click behavior on a clicked dimension (#73448)': toBe(dimensionClickBehavior); DashboardClickAction toHaveLength(1) got 0.
  not_setup_crash: true

coverage:
  be_unit: {status: untested, killed_by: []}
  fe_unit: {status: kill, killed_by: ["frontend/src/metabase/dashboard/utils/dashboard-click-drill.unit.spec.js"], clean_baseline: pass}
  notes: "FE semantic-reconstruction batch-2. Killed by shipped FE unit (jest) test => e2e repro cullable."
