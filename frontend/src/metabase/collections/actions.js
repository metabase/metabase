import { createAction } from "redux-actions";

// action constants
export const SET_OPEN_COLLECTIONS = "SET_OPEN_COLLECTIONS";

// action creators
export const setOpenCollections = createAction(SET_OPEN_COLLECTIONS);
