describe("CreateAlertModalContent", () => {
  // TODO: implement

  // eslint-disable-next-line jest/expect-expect
  it("should display Slack channel by default if it is configured", () => {});

  // eslint-disable-next-line jest/expect-expect
  it("should not display goal related settings if there is no goal configured", () => {
    // "When results go above the goal line"
    // "When results go below the goal line"
  });
});
