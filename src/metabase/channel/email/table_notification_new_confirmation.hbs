{{> metabase/channel/email/_header.hbs }}
    <p>This is a confirmation that your notification for changes to the <a href="{{{table-url payload.object.payload.table.db_id payload.object.payload.table.id}}}">{{payload.object.payload.table.display_name}}</a> table has been set up.</p>
    <p>You will be notified when {{#if (eq payload.object.payload.event_name "event/row.created")}}new rows are created{{else if (eq payload.object.payload.event_name "event/row.updated")}}existing rows are updated{{else if (eq payload.object.payload.event_name "event/row.deleted")}}rows are deleted{{else}}changes occur{{/if}} in this table.</p>
{{> metabase/channel/email/_footer.hbs}}
