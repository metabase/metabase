{
  "name": "get-query-columns",
  "description": "Lists available columns for the query as JSON. Each column has an id and name. Can be used to validate and coerce user input.",
  "parameters": {
    "type": "object",
    "properties": {
      "source": {
        "type": "object",
        "properties": {
          "id": {
            "type": "number",
            "description": "Data source ID."
          },
          "database_id": {
            "type": "number",
            "description": "Database ID."
          },
          "type": {
            "type": "string",
            "description": "Data source type.",
            "enum": ["table", "model"]
          }
        },
        "required": [
          "id",
          "database_id",
          "type"
        ],
        "additionalProperties": false
      }
    },
    "required": [
      "source"
    ],
    "additionalProperties": false
  }
}
