diff --git a/node_modules/eslint-plugin-ttag/dist/rules/no-module-declaration.js b/node_modules/eslint-plugin-ttag/dist/rules/no-module-declaration.js
--- a/node_modules/eslint-plugin-ttag/dist/rules/no-module-declaration.js
+++ b/node_modules/eslint-plugin-ttag/dist/rules/no-module-declaration.js
@@ -15,11 +15,19 @@
   },
 
   create(context) {
+    // no-module-declaration: function bodies and parameters are outside module scope.
+    function inFunction(node) {
+      for (let parent = node.parent; parent; parent = parent.parent) {
+        if (parent.type === 'FunctionDeclaration' || parent.type === 'FunctionExpression' || parent.type === 'ArrowFunctionExpression') return true;
+      }
+      return false;
+    }
     return {
       TaggedTemplateExpression(node) {
         const istTagVar = isTtag(node) || isjTtag(node);
         if (!istTagVar) return;
-        const scope = context.getScope().type;
+        if (inFunction(node)) return;
+        const scope = context.sourceCode.getScope(node).type;
 
         if (scope === 'module') {
           context.report({
@@ -32,7 +40,8 @@
       CallExpression(node) {
         const isTtagFn = isGettextFn(node) || isNgettextFn(node);
         if (!isTtagFn) return;
-        const scope = context.getScope().type;
+        if (inFunction(node)) return;
+        const scope = context.sourceCode.getScope(node).type;
 
         if (scope === 'module') {
           context.report({
