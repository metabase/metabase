import { Canvas, Story, Meta } from "@storybook/addon-docs";
import { Icon } from "metabase/core/components/Icon";
import { Text } from "metabase/ui";
import { TextInput } from "./";

export const args = {
  disabled: false,
};

export const sampleArgs = {
  ...args,
  value: "Metabase",
  label: "Company or team name",
  description: "Name used for this instance",
  placeholder: "Department of awesome",
  error: "required",
};

export const argTypes = {
  label: {
    control: { type: "text" },
  },
  description: {
    control: { type: "text" },
  },
  placeholder: {
    control: { type: "text" },
  },
  disabled: {
    control: { type: "boolean" },
  },
  error: {
    control: { type: "text" },
  },
};

<Meta
  title="Inputs/TextInput"
  component={TextInput}
  args={args}
  argTypes={argTypes}
/>

# TextInput

Our themed wrapper around [Mantine TextInput](https://mantine.dev/core/text-input/).

## Examples

export const Default = args => <TextInput {...args} />;

<Canvas>
  <Story name="Default">{Default}</Story>
</Canvas>

### Empty

export const Empty = args => (
  <TextInput
    {...args}
    label={sampleArgs.label}
    placeholder={sampleArgs.placeholder}
  />
);

<Canvas>
  <Story name="Empty">{Empty}</Story>
</Canvas>

### Filled

export const Filled = args => (
  <TextInput
    {...args}
    value={sampleArgs.value}
    label={sampleArgs.label}
    placeholder={sampleArgs.placeholder}
  />
);

<Canvas>
  <Story name="Filled">{Filled}</Story>
</Canvas>

### Asterisk

export const Asterisk = args => (
  <TextInput
    {...args}
    label={sampleArgs.label}
    placeholder={sampleArgs.placeholder}
    withAsterisk
  />
);

<Canvas>
  <Story name="Asterisk">{Asterisk}</Story>
</Canvas>

### Disabled

export const Disabled = args => (
  <TextInput
    {...args}
    label={sampleArgs.label}
    placeholder={sampleArgs.placeholder}
    disabled
    withAsterisk
  />
);

<Canvas>
  <Story name="Disabled">{Disabled}</Story>
</Canvas>

### Error

export const Error = args => (
  <TextInput
    {...args}
    label={sampleArgs.label}
    placeholder={sampleArgs.placeholder}
    error={sampleArgs.error}
    withAsterisk
  />
);

<Canvas>
  <Story name="Error">{Error}</Story>
</Canvas>

### Icon

export const Icons = args => (
  <TextInput
    {...args}
    label={sampleArgs.label}
    placeholder={sampleArgs.placeholder}
    icon={<Icon name="dashboard" />}
    withAsterisk
  />
);

<Canvas>
  <Story name="Icon">{Icons}</Story>
</Canvas>

### Right section

export const RightSection = args => (
  <TextInput
    {...args}
    label={sampleArgs.label}
    placeholder={sampleArgs.placeholder}
    rightSection={<Text color="inherit">⌘+C</Text>}
  />
);

<Canvas>
  <Story name="Right section">{RightSection}</Story>
</Canvas>

### Read only

export const ReadOnly = args => (
  <TextInput
    {...args}
    label={sampleArgs.label}
    placeholder={sampleArgs.placeholder}
    rightSection={<Text color="inherit">⌘+C</Text>}
    readOnly
  />
);

<Canvas>
  <Story name="Raed only">{ReadOnly}</Story>
</Canvas>
