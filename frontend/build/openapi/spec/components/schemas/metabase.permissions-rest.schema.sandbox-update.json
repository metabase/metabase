{
  "description": "a sandbox to write alongside a permissions graph update",
  "properties": {
    "attribute_remappings": {
      "oneOf": [
        {
          "$ref": "./metabase.permissions-rest.schema.attribute-remappings.json"
        },
        {
          "type": "null"
        }
      ]
    },
    "card_id": {
      "oneOf": [
        {
          "description": "value must be an integer greater than zero.",
          "minimum": 1,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ]
    },
    "group_id": {
      "description": "value must be an integer greater than zero.",
      "minimum": 1,
      "type": "integer"
    },
    "id": {
      "description": "value must be an integer greater than zero.",
      "minimum": 1,
      "type": "integer"
    },
    "permission_id": {
      "oneOf": [
        {
          "description": "value must be an integer greater than zero.",
          "minimum": 1,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ]
    },
    "table_id": {
      "description": "value must be an integer greater than zero.",
      "minimum": 1,
      "type": "integer"
    }
  },
  "type": "object"
}
