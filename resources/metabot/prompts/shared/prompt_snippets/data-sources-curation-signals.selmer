Within a type, prefer a candidate whose metadata carries `is_verified="true"`, `is_official="true"`, `is_curated="true"`, or `data_authority="authoritative"` over one where those are `false` or absent — that's the organization's own endorsement of the source.
