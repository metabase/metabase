/**
 * Playwright port of
 * e2e/test/scenarios/dashboard-filters-matrix/page-3.cy.spec.ts
 *
 * Generated "matrix" page 3 — matrix.slice(3*20, 3*20+20) of the 216
 * parameter-filter combinations. Runner + full matrix data live in
 * support/dashboard-filters-matrix.ts (shared home for the page-0..page-10
 * family); this spec is a thin wrapper exactly like the Cypress original.
 */
import { runPage } from "../support/dashboard-filters-matrix";

runPage(3);
