{
  "description": "One form field describing how to request an index: the same shape as a [[connection-properties]] descriptor, plus a\n  `:columns` type for the indexed columns.",
  "properties": {
    "description": {},
    "directions": {
      "type": "boolean"
    },
    "display-name": {},
    "name": {
      "type": "string"
    },
    "options": {
      "items": {
        "properties": {
          "name": {},
          "value": {
            "type": "string"
          }
        },
        "required": [
          "name",
          "value"
        ],
        "type": "object"
      },
      "type": "array"
    },
    "required": {
      "type": "boolean"
    },
    "type": {
      "enum": [
        "string",
        "boolean",
        "select",
        "integer",
        "columns"
      ],
      "type": "string"
    }
  },
  "required": [
    "name",
    "display-name",
    "type"
  ],
  "type": "object"
}
