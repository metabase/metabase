import { Canvas, Story, Meta } from "@storybook/addon-docs";
import { Icon } from "metabase/core/components/Icon";
import { Text } from "metabase/ui";
import { TextInput } from "./";

export const args = {
  disabled: false,
};

export const argTypes = {
  label: {
    control: { type: "text" },
  },
  description: {
    control: { type: "text" },
  },
  placeholder: {
    control: { type: "text" },
  },
  disabled: {
    control: { type: "boolean" },
  },
  error: {
    control: { type: "text" },
  },
};

<Meta
  title="Inputs/TextInput"
  component={TextInput}
  args={args}
  argTypes={argTypes}
/>

# TextInput

Our themed wrapper around [Mantine TextInput](https://mantine.dev/core/text-input/).

## Examples

export const Default = args => <TextInput {...args} />;

<Canvas>
  <Story name="Default">{Default}</Story>
</Canvas>

### Icons

export const Icons = args => (
  <TextInput {...args} icon={<Icon name="dashboard" />} />
);

<Canvas>
  <Story name="Icon">{Icons}</Story>
</Canvas>

### Right section

export const RightSection = args => (
  <TextInput {...args} rightSection={<Text color="inherit">⌘+C</Text>} />
);

<Canvas>
  <Story name="Right section">{RightSection}</Story>
</Canvas>
