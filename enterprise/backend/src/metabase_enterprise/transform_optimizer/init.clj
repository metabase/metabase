(ns metabase-enterprise.transform-optimizer.init
  "Side-effecting requires for the transform-optimizer module. Loaded at
  system launch via `metabase-enterprise.core.init`."
  (:require
   [metabase-enterprise.transform-optimizer.events]))
