{
  "description": "Response shape from /v1/read-resource. `:resources` is the per-URI result list;\n  `:output` is the formatted XML string the LLM consumes.",
  "properties": {
    "output": {
      "type": "string"
    },
    "resources": {
      "items": {
        "$ref": "./metabase.agent-api.api.read-resource-item.json"
      },
      "type": "array"
    }
  },
  "required": [
    "resources",
    "output"
  ],
  "type": "object"
}
