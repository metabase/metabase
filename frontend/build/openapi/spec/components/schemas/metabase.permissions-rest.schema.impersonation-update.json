{
  "description": "a connection impersonation policy to write alongside a permissions graph update",
  "properties": {
    "attribute": {
      "minLength": 1,
      "type": "string"
    },
    "db_id": {
      "description": "value must be an integer greater than zero.",
      "minimum": 1,
      "type": "integer"
    },
    "group_id": {
      "description": "value must be an integer greater than zero.",
      "minimum": 1,
      "type": "integer"
    }
  },
  "required": [
    "group_id",
    "db_id",
    "attribute"
  ],
  "type": "object"
}
