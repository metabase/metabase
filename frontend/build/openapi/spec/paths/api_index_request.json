{
  "post": {
    "description": "Create an index request on a transform's target table. Requires write access to the transform. Listed via `GET /`,\n  not under `/request`.",
    "operationId": "post-api-index-request",
    "parameters": [],
    "requestBody": {
      "content": {
        "application/json": {
          "schema": {
            "properties": {
              "structured": {
                "$ref": "../components/schemas/metabase.indexes.schema.index-structured.json"
              },
              "transform_id": {
                "description": "value must be an integer greater than zero.",
                "minimum": 1,
                "type": "integer"
              }
            },
            "required": [
              "transform_id",
              "structured"
            ],
            "type": "object"
          }
        }
      }
    },
    "responses": {
      "2XX": {
        "content": {
          "application/json": {
            "schema": {
              "additionalProperties": false,
              "properties": {
                "created_at": {},
                "created_by": {
                  "oneOf": [
                    {
                      "description": "value must be an integer greater than zero.",
                      "minimum": 1,
                      "type": "integer"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "error_message": {
                  "oneOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "id": {
                  "description": "value must be an integer greater than zero.",
                  "minimum": 1,
                  "type": "integer"
                },
                "index_name": {
                  "minLength": 1,
                  "type": "string"
                },
                "last_executed_at": {
                  "oneOf": [
                    {},
                    {
                      "type": "null"
                    }
                  ]
                },
                "status": {
                  "enum": [
                    "create-pending",
                    "update-pending",
                    "delete-pending",
                    "running",
                    "succeeded",
                    "failed"
                  ],
                  "type": "string"
                },
                "structured": {
                  "$ref": "../components/schemas/metabase.indexes.schema.index-structured.json"
                },
                "transform_id": {
                  "description": "value must be an integer greater than zero.",
                  "minimum": 1,
                  "type": "integer"
                },
                "updated_at": {}
              },
              "required": [
                "id",
                "transform_id",
                "index_name",
                "structured",
                "status",
                "error_message",
                "created_by",
                "created_at",
                "updated_at",
                "last_executed_at"
              ],
              "type": "object"
            }
          }
        },
        "description": "Successful response"
      },
      "4XX": {
        "description": "Client error response"
      },
      "5XX": {
        "description": "Server error response"
      }
    },
    "summary": "POST /api/index/request",
    "tags": [
      "/api/index"
    ]
  }
}
