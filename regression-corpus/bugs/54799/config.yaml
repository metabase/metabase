issue: 54799
issue_url: https://github.com/metabase/metabase/issues/54799
fix_commits: ["95afcd14b1867de1cabba9e96946dbbea674894f"]
fix_subject: "Allow selecting multiple ranges and running"
area: qb/native-editor-selection
fe_be_boundary: false

mutation:
  kind: semantic
  layer: frontend
  patch_file: reconstruction.patch
  report_file: reconstruction-report.md
  base_sha: "56879decb0aac2ad61b5bc5737ebd4afc56e5f81"
  applies_cleanly: true
  fe_bucket: all_specs_gone
  oracle_source: "relocated_spec (selectors.unit.spec.js -> .ts)"
  reconstructed_by: worktree_subagent
  reconstruction_notes: >
    Reverted getNativeEditorSelectedRange to drop the '[0]' (pre-fix single-range shape). The fix migrated selection storage object->array; reverting the singular selector feeds the raw array to downstream .start/.end. CAVEAT: failure manifests as a TypeError in the selector (the bug's real behavior under test), not a clean value mismatch.

validation:
  oracle: {kind: fe_unit, test: "frontend/src/metabase/query_builder/selectors.unit.spec.ts", clean_baseline: pass, reconstructed_result: "7/7 active fail (selector can't handle array-shaped selection)"}
  repro_confirmed: true
  confidence: high

failure_shape:
  observed: true
  source: observed_unit
  symptom: >
    Native-editor selected-text/cursor selectors assume a single {start,end} range and break with the multi-range array shape.
  repro_failure: >
    getNativeEditorSelectedText: TypeError 'Cannot destructure property row of undefined' (array vs object).
  not_setup_crash: true

coverage:
  be_unit: {status: untested, killed_by: []}
  fe_unit: {status: kill, killed_by: ["frontend/src/metabase/query_builder/selectors.unit.spec.ts"], clean_baseline: pass}
  notes: "FE reconstruction from all_specs_gone bucket; oracle via relocated_spec (selectors.unit.spec.js -> .ts). Killed by FE unit (jest) => e2e repro cullable."
