{
  "description": "Redshift distribution, inline. Only `:key` uses a column (the one DISTKEY column); `:all`/`:even` take none.",
  "properties": {
    "columns": {
      "items": {
        "$ref": "./metabase.indexes.schema.column.json"
      },
      "maxItems": 1,
      "minItems": 1,
      "type": "array"
    },
    "kind": {
      "const": "distkey"
    },
    "style": {
      "enum": [
        "key",
        "all",
        "even"
      ],
      "type": "string"
    }
  },
  "required": [
    "kind",
    "style"
  ],
  "type": "object"
}
