import type { MetabaseComponentTheme } from "embedding-sdk";

/**
 * Default theme options for embedded components.
 */
export const DEFAULT_COMPONENT_THEME: MetabaseComponentTheme = {
  table: {
    cell: {
      background: "#ffffff",
    },
  },
};
