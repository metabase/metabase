import { Canvas, Story, Meta } from "@storybook/addon-docs";
import { Select } from "metabase/ui";

export const args = {
  size: "md",
  placeholder: "No semantic type",
};

export const dataWithGroups = [
  { value: 1, label: "Entity key", group: "Overall row" },
  { value: 2, label: "Foreign key", group: "Overall row", disabled: true },
  { value: 3, label: "Category", group: "Common" },
  { value: 4, label: "Description", group: "Common" },
];

export const dataWithIcons = dataWithGroups.map(item => ({
  ...item,
  group: undefined,
}));

export const dataWithLabels = dataWithIcons.map(item => ({
  ...item,
  icon: undefined,
}));

export const argTypes = {
  size: {
    options: ["xs", "md"],
    control: { type: "inline-radio" },
  },
  placeholder: {
    control: { type: "text" },
  },
};

<Meta
  title="Inputs/Select"
  component={Select}
  args={args}
  argTypes={argTypes}
/>

# Select

Our themed wrapper around [Mantine Select](https://mantine.dev/core/select/).

## Docs

- [Figma File](https://www.figma.com/file/21uCY0czmCfb6QBjRce0I8/Input-%2F-Select?type=design&node-id=1-96&mode=design&t=Mk8RP6HsneuWzHtr-0)
- [Mantine Select Docs](https://mantine.dev/core/select/)

## Examples

export const DefaultTemplate = args => (
  <Select {...args} data={dataWithLabels} />
);

export const Default = DefaultTemplate.bind({});

<Canvas>
  <Story name="Default">{Default}</Story>
</Canvas>
