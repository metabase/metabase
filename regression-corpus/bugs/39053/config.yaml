issue: 39053
issue_url: https://github.com/metabase/metabase/issues/39053
fix_commits: ["576e1511ab500a71fbda38380e5c582e331663a5"]
fix_pr: 38986
fix_subject: "Persist user-local info about the last selected database"
area: users/settings (was models/user)
fe_be_boundary: false

mutation:
  kind: semantic
  patch_file: reconstruction.patch
  base_sha: "56879decb0aac2ad61b5bc5737ebd4afc56e5f81"
  applies_cleanly: true      # RECONSTRUCTION patch applies; original fix diff does NOT (conflict)
  file_relocated: true
  reconstructed_by: worktree_subagent
  reconstruction_notes: >
    Stripped the :getter guard from last-used-native-database-id so it echoes any stored id (incl deleted DBs). NOTE: named fix commit only INTRODUCED the setting; the real behavioral fix (the deleted-DB guard) was a LATER commit #39269 -> mapper mis-attributed the fix commit; oracle-driven reconstruction found the real fix anyway.

validation:
  oracle:
    kind: be_unit
    test: "metabase.users.settings-test/last-used-native-database-id-can-be-read-and-set"
    clean_baseline: pass
    reconstructed_result: "1/3 fail (returns stale -999 instead of nil for a nonexistent db)"
  repro_confirmed: true
  confidence: high

failure_shape:
  observed: true
  source: observed_unit
  symptom: >
    last-used-native-database-id returns a stale/nonexistent database id instead of nil; the native editor tries to restore a database that no longer exists.
  repro_failure: >
    'returns nil if the database doesn't exist': expected nil, actual -999.
  not_setup_crash: true

coverage:
  be_unit: {status: kill, killed_by: ["metabase.users.settings-test/last-used-native-database-id-can-be-read-and-set"], clean_baseline: pass}
  fe_unit: {status: untested, killed_by: []}
  notes: "Semantic-reconstruction batch-2. Conflict target reconstructed into current code + killed by shipped BE test => e2e repro cullable."
