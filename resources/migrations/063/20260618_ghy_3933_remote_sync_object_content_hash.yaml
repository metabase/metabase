## Add content_hash column to remote_sync_object for content-based dirty detection (GHY-3933)

databaseChangeLog:

  - changeSet:
      id: v63.2026-06-18T00:00:00
      author: ericnormand
      comment: Add remote_sync_object.content_hash to record a hash of each entity's serialized form as of the last sync, so an update event that does not change the serialized content does not mark the entity dirty
      changes:
        - addColumn:
            tableName: remote_sync_object
            columns:
              - column:
                  name: content_hash
                  type: ${text.type}
                  remarks: SHA-256 (hex) of this entity's serialized YAML as of the last sync; an update whose recomputed hash matches stays synced. Null means no baseline yet (treated as dirty, exports once).
                  constraints:
                    nullable: true
      rollback:
        - dropColumn:
            tableName: remote_sync_object
            columnName: content_hash
