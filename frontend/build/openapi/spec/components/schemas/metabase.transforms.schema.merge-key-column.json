{
  "description": "One column of a merge unique key. Carries a resolved `:field-id` when the target column is known,\n  degrading to a `:name` ref when the target table doesn't exist yet (mirrors `::source-table-entry`).",
  "properties": {
    "field-id": {
      "oneOf": [
        {
          "$ref": "./metabase.lib.schema.id.field.json"
        },
        {
          "type": "null"
        }
      ]
    },
    "name": {
      "minLength": 1,
      "type": "string"
    }
  },
  "type": "object"
}
