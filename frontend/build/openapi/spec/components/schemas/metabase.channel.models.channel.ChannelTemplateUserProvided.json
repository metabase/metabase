{
  "description": "Channel Template schema for API-provided templates. Does not allow handlebars-resource.",
  "oneOf": [
    {
      "properties": {
        "channel_type": {},
        "details": {
          "$ref": "./metabase.channel.models.channel.ChannelTemplateEmailDetailsUserProvided.json"
        },
        "id": {
          "description": "value must be an integer greater than zero.",
          "minimum": 1,
          "type": "integer"
        },
        "name": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "channel_type",
        "details"
      ],
      "type": "object"
    },
    {
      "properties": {
        "channel_type": {},
        "id": {
          "description": "value must be an integer greater than zero.",
          "minimum": 1,
          "type": "integer"
        },
        "name": {
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "channel_type"
      ],
      "type": "object"
    }
  ]
}
