{
  "description": "Snowflake (standalone) / BigQuery (inline) clustering. `:name` is required for the standalone form.",
  "properties": {
    "columns": {
      "items": {
        "$ref": "./metabase.indexes.schema.column.json"
      },
      "minItems": 1,
      "type": "array"
    },
    "kind": {
      "const": "clustering"
    },
    "name": {
      "$ref": "./metabase.indexes.schema.index-name.json"
    }
  },
  "required": [
    "kind",
    "columns"
  ],
  "type": "object"
}
