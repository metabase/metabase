# Agent G: explainer, worklog

Session: metabase-sqlite-semantic-search (main worktree session)

## 2026-09-23 — Explainer level chosen by Voytek
- **What**: Drafted three terms (nDCG@10, Jaccard@10, CI) at three levels (A: one line; B: intuition + example from our data; C: formula + worked example).
- **Why**: Voytek wants every term explained, at the level that matches his understanding.
- **How**: Voytek picked **A (one line, just what it tells you)**, placed **in the dashboard**. Applying that level everywhere.
- **Open**: The brief says grades 1–3, but the contract and golden.json only use 1–2. I'm using 1–2.

## 2026-09-23 — Glossary + per-card term explainers
- **What**: New `results/src/glossary.ts`, the single source of ~50 one-line terms in 6 sections. `results/src/dashboard-cards.ts`: `card()` now requires `terms: Term[]` and appends each term's line to the card description (ⓘ tooltip). Added a "Glossary" text card on the About tab. Fixed "~40 scenarios" → 56.
- **Why**: Every term shown on a card gets its explainer where it's read. The glossary card and the tooltips come from one source, so they can't drift, and a new card can't be added without declaring its terms (type error).
- **How**: Checked facts against the code and data: 56 scenarios / 8 categories (golden.json), cosine *distance* cutoff 0.7 (`semantic_search/index.clj:716`), `semantic-pure` = top-up off (shared/types.ts). `tsc --noEmit` passes clean; all 24 cards have descriptions with their terms.
- **Open**: Pushing to :3002 is waiting on F's go-ahead. The Quality summary description still says "overlapping intervals: do not call a winner", which is the unpaired-CI problem in brief item 1. I haven't fixed it here (it's not an explainer issue).

## 2026-09-23 — F's glossary fixes, BL-01 follow-up, pushed to :3002
- **What**: Applied F's three corrections in `results/src/glossary.ts`: appdb on H2 ranks on recency/popularity (it does *not* score a flat 1); iteration excludes warm-up repeats; log scale is for comparing growth rates. Fixed the matching appdb claim in the About text. "Runs in view" (`dashboard-cards.ts`) now reads `harness_latest_engine_run` (BL-01 follow-up; marked in BACKLOG.md), so a separate semantic-pure run shows as its own row, with an Engines column.
- **Why**: F verified appdb scores range 6.8–108 on the latest golden run. BL-01 left "Runs in view" on the old one-run-per-slice view.
- **How**: `tsc --noEmit` clean; `npm run dashboard`; `npm run check` → `0 error(s), 2 empty card(s)` (both embedding-text Δ cards: no variant runs yet). `npm run serdes -- export` → `hackathon/harness/dashboard/serdes`.

## 2026-09-23 — Glossary moved to its own tab; per-tab "Terms on this tab" cards
- **What**: The glossary text card overflowed on About (it scrolled inside the card), so it now has its own **Glossary** tab (2nd). Browser check: Metabase truncates the ⓘ tooltip after ~2 paragraphs, so the per-card term lines were mostly invisible on hover. Every tab with cards now ends with an auto-generated "Terms on this tab" text card (the union of its cards' `terms`, via `termsMarkdown` in glossary.ts). Card height = ceil(0.6 × terms) + 3 rows. Tooltips keep the full text (visible in full when you open the card).
- **Why**: Voytek asked for every term to be explained *in the dashboard*; a truncated tooltip doesn't do that.
- **How**: Chrome on :3002: JS check over all 9 tabs found 0 overflowing text cards (at +2 rows, 4 tabs overflowed by ~1 line, hence +3). Screenshot-checked the Glossary and Quality tabs. check: 0 errors.
- **Open**: The browser had a remembered Scale=100 filter, which empties every golden card (golden runs have no scale tier). That's per-user Metabase state, not a dashboard default, but a first-time viewer who sets Scale will hit it. Worth a note in the reading guide. The Quality summary still carries the unpaired-CI "do not call a winner" line (BL-06). "This scenario, per engine" and the drill-down "Scenario" card still read `harness_latest_run`, so a separate-run engine (semantic-pure) is missing from their latency / answer-key lookups. Reported to F.

## 2026-09-23 — BL-06: paired quality comparison
- **What**: `results/src/dashboard-cards.ts`, Quality tab. New card "Is semantic really better? Paired, question by question": for each other engine, it pairs nDCG@10 per question (same scenario, embedder, scale, embedding text) against `REFERENCE_ENGINE = "semantic"`. It shows wins/ties/losses (tie = |Δ| ≤ 1e-9), avg Δ ± 95% CI, and a verdict (better/worse only when the whole range is on one side of 0). "Quality summary" lost its unpaired CI column and the "do not call a winner" text; it now groups by scale as well, so tiers don't pool. Glossary: added "Wins / ties / losses"; the Top-up line now points at semantic-pure.
- **Why**: BL-06 / brief item 1. Unpaired intervals overlapped and hid a real difference.
- **How**: Tested the SQL in psql first, then fetched the card through the Metabase API with dashboard defaults: vs in-place 26/12/14, +0.110 ± 0.090; vs appdb 33/17/2, +0.292 ± 0.089; vs semantic-pure 1/49/2, +0.003 ± 0.008 (no proven difference). Targets were +0.111 / +0.291, from an earlier run; this is the latest baseline 20260923-154430-7f2163. `npm run check`: 0 errors, 0 empty. Serdes re-exported.

## 2026-09-23 — BL-26: drill-down cards per engine run
- **What**: `results/src/dashboard-cards.ts`. "This scenario, per engine": the latency CTE now keeps (run_id, engine) pairs from `harness_latest_engine_run`. "Scenario": reads every latest engine run; the answer key is shown by `ref` (e.g. `card/customer-churn-rate`) and de-duplicated; ID and Name columns removed.
- **Why**: semantic-pure ran on its own instance, so its item ids differ from the main run's (concept-01: card 169 vs 173). Reading all runs by id would have listed every answer twice. The old `names` lookup joined on (model, id) across runs regardless of instance, so it could show the wrong item's name.
- **How**: Checked in psql that the ids differ per run, and that every real run carries `ref` (fixture runs don't; fallback is model/id, fine on their single instance). Fetched both cards via the API with defaults: 5 answers listed once; semantic-pure row with p50 32.2 ms. `npm run check`: 0 errors, 0 empty. Serdes re-exported.

## 2026-09-23 — BL-15 dashboard half: "Unscored" column
- **What**: Quality summary gets an "Unscored" column: the mean of per-scenario `unscored_rate` (H's metric, BL-15). No coalesce, so runs recorded before BL-15 show blank (unknown), not 0. Glossary: new "Unscored" term, listed on the Quality tab's terms card.
- **Why**: Scenarios where every iteration errored silently drop out of an engine's averages. This makes the drop visible.
- **How**: Metric not yet in harness_metric (0 rows), so the column is blank everywhere today, as intended. `npm run check`: 0 errors, 0 empty. Serdes re-exported. BL-26 marked verified by F.
- **Note**: The arctic golden run (A) has landed: Quality summary shows semantic arctic nDCG 0.676 vs all-minilm 0.601. The keyword engines are identical across embedders (as expected).

## 2026-09-23 — The dashboard is the reading guide (Voytek's call)
- **What**: Voytek, asked where the guide should live: "cant it be in the dahsbaord? If the info is already there we dont need more. I want the dahsbaord to be as self-explanatory as possible". So no separate artifact or doc. Changes in `results/src/dashboard-cards.ts` (+ `dashboard.ts`, `glossary.ts`):
  - Every card declares `question` + `read` (required by type). Both lead its description, and each tab opens with an auto-built "How to read this tab" card listing them.
  - `hidden` (with a reason) hides degenerate/duplicate cards without deleting code; the tab's guide lists them. Hidden: Where the time goes (always 100% Other over HTTP), Recall box plot (duplicate), Spread within each category (duplicate), variance share + swap embedder/engine (need ≥2 vector engines × ≥2 embedders), and the old variance explainer text. Kept the latency-vs-size and embedding-text cards (A/I runs will fill them).
  - About → **Start here**: what this is (5 sentences), how to use the tabs, filters + the "clear Scale" tip, **the verdict** (6 points, run ids stamped in `VERDICT_RUNS`), the live paired card, caveats, what would change the verdict (and which card shows it), then Runs in view + Permission leaks.
  - New card **"Does the embedding model matter? Paired, by question type"** (Model vs engine tab; the old "Store vs model" tab is renamed). Each other embedder vs all-minilm per vector engine, overall + 8 categories, with verdict ("too few questions" below n=5).
  - BL-17: category heatmaps restricted to the 8 categories in contract order; nDCG heatmap has "Expected winner" (01-contracts §3). 25 → 7 rows.
  - BL-07: H's version B (pair within scale × embedder), plus H's follow-up: lone baseline rows dropped.
  - Verdict columns moved next to the comparison name, since they were off-screen. Several table sizes bumped. `dashboard.ts`: newly created cards are remembered, so one card can sit on two tabs (the paired card is on Start here and Quality).
- **Why**: Voytek wants the dashboard self-explanatory. F's rule: numbers live in query cards; any number quoted in text is stamped with its run.
- **How**: Re-derived every verdict number in psql, then read the cards through the API. semantic vs in-place 26/12/14, +0.110 ± 0.090; vs appdb 33/17/2, +0.292 ± 0.089; vs pure 1/49/2, +0.003 ± 0.008; arctic vs minilm 20/15/17, +0.076 ± 0.078 (not proven); cross-lingual +0.509 ± 0.218 (proven); typo +0.193 ± 0.431 (NOT proven, n=5; F's suggested sentence said typos gained, so the verdict was corrected); context −0.028 ± 0.051; arctic false-positive 3/4 vs 1/4; arctic p50 46.5 vs 34.0 ms. F's "model matters more than anything else" is also wrong, because engine gaps are larger (+0.29): the verdict says "of the things we can change inside semantic". `npm run check`: 0 errors, 0 empty. Browser: all 21 text cards fit (JS overflow check), and Start here / Quality / Model vs engine were checked by screenshot. Serdes re-exported. BACKLOG: BL-17 done (awaiting F); BL-16/24/27 worked around.
- **Open**: The done-test (fresh agent, dashboard snapshot only) is running.

## 2026-09-23 — Done-test and fixes from it
- **What**: Fresh general-purpose agent, given only a text snapshot of the dashboard (every tab, text card, card title/tooltip, and card data at default filters; `scratchpad/dashboard-snapshot.md`), answered the brief's 5 questions. **Passed**: all 5 answered correctly with numbers and card sources (best engine + margins + certainty, per-category failures, what semantic includes + semantic-pure, latency table and "doesn't matter at 235 items", the unknowns list).
- **Fixed from its confusion list**: glossary (semantic's pgvector is a separate Postgres; appdb differs slightly between runs; in-place orders its own way but reports no score; top-up threshold is 100 by default and never fires when semantic finds nothing, verified in `semantic_search/core.clj:103-136`; Category lists all 8 and explains 53 vs 52 (exact-07 has two tags); nDCG: grade 2 counts twice grade 1, verified in `metrics.ts:101`; CI formula stated; Unscored blank wording; baseline embeds the item type too). Each tab-guide entry now says which runs the card shows ("the model picked in the Embedder filter" vs "every embedding model"; every size tier when Scale doesn't apply). Verdict #1 no longer says "it isn't luck" (in-place still wins 14/52). The appdb-on-Postgres claim is now "might; not measured". New caveat: permission leaks rest on 1 question, no-answer on 4. Latency lines label the golden point "golden (no tier)". Runs in view: dropped the raw JSON Notes. Embedding-text intro rewritten without internal jargon ("Axis 4", "Agent E", "bottom right"). Drill-down Scenario card: adds "Name as shown in results" (same-run id match) and the author's "Why this question is tricky" note.
- **How**: `npm run check`: 0 errors, 0 empty after every push. Drill-down verified through the API (card/customer-churn-rate = "Trail Club churn rate"). Serdes re-exported.
- **Open**: **Browser extension disconnected**, so the final overflow check wasn't re-run after the guide cards grew. The height formula was raised to 1.25 rows per entry (it was 0.9, which fit before the "Shows:" line was added). Needs one visual pass. Not fixed: semantic vs semantic-pure +0.003 with 1 win / 2 losses looks odd to a reader but is arithmetic (one large win).

## 2026-09-23 — BL-30: latency vs size across tier corpora; cross-instance caveat; self-sizing text cards
- **What**: `dashboard-cards.ts`. The p50/p95 latency-vs-size lines and the latency table read `SIZE_SCOPE` = the selected corpus plus every `scale-%` corpus (each tier is its own corpus), plotted as "235 (golden)", "1k", "10k" in size order. Verdict #6 is rewritten around the 10k result (runs 155756-94d8c0 and 160501-03ff76 added to `VERDICT_RUNS`). Caveats and "what would change" updated (tiers are generated and timing-only; semantic-pure/arctic at scale would split top-up vs model). `LATENCY_CAVEAT` (F): compare latency only within a run; across runs, <2× may be the machine. It's on every latency card. Verdict #4 no longer says arctic is slower. The glossary Scale line is updated. Text cards now size themselves from their text (`fitRows`), after 9 overflowed as lines grew.
- **Why**: A/F: the size curve could never show more than one point, which hid the day's biggest finding (at 10k, semantic p50/p95 454/1,422 ms vs appdb 94/275 and in-place 207/549).
- **How**: psql re-derivation matches A's numbers; the card rows via the API match. `npm run check`: 0 errors, 0 empty. Browser: 21/21 text cards fit (JS overflow check). Serdes re-exported. BACKLOG: BL-17 verified, BL-30 done (awaiting F). dashboard-cards.ts handed to H for BL-24.
- **Open**: The Q4–5 re-test with a fresh agent is running.

## 2026-09-23 — Q4–5 re-test fixes; G done (build)
- **What**: On top of H's BL-24/27 push (files handed over and back explicitly). (1) Runs in view includes the `scale-%` corpora (7 runs). (2)(3)(6) Verdict #6 rewritten in ms: at golden appdb is ~2.5× faster than semantic within one run, but it doesn't matter (<40 ms); at 10k semantic is slowest (454/1,422 vs 94/275 and 207/549); "9× jump from 1k to 10k" replaces the ambiguous "10× gap". (4) Removed the wrong "one decade per tier = linear" hint. (5) Latency lines: trust >2× jumps along a line, and a single point means "only run on golden". Glossary Iteration: 5 timed repeats, n = questions × 5 (verified: max(iteration)+1 = 5). Start-here filter note rewritten, because H's BL-27 fix made "clear Scale" obsolete.
- **How**: check 0 errors / 0 empty; browser 21/21 text cards fit, with a remembered Scale=100 applied, and golden still renders (H's fix works). Serdes re-exported.
- **Status**: **G done (build)**, per F. Reopen triggers: I's text-strategy runs (A added `text_strategy` to all views; the cards need a Text strategy filter, text_strategy in the embedding-text Δ pairing, and probably a "Δ vs strategy none" card), and J's first sqlite-vec1/lucene runs (engine columns are already generated from ENGINES; update the verdict and un-hide the Model vs engine cards).

## 2026-09-23 — BL-31: text_strategy filter and pairing
- **What**: `dashboard-cards.ts`: new required filter "Text strategy" (`strategy` tag, column text_strategy, default `none`), added to all 22 run-reading cards (not Runs in view, which shows it as a column instead). text_strategy is in every self-join (paired engines, paired embedders, BL-07 Δ, Δ by category, sanity). New tab "Text strategy" with the card "Change vs text strategy 'none'": each strategy vs `none` per engine, pairing on engine, scenario, embedding_text, scale and embedder. It ignores the strategy filter; `none` rows show as "reference". `dashboard.ts`: stable tag id …006. Glossary: "Text strategy". Start here: filter note + tab list.
- **Why**: F (BL-31). The views key on text_strategy, but no card did, so I's first strategy run would have pooled with baseline (the BL-07 bug class).
- **How**: A rollback smoke test instead of a scratch DB (Metabase reads `harness`): a script rendered every card's SQL with the dashboard defaults, then in ONE transaction ran all cards, inserted a copy of run 7f2163 as `smoke-test-rollback` with text_strategy `smoke-test` (nDCG ×0.9), ran all cards again, and ROLLBACK. Result: 20/22 cards identical; Runs in view gains the smoke row; the strategy card shows smoke-test rows separately (semantic 0.541, Δ −0.060 ± 0.010, 0/8/44). After the test, `harness_run` has 0 rows with text_strategy ≠ none. Then pushed: check 0 errors / 0 empty; browser 23/23 text cards fit, and the filter shows "Text strategy: none". Serdes re-exported.

## 2026-09-23 — BL-31 verified by F; back to done (build)
- **Open**: when I's first real strategy run lands, check the Text strategy tab and the default cards behave as in the rollback test, and report to F.

## 2026-09-23 — northwind-sql-v1 runs (A): empty-card wording
- **What**: The SQL corpus (runs 163103-6ee999 minilm, 163607-383d40 arctic; strategy none) leaves 5 cards empty for lack of data (no expectedAbsent, no empty-expected, no variant run). The drill-down read now explains that the Scenario list spans corpora; the FP card and the embedding-text intro say what empty means. check: default 0/0; sql corpus + sql-sqlc-01: 0 errors, 5 empty (all data-absent, each explained). Not the strategy-run check F asked for; that waits for I.
- **Follow-up (F via A)**: Permission leaks and false positives now return one explanatory row ("this corpus has no leak-check questions" / "…no no-answer questions") instead of nothing. Golden unchanged: check 0/0. SQL corpus: 0 errors, 3 empty (embedding-text, no variant run; explained in the tab intro). A per-corpus default scenario is not possible: Metabase filter defaults are one fixed value and card-sourced dropdowns cannot be linked to another filter.

## 2026-09-23 — BL-32: semantic is hybrid (wording)
- **What**: glossary.ts (semantic, semantic-pure, sqlite-vec1/lucene, Vector vs keyword, Top-up, new Hybrid) and dashboard-cards.ts (verdict #3, caveat, what-would-change, Hybrid on the paired card terms). No SQL change.
- **Why**: F: pgvector semantic merges vector + keyword results with RRF and has no toggle (verified: `semantic_search/index.clj` hybrid-search-query / scored-search-query). "semantic-pure" was misleading.
- **How**: grep for "pure" in both files, all fixed or accurate. check 0/0; serdes re-exported. Text cards size themselves, so no overflow re-check (the browser was not re-opened).
- BL-32 verified by F; back to done (build). Open: check the Text strategy tab when I's first strategy run lands.

## 2026-09-23 — G DONE (build), confirmed by F
- F: "G is done". Nothing outstanding. F takes the Text strategy tab check when I's first strategy run lands (8 queued). Future card work: F reopens G or hands it to H. results/src/dashboard-cards.ts, dashboard.ts and glossary.ts are free.

## 2026-09-23 — BL-36: real corpora in the text (reopened by F)
- **What**: glossary.ts Corpus = one line per real corpus (golden 235 items / 56 labelled; sql 60 labelled, 36 dev + 24 held-out, pooled on the dashboard, per research/embedding-text-recommendation.md:33; scale-* timing-only; runner-smoke = pipeline smoke tests); Scenario line adds sql 60. dashboard-cards.ts: Start-here filter note, the hidden "Where the time goes" description, and the FILTERS comment no longer mention the deleted fixture corpus. The Corpus filter values come live from harness_run (no fixture rows), so nothing to remove there.
- **How**: check 0/0; serdes re-exported. One stale "fixture" string remains in the export for the hidden "Where the time goes" card (hidden cards are not pushed, so Metabase keeps its old description; not visible on the dashboard).
- **Open**: deleting the 2 runner-smoke runs (0 metric, 180 query_result, 12 scenario, 2 run rows) waits on Voytek's direct confirmation. F approved, but it is an irreversible delete of shared data.
- **Delete done** (Voytek confirmed directly): one transaction; deleted harness_metric 0, harness_query_result 180, harness_scenario 12, harness_run 2. Remaining corpora: golden 8 runs, sql 13, scale-1000 1, scale-10000 1. The runner-smoke glossary mention was removed; check 0/0; serdes re-exported.

## 2026-09-23 — BL-37 latency half: overlap view
- **What**: sql/02-views.sql (A's file, A OK'd, additive): `harness_run_concurrency(run_id, overlapped)`, with coalesce(finished_at, started_at) so a crashed run flags nothing after it. dashboard-cards.ts: SIZE_SCOPE excludes overlapped runs (latency lines and table); LATENCY_CAVEAT mentions it; Runs in view "Overlapped" column.
- **How**: 01 + 02 applied twice via psql (ok ×2). `npm run schema` fails on someone else's new `sql/10-candidate-overlap.sql` (psql :vars; the script applies every .sql in the folder); not touched, reported to F. 0/24 runs overlapped, so latency rows are unchanged (10/10/13). Rollback test: forced 1k/10k overlap, both flagged, 0 observations left, 0 after rollback. check 0/0; serdes.
- **Open**: switch to job windows once A loads harness_job (queue events include boot and indexing, the real Ollama noise).
- BL-37 G half verified by F. `npm run schema` passes again (J removed sql/10-candidate-overlap.sql). Follow-up approved: key on harness_job windows once A loads them.

## 2026-09-23 — Final glossary lines; G DONE (confirmed by F)
- **What**: glossary.ts: sqlite-vec1 and lucene split (lucene = "Paolo's engine, not yet available"); all-minilm (384 numbers, reads up to ~512 tokens, per Ollama model_info) and snowflake-arctic-embed2 (1,024 numbers, up to 8,192 tokens, multilingual); one line each for none / mech-fill-empty / mech-fill-all / llm-fill-empty / llm-fill-all (per research/embedding-text-recommendation.md:17-23); Text strategy notes that added text is keyword-visible. The model terms are on the Model vs engine tab, the strategy terms on the Text strategy tab.
- **How**: check 0/0; serdes re-exported.
- **Status**: DONE, per F. Handed off: BL-38 (overlap on harness_job windows), BL-39 (SQL-corpus verdict, waits for the vector-only runs, BL-35). H, or G if reopened.

## 2026-09-23 — BL-38: overlap on job windows (reopened by F)
- **What**: sql/02-views.sql, harness_run_concurrency body only (same columns): overlapped = another run's timed window OR another harness_job whole window (coalesce(finished_at, now())) intersects this run's timed window, strict <; own job via run_ids.
- **How**: 01+02 applied twice (ok ×2). Live: the two sqlite retry runs (concurrent quality jobs) are flagged; older runs unchanged. Rollback tests: (1) sqlite 1k latency job finished with run_ids → f; (2) other job ending at exactly run start → f; (3) +1 ms → t; (4) pre-queue golden run + inserted job → t; 0 test rows after rollback. check 0/0; serdes.
- **Open**: queue.ts writes run_ids only at job end, so a finished run is flagged by its own still-running job until then (self-healing). Suggested to A: record the job id on the run (e.g. harness_run.job_id), or write run_ids when the run starts.
- BL-38 verified by F (the 4350df transient had cleared). G done again. The run_ids-at-end follow-up is A's.

## 2026-09-23 — BL-38 follow-up: own job via harness_run.job_id (F: do it)
- **What**: harness_run_concurrency: w CTE carries job_id; own-job test = `j.job_id IS DISTINCT FROM a.job_id AND NOT (a.run_id = ANY (j.run_ids))` (run_ids fallback for earlier queue runs). Comment updated.
- **How**: 01+02 applied twice ok. Live: the sqlite 1k/10k latency runs are not overlapped; the two BL-33 runs (9b2660 job 3803, 7f6f47 job 53c4) are overlapped, correctly, since the two quality jobs run concurrently. Isolated rollback test in a quiet window (17:19-17:20): own job running with empty run_ids + job_id set → f; the same with job_id NULL (old behaviour) → t. 0 leftovers. A first attempt was contaminated by live BL-33 runs landing in the test window (run-vs-run check), so the test was redone in the quiet window. check 0/0; serdes. G closed again.
