import React from "react";
import { Database } from "../../types";

interface Props {
  databases: Database[];
  onCollapse: () => void;
}

const DatabaseStatusLarge = (props: Props) => {
  return <div />;
};

export default DatabaseStatusLarge;
