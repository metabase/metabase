issue: 55678
issue_url: https://github.com/metabase/metabase/issues/55678
fix_commits: ["4de6e9f7e2340b0e8e6a0c620d09e34a5a6e6664"]
fix_subject: "Ignore parameters targets with stage-number"
area: parameters/mapping-options
fe_be_boundary: false

mutation:
  kind: semantic
  layer: frontend
  patch_file: reconstruction.patch
  report_file: reconstruction-report.md
  base_sha: "56879decb0aac2ad61b5bc5737ebd4afc56e5f81"
  applies_cleanly: true
  fe_bucket: partial
  oracle_source: "relocated_spec (.js -> .tsx)"
  reconstructed_by: worktree_subagent
  reconstruction_notes: >
    Reverted getMappingOptionByTarget's stage-aware lookup (reads stage-number, guards invalid stage, filters to that stage) back to the all-stages loop ignoring stage-number. Oracle migrated .js->.tsx; the other candidate spec (query.unit.spec.ts) had drifted non-discriminating.

validation:
  oracle: {kind: fe_unit, test: "frontend/src/metabase/parameters/utils/mapping-options.unit.spec.tsx", clean_baseline: pass, reconstructed_result: "2/30 fail (cross-stage column confusion; invalid stage not ignored)"}
  repro_confirmed: true
  confidence: high

failure_shape:
  observed: true
  source: observed_unit
  symptom: >
    Parameter mapping confuses columns from different stages (and doesn't ignore targets with an out-of-range stage-number), mapping to the wrong column.
  repro_failure: >
    'not confuse columns from different stages': expected mappingOptions[1], got [0]; 'ignore invalid stage index': expected undefined.
  not_setup_crash: true

coverage:
  be_unit: {status: untested, killed_by: []}
  fe_unit: {status: kill, killed_by: ["frontend/src/metabase/parameters/utils/mapping-options.unit.spec.tsx"], clean_baseline: pass}
  notes: "FE reconstruction from partial bucket; oracle via relocated_spec (.js -> .tsx). Killed by FE unit (jest) => e2e repro cullable."
