{
  "description": "Schema for a Metric in list responses (without hydrated dimensions).",
  "properties": {
    "collection": {
      "oneOf": [
        {
          "properties": {
            "id": {
              "oneOf": [
                {
                  "description": "value must be an integer greater than zero.",
                  "minimum": 1,
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ]
            },
            "name": {
              "type": "string"
            }
          },
          "required": [
            "id",
            "name"
          ],
          "type": "object"
        },
        {
          "type": "null"
        }
      ]
    },
    "collection_id": {
      "oneOf": [
        {
          "description": "value must be an integer greater than zero.",
          "minimum": 1,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ]
    },
    "database_id": {
      "oneOf": [
        {
          "description": "value must be an integer greater than zero.",
          "minimum": 1,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ]
    },
    "dataset_query": {
      "oneOf": [
        {
          "description": "Value must be a map.",
          "properties": {},
          "type": "object"
        },
        {
          "type": "null"
        }
      ]
    },
    "description": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "dimension_mappings": {
      "items": {
        "$ref": "./metabase.metrics.dimension.dimension-mapping.json"
      },
      "type": "array"
    },
    "dimensions": {
      "items": {
        "$ref": "./metabase.metrics.dimension.dimension.json"
      },
      "type": "array"
    },
    "id": {
      "description": "value must be an integer greater than zero.",
      "minimum": 1,
      "type": "integer"
    },
    "name": {
      "type": "string"
    },
    "result_column_name": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "required": [
    "id",
    "name",
    "description",
    "collection_id",
    "dimensions",
    "dimension_mappings"
  ],
  "type": "object"
}
