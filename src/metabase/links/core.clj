(ns metabase.links.core
  "Public API for the links module."
  (:require
   [metabase.links.models.link]))

(comment metabase.links.models.link/keep-me)

;; Import any functions that need to be exposed to other modules here using potemkin when needed
;; For now, this is minimal as we're just establishing the module structure
