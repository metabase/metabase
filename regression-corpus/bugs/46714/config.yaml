issue: 46714
status: reconstruction_failed
failure_category: dead_target
mutation:
  kind: semantic
  layer: frontend
  fe_bucket: partial_or_all_specs_gone
  reconstruction_notes: |
    FilterPopover/OperatorSelector — dead target. code path + spec DELETED by MBQL migration #50311; nothing to revert; real oracle Cypress
validation:
  oracle:
    kind: none
    test: styled.tsx (Cypress)
    reconstructed_result: dead_target__code_and_spec_deleted
coverage:
  fe_unit: untestable
