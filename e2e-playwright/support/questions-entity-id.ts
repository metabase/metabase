// ORDERS_QUESTION_ENTITY_ID is now canonical in ./sample-data; re-exported so
// this module's consumers keep their import unchanged.
export { ORDERS_QUESTION_ENTITY_ID } from "./sample-data";

// Port of H.main() — now canonical in ./ui; re-exported so this module's
// consumers keep their import unchanged.
export { main } from "./ui";
