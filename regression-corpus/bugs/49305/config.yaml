issue: 49305
issue_url: https://github.com/metabase/metabase/issues/49305
fix_commits: ["c01d67df0ed9acd664b1c3dfde004c1515201aa1"]
fix_subject: "Allow boolean functions in custom columns (#49123)"
area: lib/schema/filter (named file vestigial)
fe_be_boundary: false

mutation:
  kind: semantic
  patch_file: reconstruction.patch
  report_file: reconstruction-report.md
  base_sha: "56879decb0aac2ad61b5bc5737ebd4afc56e5f81"
  applies_cleanly: true
  file_relocated: true
  reconstructed_by: worktree_subagent
  oracle_source: modified_deftest    # fix ADDED assertions to an existing deftest
  reconstruction_notes: >
    NAMED product file legacy_mbql/schema.cljc was VESTIGIAL (reverting it had zero effect - proven empirically). Behavior migrated to lib/schema/filter.cljc via the annotate->Lib refactor; flipped the 12 fix-added boolean filter clauses :- :type/Boolean -> :- :type/*. Oracle also relocated (col-info-expressions-test -> result-metadata-test/col-info-expressions-test-1b).

validation:
  oracle: {kind: be_unit, test: "metabase.lib.metadata.result-metadata-test/col-info-expressions-test-1b", clean_baseline: pass, reconstructed_result: "12/21 fail (the 12 fix-added boolean clauses; 9 pre-fix pass)"}
  repro_confirmed: true
  confidence: high

failure_shape:
  observed: true
  source: observed_unit
  symptom: >
    Boolean functions used as custom columns aren't recognized as boolean (:base-type falls through to :type/*).
  repro_failure: >
    col-info-expressions-test-1b: expected {:base-type :type/Boolean}, actual {:base-type :type/*}.
  not_setup_crash: true

coverage:
  be_unit: {status: kill, killed_by: ["metabase.lib.metadata.result-metadata-test/col-info-expressions-test-1b"], clean_baseline: pass}
  fe_unit: {status: untested, killed_by: []}
  notes: "Semantic-reconstruction batch-5 (no_added_deftest bucket; modified-deftest oracle). Killed by shipped BE test => e2e repro cullable."
