name: CLJS

on:
  push:
    branches:
      - 'master'
      - 'release-**'
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref && github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  files-changed:
    name: Check which files changed
    runs-on: ${{ vars.SLIM_RUNNER_KEY }}
    timeout-minutes: 3
    outputs:
      changed: ${{ steps.changes.outputs.changed }}
    steps:
      - uses: actions/checkout@v6
        with:
          # Everything files-changed needs — the action and the filter config —
          # lives under .github, and paths-filter diffs refs, not the worktree.
          sparse-checkout: .github
      - name: Test which files changed
        id: changes
        uses: ./.github/actions/files-changed
        with:
          any-of: cljs

  shared-tests-cljs:
    needs: files-changed
    if: needs.files-changed.outputs.changed == 'true'
    runs-on: ${{ vars.DEFAULT_RUNNER_KEY }}
    timeout-minutes: 15
    steps:
    - uses: actions/checkout@v6
    - name: Prepare front-end environment
      uses: ./.github/actions/prepare-frontend
    - name: Prepare back-end environment
      uses: ./.github/actions/prepare-backend
    - name: Run Cljs tests
      run: bun run test-cljs
