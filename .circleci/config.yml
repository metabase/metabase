version: 2.1

workflows:
  build_test:
    jobs:
      - build_test

jobs:
  build_test:
    docker:
      - image: cimg/base:stable

    steps:
      - checkout
      - run:
          name: Run Tests
          command: echo "Hello, World!" && false
