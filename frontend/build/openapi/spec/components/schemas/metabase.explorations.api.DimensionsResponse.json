{
  "description": "Schema for GET /dimensions: metrics referencing dimensions by id, plus the grouped dimension list.",
  "properties": {
    "dimension_groups": {
      "items": {
        "$ref": "./metabase.explorations.api.ExplorationDimensionGroup.json"
      },
      "type": "array"
    },
    "metrics": {
      "items": {
        "$ref": "./metabase.explorations.api.ExplorationMetric.json"
      },
      "type": "array"
    }
  },
  "required": [
    "metrics",
    "dimension_groups"
  ],
  "type": "object"
}
