{
  "allOf": [
    {
      "$ref": "./metabase.metrics.dimension.dimension-mapping.json"
    }
  ],
  "description": "A dimension mapping in its internal kebab-case shape, annotated with the rules that convert it to\n   and from the snake_case wire shape. See [[metabase.metrics.dimension/dimension-mapping]]."
}
