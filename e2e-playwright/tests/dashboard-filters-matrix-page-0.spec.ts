/**
 * Playwright port of
 * e2e/test/scenarios/dashboard-filters-matrix/page-0.cy.spec.ts
 *
 * A generated "matrix" page: page 0 is `matrix.slice(0, 20)` — the first 20 of
 * the 216 parameter-filter combinations (all single/search cases). The runner
 * machinery and the full matrix data live in support/dashboard-filters-matrix.ts
 * (the shared home for the whole page-0..page-10 family), so this spec is a
 * thin wrapper exactly like the Cypress original.
 */
import { runPage } from "../support/dashboard-filters-matrix";

runPage(0);
