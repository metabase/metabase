issue: 52236
issue_url: https://github.com/metabase/metabase/issues/52236
fix_commits: ["3e046e5735e4304d76d90f504ce4b8a35f9d3496"]
fix_subject: "Disable pivot drill thru for multi-stage queries"
area: lib/drill-thru/pivot
fe_be_boundary: false

mutation:
  kind: semantic
  patch_file: reconstruction.patch
  report_file: reconstruction-report.md
  base_sha: "56879decb0aac2ad61b5bc5737ebd4afc56e5f81"
  applies_cleanly: true
  file_relocated: false
  reconstructed_by: worktree_subagent
  oracle_source: modified_deftest    # fix ADDED assertions to an existing deftest
  reconstruction_notes: >
    Re-added the one-line guard (not (lib.underlying/strictly-underlying-aggregation? query column)) the fix introduced, blocking the pivot drill on multi-stage aggregation queries. (The fix was a stopgap that commented OUT the multi-stage test variants; a later fix re-enabled them, so the fix-added-assertion framing is inverted but the guard is the right revert.)

validation:
  oracle: {kind: be_unit, test: "metabase.lib.drill-thru.pivot-test/returns-pivot-test-2a-cat+loc-with-date", clean_baseline: pass, reconstructed_result: "3/8 fail (multi-stage variants: pivot-drill returns nil; single-stage passes)"}
  repro_confirmed: true
  confidence: high

failure_shape:
  observed: true
  source: observed_unit
  symptom: >
    The pivot drill-thru is offered for multi-stage queries (filter stage over an aggregation) where it produces wrong results.
  repro_failure: >
    returns-pivot-test-*a multi-stage variant: expected a :drill-thru/pivot, actual nil.
  not_setup_crash: true

coverage:
  be_unit: {status: kill, killed_by: ["metabase.lib.drill-thru.pivot-test/returns-pivot-test-2a-cat+loc-with-date"], clean_baseline: pass}
  fe_unit: {status: untested, killed_by: []}
  notes: "Semantic-reconstruction batch-5 (no_added_deftest bucket; modified-deftest oracle). Killed by shipped BE test => e2e repro cullable."
