import {
  CloseButton as MantineCloseButton,
  type CloseButtonProps as MantineCloseButtonProps,
} from "@mantine/core";

export const CloseButton = (props: MantineCloseButtonProps) => {
  return <MantineCloseButton {...props} />;
};
