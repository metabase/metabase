{
  "description": "Dimension reference clause [:dimension opts uuid].\n   Handles normalization from JSON where the tag arrives as a string.",
  "prefixItems": [
    {
      "const": "dimension"
    },
    {
      "$ref": "./metabase.lib-metric.schema..dimension-reference.options.json"
    },
    {
      "$ref": "./metabase.lib-metric.schema.dimension-id.json"
    }
  ],
  "type": "array"
}
