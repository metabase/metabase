{
  "name": "change-table-visualization-settings",
  "description": "Use this tool only to show or hide columns in a table. Make sure that the visualization type is `table` before calling this tool. Do not ever rename columns. Renaming columns is done with the change-column-settings tool.",
  "parameters": {
    "type": "object",
    "properties": {
      "visible_columns": {
        "type": ["array"],
        "description": "List of columns to show in the table. To hide columns, include all but the ones you want to hide in the list.",
        "items": {
          "type": "string"
        }
      }
    },
    "required": ["visible_columns"],
    "additionalProperties": false
  }
}
