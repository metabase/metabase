# Papercuts from Claude transcripts: index

97 papercuts. Mined 2026-09-23 with Jev classification, Jev segment localization and Claude drill-down. Pipeline: [`pipeline/`](../../pipeline/).

Kinds: codebase-trap (the Metabase code invites the mistake), test-harness (test helpers and runners), misleading-signal (output that lies), doc-gap (missing or wrong docs), tool-quirk (CLI and tool behaviour), env-friction (shell, worktree and local environment), agent-behaviour (recurring agent habit).

## codebase-trap (14)

| severity | occ | status | papercut | area |
|---|---|---|---|---|
| high | 1 | open | [The provider connect probe saves `:probed-model` into the connection config, and the closed `LLMCredentials` schema rejects that key, so every Metabot call on a Google or vLLM connection saved through the API fails in dev and tests while prod and settings-stubbing unit tests pass](andrei.claude.llm-credentials-closed-schema-rejects-probed-model.md) | src/metabase/metabot/self/core.clj (ApiKeyCredentials, GoogleCredentials, LLMCredentials, LLMRequestOpts); src/metabase/llm/api/provider.clj (POST /providers merges :learned-config into :config); src/metabase/llm/provider.clj (resolve-model-ref, with-field-defaults); src/metabase/metabot/self/google.clj and vllm.clj (list-models :probe? returns :learned-config {:probed-model ...}); src/metabase/util/malli/fn.clj (instrument-ns?) |
| high | 1 | open | [The reindex orphan sweep issues `DROP TABLE` on whatever connection is bound, including a request transaction conveyed into a reindex `future`, so on Postgres a sweep that loses a race to another sweep aborts the request's transaction](andrei.claude.search-temp-index-ddl-commits-enclosing-with-temp.md) | src/metabase/search/appdb/index.clj (delete-obsolete-tables!, orphan-indexes); src/metabase/search/db.clj (drop-search-index-table!); setup API (t2/with-transaction) and the site-locale setting's reindex future; test/metabase/setup_rest/api_test.clj |
| medium | 2 | open | [`test/metabase/search/appdb/index_test.clj` contains NUL and control bytes, so the agent shell's grep prints nothing for it (even `grep -c ''`), and an agent resolving a backport concluded the cherry-picked test was missing](andrei.claude.index-test-nul-bytes-hide-from-grep.md) | test/metabase/search/appdb/index_test.clj; the Bash tool's grep shell function |
| medium | 1 | documented-still-hit | [With `MB_LLM_ANTHROPIC_API_KEY` set, a key entered in the admin UI lands on a new `anthropic-2` connection that `MB_LLM_METABOT_PROVIDER = "anthropic/…"` never uses, so Metabot keeps failing with 401 on the env-backed key](andrei.claude.env-llm-key-shadows-ui-connection-metabot-pinned.md) | src/metabase/llm/provider.clj `connections` and env overlays; MB_LLM_METABOT_PROVIDER; mise.local.toml; admin AI settings |
| medium | 1 | open | [In dev and test, the closed `ToolCallArguments` schema only accepts string-keyed nested maps while `parse-tool-arguments` keywordizes every level, so a local Metabase fails any Metabot tool call with object arguments and agents fall back to prod run mode, which switches the checks off](andrei.claude.metabot-tool-call-arguments-schema-rejects-keyword-maps.md) | src/metabase/metabot/self/core.clj (ToolCallArguments, AISDKPart, parse-tool-arguments); src/metabase/request/schema.clj (::json-value); task.suggested-prompts-generator; deps.edn :run alias (-Dmb.run.mode=dev) |
| medium | 1 | open | [Metabot's chart-type list offers "sunburst", which is not a card display, and the frontend silently renders any unknown display as a table, so agents guessed wrong about what happens and a bogus display persists without an error](andrei.claude.unknown-card-display-falls-back-to-table.md) | src/metabase/metabot/tools/shared.clj (chart-types); frontend/src/metabase/viz-core/lib/registry.ts (visualizations.get fallback; was visualizations/lib/registry.ts); frontend visualizations/echarts/pie/option.ts |
| medium | 1 | open | [The display types Metabot and the Agent API accept were hand-copied into several enums, so a new chart type lands in some lists and not others and the model silently loses it on one surface](andrei.claude.chart-display-type-lists-hand-copied.md) | src/metabase/metabot/tools/shared.clj (chart-types); metabot tools charts.clj, charts/create.clj, charts/edit.clj, document.clj; src/metabase/agent_api/api.clj (::card-display); frontend cardDisplayTypes |
| medium | 1 | open | [Without a running nREPL, `./bin/mage run-tests` prints `clj -X:dev:ee:ee-dev:test` but executes `-X:dev:dev-ee:ee:test` (no such alias), so every enterprise test namespace fails with "Could not locate ..._test__init.class"](andrei.claude.mage-run-tests-cli-fallback-drops-ee-test-path.md) | mage/src/mage/quick_test_runner.clj (run-tests-cli), deps.edn :ee-dev alias, bb.edn run-tests task |
| medium | 1 | open | [`SettingsSection` renders its title and description only when `children` is truthy, so the natural `isEnabled && (...)` guard hides the card's own on/off switch and the setting can never be turned back on](andrei.claude.settings-section-drops-header-on-falsy-children.md) | frontend/src/metabase/settings-components/SettingsSection/SettingsSection.tsx; frontend/src/metabase/admin/ai/McpAppsSettings.tsx and its unit spec |
| medium | 1 | open | [`track-token-usage!` slices its options with hand-kept `snowplow-arg-keys`/`prometheus-arg-keys` vectors that duplicate the closed `SnowplowArgs`/`PrometheusArgs` schemas, so a key added only to a schema passes validation and is then silently dropped (the Prometheus label falls back to `unknown`)](andrei.claude.llm-token-usage-arg-keys-duplicate-closed-schemas.md) | src/metabase/analytics/llm_token_usage.clj (snowplow-arg-keys, prometheus-arg-keys, SnowplowArgs, PrometheusArgs, TrackTokenUsageArgs, track-token-usage!, track-prometheus!) |
| medium | 1 | open | [dev-ee's `--hot` reload re-requires git-dirty namespaces in no particular order, so adding a var in one file and using it in another made every request 500 ("No such var") on the shared server until both were reloaded by hand](andrei.claude.dev-hot-reload-ignores-namespace-dependency-order.md) | dev/src/dev/reload.clj (changed-namespaces, reload-namespaces!), `bun dev-ee --hot`, src/metabase/metabot/settings.clj, src/metabase/metabot/tools/search.clj |
| medium | 1 | unknown | [Adding MB_ENCRYPTION_SECRET_KEY to an existing plaintext dev app DB made the backend exit at startup, and the documented `enable-encryption` command then aborted on a strict setting read, so the agent had to move to a fresh app DB](andrei.claude.enable-encryption-fails-on-plaintext-dev-appdb.md) | src/metabase/cmd/enable_encryption.clj, app_db/encryption.clj, settings strict decrypt (as of 2026-09-03), local dev-ee startup |
| low | 1 | open | [Metabot's tool-output `:error` map and Snowplow `event_details` are closed malli maps checked far from where they are built, so a key added in `run-tool` or the tool-usage event passes locally and only throws in dev on the next LLM call or event send](andrei.claude.metabot-closed-part-schemas-fail-next-llm-call.md) | src/metabase/metabot/self/core.clj (run-tool, AISDKPart, LLMRequestOpts), src/metabase/metabot/self.clj (agent_used_tool event), src/metabase/analytics/snowplow.clj (:event-details) |
| low | 1 | open | [`create-api-key-with-new-user!` returns `:unmasked_key` as a `metabase.util.secret` wrapper, so an agent scripting over nREPL writes `<< REDACTED SECRET >>` into its .env and only finds out when zsh fails to source it](andrei.claude.api-key-unmasked-key-is-a-secret-wrapper.md) | src/metabase/api_keys/models/api_key.clj (create-api-key-with-new-user!), src/metabase/util/secret.clj (Secret toString), nREPL dev scripting |

## test-harness (28)

| severity | occ | status | papercut | area |
|---|---|---|---|---|
| high | 1 | documented-still-hit | [Running backend tests with `clojure -M:dev:ee:ee-dev -e "(clojure.test/run-tests ...)"` instead of `./bin/test-agent` leaves the JVM alive after the tests finish and uses the checkout's dev-mode H2 file, so each call burns the whole Bash timeout, trailing cleanup such as `git stash pop` never runs, and leftover JVMs make later runs fail with `Unable to connect to Metabase h2 DB`.](andrei.claude.adhoc-clojure-e-test-runs-never-exit.md) | clojure -M:dev -e test runs, deps.edn :dev vs :test aliases, bin/test-agent, H2 app DB file, Bash tool timeout |
| medium | 4 | documented-still-hit | [`dev/src/user.clj` merges the developer's `mise.local.toml` `[env]` block into `environ` in every JVM with the `:dev` alias, including `./bin/test-agent` runs, so locally set `MB_LLM_*` vars make metabot settings tests fail on the laptop but not in CI, and review agents diagnose the failures as a broken test fixture.](andrei.claude.dev-user-clj-leaks-mise-local-env-into-tests.md) | dev/src/user.clj (load-mise-local!), mise.local.toml, bin/test-agent (:dev alias), metabase.metabot.settings-test (do-with-selected-model!) |
| medium | 4 | fixed | [The `documents/comments.cy.spec.ts` e2e spec failed intermittently on master and on unrelated PRs in three different ways and was not quarantined, so each hit hard-failed e2e-group-35 and agents spent reruns and log triage proving it was not their change](andrei.claude.document-comments-hover-reveal-click-flake.md) | `e2e/test/scenarios/documents/comments.cy.spec.ts`, e2e-group-35-ee job, CI Conductor quarantine gate |
| medium | 2 | fixed | [A `^:parallel` metabot viewing-context test used a hardcoded card id (789) expecting "not found", so whenever another test owned that id it got a 403 path instead and failed on unrelated PRs across several drivers](andrei.claude.tests-assume-hardcoded-entity-ids-absent.md) | test/metabase/metabot/agent/user_context_test.clj (format-viewing-context-test-2c) |
| medium | 1 | fixed | [A Slack file dropped with no text became an empty user turn in the Slackbot's rebuilt history, so every later request in that DM was rejected by the model API and a live replication gave identical wrong replies for two different tests](andrei.claude.slackbot-file-share-without-text-poisons-thread.md) | src/metabase/slackbot/streaming.clj (thread->history, ignore-msg?, user-msg-content); manual Slack testing (one bot DM reused for every run); Anthropic messages API |
| medium | 1 | fixed | [`accepted-admin-emails` only mails admins who have a `last_login`, so `publish-refresh-error-event-sends-admin-email-test` passed or failed depending on whether an earlier test had logged in the shared admin, and an agent blamed a shared-inbox race instead](andrei.claude.admin-failure-email-skips-never-logged-in-admins.md) | src/metabase/channel/db.clj `accepted-admin-emails`; test/metabase/model_persistence/task/persist_refresh_test.clj |
| medium | 1 | open | [`MetabotAgentDataSourcePills.unit.spec.tsx` "sends a feedback request when changing an already selected source" fails about one run in three, and a single passing master control convinced an agent its unrelated change broke it](andrei.claude.metabot-datasource-pills-feedback-test-click-race.md) | frontend/src/metabase/metabot/components/MetabotChat/MetabotAgentDataSourcePills.unit.spec.tsx |
| medium | 1 | open | [`getSettings(state)` falls back to `window.MetabaseBootstrap`, which the core jest setup replaces with a fresh object before every test and deletes after it, so a spec reusing one module-level `state` sees memoized selectors like `getMetadata` change identity from the second test on, which looks like an arity or memoization bug](andrei.claude.getsettings-window-bootstrap-deleted-between-jest-tests.md) | frontend/src/metabase/settings/selectors.ts `getSettings`; frontend/test/jest-setup-env.js afterEach; frontend/src/metabase/metadata-store/selectors.ts `getMetadata` |
| medium | 1 | open | [`metabase-enterprise.metabot.usage-test` leaves a 'limit reached' result in the 10-second TTL memo of the usage-limit check, so Metabot namespaces run after it in the same JVM fail with `ai_usage_limit_reached` ('test limit reached')](andrei.claude.metabot-usage-limit-ttl-cache-leaks-across-tests.md) | enterprise/backend/src/metabase_enterprise/metabot/usage.clj (check-instance-limit*, check-tenant-limit*, check-user-limit* via memoize/ttl; clear-limit-cache!); enterprise/backend/test/metabase_enterprise/metabot/usage_test.clj; test/metabase/metabot/self_test.clj, agent/core_test.clj |
| medium | 1 | open | [`mt/metric-value` hands any label map to iapetos, which keys series by the collector's declared labels: a lookup missing a declared label silently reads a fresh 0.0 child and an undeclared label is silently ignored, so adding a Prometheus label breaks tests far from the diff while the new label's own test passes against reverted code](andrei.claude.metric-value-label-mismatch-reads-zero.md) | `test/metabase/test/util.clj` `metric-value`, `src/metabase/analytics/prometheus.clj` (`qualified-vals`, `:metabase-metabot/llm-*` collectors), iapetos `set-labels`, `test/metabase/metabot/self_test.clj`, `test/metabase/metabot/api/document_test.clj`, `test/metabase/analytics/llm_token_usage_test.clj` |
| medium | 1 | open | [`vllm-test/preflight-cancels-the-sibling-probe-on-failure-test` fails under CI load when the sibling probe's future is cancelled before it starts, so its `interrupted` promise is never delivered](andrei.claude.vllm-preflight-cancel-test-race.md) | test/metabase/metabot/self/vllm_test.clj (preflight-cancels-the-sibling-probe-on-failure-test); src/metabase/metabot/self/vllm.clj preflight probes |
| medium | 1 | open | [`with-fake-snowplow-collector` turns anon tracking on but fakes only Snowplow, so with `MB_METAPLOW_URL` in mise.local.toml backend tests POST real events to the collector and a Metabot test fails locally](andrei.claude.fake-snowplow-collector-sends-real-metaplow-events.md) | test/metabase/analytics/snowplow_test.clj (do-with-fake-snowplow-collector!), src/metabase/analytics/event.clj (track-event! fan-out), src/metabase/analytics/settings.clj (metaplow-tracking-enabled), dev/src/user.clj (load-mise-local!), metabase.metabot.agent.core-test |
| low | 3 | open | [The MetabotSettingsPanel "should toggle embedded metabot enabled state" spec clicks a switch that is disabled while settings load, so it fails locally about 3 runs in 5 on clean master while CI stays green](andrei.claude.metabot-settings-panel-toggle-spec-flaky.md) | frontend/src/metabase/admin/ai/MetabotSettingsPanel.unit.spec.tsx (enabledToggle, waitForRequest); frontend/src/metabase/admin/ai/MetabotSettingsPanel.tsx |
| low | 3 | open | [`llm-token-usage-test/track-snowplow!-premium-token-test` fails in every targeted local run, on the branch and on master, because the first read of `instance-creation` in a fresh app DB emits a `new_instance_created` event into the test's fake Snowplow collector](andrei.claude.standalone-test-controls-mislead-about-branch-regressions.md) | test/metabase/analytics/llm_token_usage_test.clj (track-snowplow!-premium-token-test); src/metabase/analytics/settings.clj (-instance-creation); snowplow-test/with-fake-snowplow-collector |
| low | 2 | open | [The e2e test 'should deep duplicate a dashboard and its cards to a collection created on the go' in `dashboard/dashboard-management.cy.spec.js` intermittently times out waiting for `[data-testid="create-collection-on-the-go"]`](andrei.claude.dashboard-management-deep-duplicate-e2e-flake.md) | e2e/test/scenarios/dashboard/dashboard-management.cy.spec.js; e2e-group-27-ee |
| low | 1 | documented-still-hit | [Capturing `ns/f` by value to "restore the real one" inside `with-dynamic-fn-redefs` captures the proxy once any earlier test patched the var, so the test passes alone and fails in its namespace](andrei.claude.dynamic-redefs-captured-var-value-is-proxy.md) | test/metabase/test/util/dynamic_redefs.clj (with-dynamic-fn-redefs, patch-vars!, original-fn); test/metabase/slackbot/streaming_test.clj |
| low | 1 | fixed | [`field-values-test/detached-fetch!-sweeps-stalled-fetches-test` raced on a `:future-ref` that was still nil (NPE in `future-cancelled?`), failing the H2 driver job on an unrelated PR](andrei.claude.field-values-detached-fetch-sweep-test-race.md) | test/metabase/warehouse_schema/models/field_values_test.clj; src/metabase/warehouse_schema/models/field_values.clj in-flight fetch registry |
| low | 1 | open | [The SDK host-app e2e 'serves the report to shell agents, with cursor filtering' in `data-app-dev-diagnostics.cy.spec.ts` intermittently fails with 'expected [ Array(3) ] to be empty' on the production-bundle job](andrei.claude.sdk-data-app-dev-diagnostics-e2e-flake.md) | e2e/test-host-app/data-apps/data-app-dev-diagnostics.cy.spec.ts; sdk-tests e2e-host-app-vite-6-data-app-host-app-e2e-tests-production-bundle |
| low | 1 | open | [The e2e test 'Pro-cloud instance should be able to save and clear email settings' in `admin-2/settings.cy.spec.js` still flakes with '`cy.blur()` can only be called when there is a currently focused element' despite an earlier flake fix](andrei.claude.admin-settings-email-e2e-blur-flake.md) | e2e/test/scenarios/admin-2/settings.cy.spec.js (email settings, Pro-cloud instance); e2e-group-16-ee |
| low | 1 | open | [The e2e test 'should handle required parameters' in `embedding/embedding-dashboard.cy.spec.js` intermittently times out finding the 'Update filter' button, turning e2e-group-43-ee red on unrelated PRs](andrei.claude.embedding-dashboard-required-parameters-e2e-flake.md) | e2e/test/scenarios/embedding/embedding-dashboard.cy.spec.js ('should handle required parameters'); e2e-group-43-ee |
| low | 1 | open | [The jest helper `findRequests` awaits `fetchMock.callHistory.flush()`, which waits for every in-flight response, so a spec that deliberately holds a PUT pending hangs to jest's 30 s timeout; and overriding the settings PUT mock needs the helper's exact route name `update-setting`, otherwise the helper's earlier route keeps answering silently.](andrei.claude.findrequests-flush-hangs-on-held-responses.md) | frontend/test/__support__/server-mocks/util.ts (findRequests), frontend/test/__support__/server-mocks/settings.ts (setupUpdateSettingEndpoint), fetch-mock removeRoute |
| low | 1 | open | [`cloud-migration-test/migrate!-test` fails intermittently on app-DB jobs because `POST /api/cloud-migration` returns 409 'There's an ongoing migration already' whenever any non-terminal cloud_migration row exists](andrei.claude.cloud-migration-post-409-test-flake.md) | test/metabase/cloud_migration/models/cloud_migration_test.clj (migrate!-test); src/metabase/cloud_migration/api.clj (POST /) |
| low | 1 | open | [`field-values-test/get-or-create-full-field-values!-outlives-canceled-caller-test` polls up to 10 s for FieldValues saved by a background fetch and ran out of time on the H2 (OSS) driver job of an unrelated PR](andrei.claude.field-values-canceled-caller-test-timing-flake.md) | test/metabase/warehouse_schema/models/field_values_test.clj (get-or-create-full-field-values!-outlives-canceled-caller-test); metabase.test.util/poll-until |
| low | 1 | open | [`metabase.llm.api.provider` keeps model listings in a private `defonce` 60-second TTL cache that no test fixture resets, so rerunning `provider-test` in a warm JVM within a minute fails five cache-keying tests whose stubs are never called.](andrei.claude.llm-provider-models-cache-survives-test-reruns.md) | src/metabase/llm/api/provider.clj (models-cache, models-cache-key), test/metabase/llm/api/provider_test.clj, warm-REPL test runs |
| low | 1 | open | [`metabase.metabot.self-test/sse-reducible-stops-response-test` asserts the server wrote fewer than 10 of 30 events within a fixed 30 ms sleep, which fails on a loaded CI runner](andrei.claude.sse-reducible-stop-test-timing-flake.md) | test/metabase/metabot/self_test.clj (sse-reducible-stops-response-test); metabase.metabot.self.core/sse-reducible |
| low | 1 | open | [`python-transform-logging-test` asserts that a deliberately slow Python transform produced more than one partial log message before finishing, a timing-dependent check that failed a CI job on an unrelated backend PR (`expected (< 1 (count observed-messages))`, got 1).](andrei.claude.python-transform-logging-test-timing-flake.md) | enterprise/backend/test/metabase_enterprise/transforms_python/transforms_api_test.clj |
| low | 1 | open | [`vector-strategy-matrix-test/distance-recall-matrix-test` asserts a naive HNSW query returns exactly `[]` ("structural, not graph luck"), but HNSW is approximate and returned a rare-band doc, failing semantic-search CI on an unrelated PR](andrei.claude.semantic-search-hnsw-recall-test-asserts-exact-empty.md) | enterprise/backend/test/metabase_enterprise/semantic_search/vector_strategy_matrix_test.clj (line ~397) |
| low | 1 | open | [jest.config.js splits tests into `projects` that each spread the base config, so a wrapper config overriding `moduleNameMapper` or `rootDir` at the top level never reaches the tests and a mutation run passes against the real component](andrei.claude.jest-config-projects-ignore-top-level-overrides.md) | jest.config.js, jest.base.conf.js; ad-hoc `--config` wrappers used for mutation or isolation runs |

## misleading-signal (12)

| severity | occ | status | papercut | area |
|---|---|---|---|---|
| medium | 3 | unknown | [A one-line frontend dependency bump went red on nine checks (classpath resolution, backend start timeouts, an H2 export test), and it took an hour, a user push-back and 36-run base rates to show they were infra flakes](andrei.claude.ci-clojure-classpath-resolution-flake.md) | Run tests workflow (e2e groups, SDK backward-compat, driver H2), prepare-backend, ci-conductor quarantine |
| medium | 2 | open | [`./bin/mage cljfmt-files -c <path>` silently skips paths that do not exist and prints "All source files formatted correctly" with exit 0, so a typo'd or glued path turns the formatter gate into a false green](andrei.claude.mage-cljfmt-files-passes-nonexistent-paths.md) | bb.edn `cljfmt-files` task, mage/src/mage/format.clj, cljfmt.tool/find-files |
| medium | 1 | fixed | [`./bin/mage kondo` cleared the Kondo cache and linted in one pass, so cache-reading hooks such as `:metabase/prefer-with-dynamic-fn-redefs` never fired, locally or in CI, and a new `with-redefs` stub passed every lint gate until a reviewer indexed the redefined namespace](andrei.claude.kondo-cache-reading-hooks-silent-on-cold-cache.md) | mage/src/mage/kondo.clj, .clj-kondo hooks reading .clj-kondo/.cache (hooks/ns-analysis), :metabase/prefer-with-dynamic-fn-redefs, CI be-linter-clj-kondo |
| medium | 1 | open | [GitHub's contents API `Last-Modified` header reflects the latest push to the branch, not the last commit that touched the file, so an ingestion connector's freshness check was built on repository activity](andrei.claude.github-contents-last-modified-is-repo-head.md) | GitHub REST contents API (`Last-Modified` header); freshness checks for connectors that ingest a file from a GitHub repo |
| medium | 1 | open | [mu/defn "Invalid input" errors print one nil per valid element of a sequential argument, so on a long Metabot parts vector the real violation is buried behind dozens of nils or truncated away](andrei.claude.malli-invalid-input-error-pads-valid-elements-with-nil.md) | src/metabase/util/malli/fn.clj (validate), Metabot agent loop validating LLMRequestOpts in src/metabase/metabot/self/core.clj, metabase.lib.equality/find-matching-column, dev-ee.log, metabot_message.error |
| low | 2 | open | [Grepping GitHub Actions job logs for "error" matches the step scripts GitHub prints before running them, so `echo "::error::Docker login failed after 3 attempts"` template lines were reported to the user as evidence of infra failures](andrei.claude.gha-log-grep-matches-echoed-error-templates.md) | GitHub Actions job logs, .github/actions/* scripts with retry/`::error::` messages |
| low | 2 | open | [Metabase test output carries ANSI color codes between words even in CI logs and redirected files (`ESC[31mERROR ESC[m in ...`), so `grep 'FAIL in\/ERROR in'` silently matches nothing and the failure detail looks missing](andrei.claude.ci-and-test-logs-ansi-escapes-break-grep.md) | metabase test reporter output (`clojure -X:dev:test`, mb.hawk), GitHub Actions job logs via `gh run view --log-failed` |
| low | 1 | open | [A stale `origin/<branch>` ref (no fetch prune) made a merged, auto-deleted PR branch look in sync, so pushing a master merge to it recreated the branch on the public repo](andrei.claude.stale-remote-tracking-ref-resurrects-merged-pr-branch.md) | git fetch without --prune, per-PR worktrees under ~/src/mb/wt, GitHub delete-branch-on-merge |
| low | 1 | open | [The Liquibase migration linter's strict spec allows one column per `addColumn` change but reports a two-column change only as a raw clojure.spec "Extra input" problem map buried among hundreds of "Validating ..." lines](andrei.claude.migration-linter-one-column-per-addcolumn-raw-spec-error.md) | bin/lint-migrations-file.sh, bin/lint-migrations-file/src/change/strict.clj (:change.strict.add-column/columns), resources/migrations |
| low | 1 | open | [`bun frontend/lint/scripts/side-effect-files.js` fails on any stale registry entry, but the CI spec guarding the same registry deliberately tolerates stale `self` entries, so a review agent drafted a nit claiming the check "isn't run in CI" for drift CI accepts by design](andrei.claude.side-effect-registry-script-stricter-than-ci-spec.md) | frontend/lint/scripts/side-effect-files.js; frontend/lint/side-effect-files.json; frontend/lint/tests/side-effect-files.unit.spec.js |
| low | 1 | open | [`gh pr checks <n>` without `-R` resolves the repository from the current directory, so after a `cd` into another repository it failed and, piped through grep/awk, printed an empty summary that read like 'no failures'](andrei.claude.gh-pr-commands-resolve-repo-from-cwd.md) | gh pr checks / gh pr view without -R; workspace holding several git repos and worktrees |
| low | 1 | open | [metabase still ships `.coderabbit.yaml` with a `coderabbit` label trigger and keeps a `Claude Code Review` label, but neither review bot runs any more, so applying either label silently does nothing](andrei.claude.stale-coderabbit-config-and-review-labels.md) | `.coderabbit.yaml`, repo labels `coderabbit` and `Claude Code Review`, deleted `.github/workflows/claude-code-review.yaml` |

## doc-gap (4)

| severity | occ | status | papercut | area |
|---|---|---|---|---|
| medium | 6 | open | [The per-file Clojure lint command is `./bin/mage kondo <files>`, documented only in skill files, so agents reach for `./bin/kondo.sh` (deleted in 2025) or a bare `clj-kondo` that is not on PATH, and sometimes give up on lint](andrei.claude.bare-clj-kondo-lint-misses-repo-hooks.md) | `clj-kondo` (not installed), `mise exec`, `./bin/mage kondo`, metabase `CLAUDE.md` |
| medium | 1 | documented-still-hit | [No local command mirrors the backend CI lint jobs (cljfmt, kondo, Eastwood on sources, Eastwood on changed tests), so a fix was pushed with only kondo and Eastwood run locally and CI failed on a blank line](andrei.claude.no-single-local-command-for-backend-lint-gates.md) | .github/workflows/backend.yml (be-cljfmt, be-linter-clj-kondo, Eastwood jobs); mage project-tests |
| medium | 1 | open | [New Liquibase migrations must live in `resources/migrations/0<CURRENT_VERSION+1>/` with matching `v<N>.` IDs, which changes the day a release branch is cut; only CI's `check-migration-versions` knows, the local migration lints pass, and `CURRENT_VERSION` is a repo variable agents cannot read](andrei.claude.new-migrations-dir-moves-after-release-cut.md) | resources/migrations/0NN/; bin/check-migration-versions; .github/workflows/migrations.yml (check-migration-versions, vars.CURRENT_VERSION); ./bin/mage lint-migrations; bin/lint-migrations-file |
| low | 1 | open | [A comment in Metabase's deps.edn tells readers to run `./bin/mage cljfmt-file`, a task that does not exist (it is `cljfmt-files`), and mage answers the unknown task by printing its task list with no error line](andrei.claude.deps-edn-cljfmt-comment-names-missing-mage-task.md) | deps.edn comment block near the old :cljfmt alias, bb.edn mage tasks, mage unknown-task handling |

## tool-quirk (22)

| severity | occ | status | papercut | area |
|---|---|---|---|---|
| medium | 3 | documented-still-hit | [Running Metabase backend tests as raw `clojure -X:dev:test` into a log file writes the progress bar's carriage returns and ANSI colours into it, so grepping for failures returned 90 KB of noise, although the repo's `./bin/test-agent` exists for exactly this](andrei.claude.raw-clojure-test-run-ansi-hides-failures.md) | metabase test runner progress reporter, `clojure -X:dev:test`, `./bin/test-agent`, log files under /tmp |
| medium | 2 | open | [A PR GitHub still counts as part of a stack (even after its base was retargeted to master) cannot be merged with `gh pr merge` or `PUT /pulls/{n}/merge`; only the `/merge-async` endpoint works, and it merges every PR below it in the stack](andrei.claude.stacked-pr-merge-needs-merge-async.md) | gh pr merge; GitHub REST PUT /repos/{o}/{r}/pulls/{n}/merge and /merge-async; stacked PRs |
| medium | 1 | documented-still-hit | [Bare `grep` in the Bash tool is a shell function running ugrep with `-I`, so on a CI log containing a stray binary byte it prints nothing and exits 1; the agent read that as 'no FAIL/ERROR lines' until `/usr/bin/grep -a` found them](andrei.claude.bash-grep-is-ugrep-shim.md) | Claude Code Bash tool `grep` function (ugrep `-G --ignore-files --hidden -I`), GitHub Actions job logs saved to files |
| medium | 1 | open | [A "revert the fix, watch the test fail" probe built on `git stash push -- <file>` is a silent no-op once the fix is committed, so the test ran against the fixed code, passed, and the agent briefly concluded its regression test was vacuous](andrei.claude.git-stash-revert-probe-noop-on-committed-fix.md) | git stash push with a pathspec, compound Bash commands with output sent to /dev/null, revert-and-rerun test probes |
| medium | 1 | open | [A draft the agent wrote as interim text between tool calls never reached the user, because the desktop app collapses that text into a one-line summary](andrei.claude.interim-text-between-tool-calls-hidden-in-desktop-app.md) | Claude desktop app transcript rendering; agent turn structure (deliverable plus follow-on work in one turn) |
| medium | 1 | open | [On macOS, `git grep -E 'name\b'` silently matches nothing (exit 1) while BRE, `-P`, `-w` and `/usr/bin/grep -E` all match, so a reviewer three times concluded a definition did not exist and grepped an empty file name](andrei.claude.git-grep-extended-regex-word-boundary-matches-nothing.md) | git grep on macOS (Apple Git); regex patterns with `\b` |
| medium | 1 | open | [Rerunning a label-gated GitHub Actions job replays the original event payload, so 'Decide whether to backport or not' fails again after the label is added, and the fresh red rerun is what `gh pr checks` then reports although the merge box ignores it](andrei.claude.gha-rerun-replays-original-event-payload.md) | `.github/workflows/backport-reminder.yml` (`contains(github.event.pull_request.labels.*.name, ...)`), `gh run rerun --failed`, `gh pr checks` |
| medium | 1 | open | [The Bash tool runs every command as `eval '…' && pwd -P >/ …`, so `set -e` never aborts a script: a failed step is skipped over and later steps run on unmodified inputs](andrei.claude.set-e-ignored-in-bash-tool-eval-chain.md) | Claude Code Bash tool (zsh wrapper); multi-step verification scripts such as mutation harnesses |
| medium | 1 | open | [`./bin/mage kondo-updated` never copies the dependency kondo configs, so in a fresh worktree it reports bogus errors ("Could not find namespace: hooks.toucan2.tools.with-temp", unresolved symbols) until `.clj-kondo/imports` is copied in by hand](andrei.claude.kondo-updated-skips-dependency-config-copy.md) | mage/src/mage/kondo.clj (kondo-updated vs kondo), .clj-kondo/imports, git worktrees under ~/src/mb/wt |
| medium | 1 | open | [`jest --ignoreProjects ci-scripts <spec path>` treats the path as a second project to ignore because the option is variadic, so jest silently runs all ~2,200 frontend suites instead of one](andrei.claude.jest-ignoreprojects-swallows-test-path.md) | package.json test-unit / test-unit-keep-cljs scripts; jest-cli --ignoreProjects (yargs type: 'array'); jest.config.js projects |
| medium | 1 | unknown | [Headless `codex exec` inherits the global ~/.codex config (ultra reasoning effort), so a one-file edit task ran for 10 minutes spawning sub-agents and was killed by the Bash tool timeout with no output](andrei.claude.codex-exec-inherits-global-reasoning-effort.md) | codex exec (VS Code extension bundle, 0.151 alpha); ~/.codex/config.toml (model_reasoning_effort); Claude Code Bash 10-minute timeout |
| low | 7 | open | [`gh api repos/<repo>/actions/jobs/<id>/logs` refuses to print a CI log that contains ANSI escapes (one notice line on stderr) unless given `--allow-escape-sequences`, so `2>/dev/null / grep` comes back empty and `> file` yields a one-line file while the agent hunts for a CI error.](andrei.claude.gh-ci-inspection-silent-empty-and-in-progress-logs.md) | gh api job logs, gh run view --log-failed, CI failure triage |
| low | 2 | open | [`gh run rerun --job`/`--failed` on a red job refuses with 'cannot be rerun' while any other job in the run is still going, so agents that rerun a flake straight away need an extra wait loop](andrei.claude.gh-rerun-refused-while-run-in-progress.md) | gh run rerun; GitHub Actions REST POST /actions/jobs/{id}/rerun |
| low | 1 | documented-still-hit | [Running the Eastwood changed-test gate with `:namespaces '"[a b]"'` passes a string, and the linter dies with an opaque `Character cannot be cast to Symbol` buried under JVM frames](andrei.claude.eastwood-namespaces-arg-quoted-as-edn-string.md) | clojure -X:...:eastwood/test :namespaces; test/metabase/linters/eastwood.clj (eastwood-tests); .github/workflows/backend.yml changed-test Eastwood step |
| low | 1 | open | [Bun's SQL client JSON-encodes a string bound to a jsonb parameter, so the node-postgres habit `${JSON.stringify(obj)}::jsonb` silently stores a JSON string instead of an object](andrei.claude.bun-sql-jsonb-param-double-encodes-strings.md) | Bun 1.3.14 `SQL` tagged template against Postgres jsonb columns; agent-written Bun scripts |
| low | 1 | open | [In a worktree-isolated session the guard refused six git-free commands, a `for` loop of `sed / grep` over source files and Python heredocs that read or wrote scratchpad files among them, as "too complex to verify that it stays inside the worktree"](andrei.claude.worktree-isolation-guard-refuses-heredoc-redirects.md) | Claude Code worktree isolation guard (after EnterWorktree); Bash commands with loops, heredocs, redirects and `$(...)` |
| low | 1 | open | [`gh pr view --json body --jq .body / diff - body.md` after `gh pr edit --body-file body.md` always reports one extra trailing line, so the agent's post-edit check failed and was ignored](andrei.claude.gh-pr-body-roundtrip-extra-trailing-newline.md) | gh pr edit --body-file; gh pr view --json body --jq .body |
| low | 1 | open | [`git grep -n PATTERN -A 12 -- path` fails with 'fatal: unable to resolve revision: -A' because git grep stops parsing options at the pattern, and agents used to GNU grep keep putting options after the pattern](andrei.claude.git-grep-options-after-pattern.md) | git grep |
| low | 1 | unknown | [Driving github.com through the Chrome extension to attach screenshots to PR descriptions failed right after each navigation ("No URL available for active tab"), so the user had to upload the images by hand](andrei.claude.chrome-extension-loses-github-tab-url.md) | Claude in Chrome extension, github.com PR pages, PR description screenshots |
| low | 1 | unknown | [Navigating the Browser pane to a file:// page outside the project opens an inert "static snapshot" tab where `screenshot` and `get_page_text` fail with "No site is open in this tab" and further navigation is refused](andrei.claude.browser-pane-file-url-tab-is-inert-snapshot.md) | mcp__Claude_Browser__navigate with file:// URLs; local HTML previews |
| low | 1 | unknown | [The Browser pane `screenshot` action returns an 800-pixel JPEG only inline, with no way to write it to disk, so an agent asked for PNG files searched ~/.claude, /tmp and /var/folders and finally base64-decoded the image out of its own transcript JSONL](andrei.claude.browser-pane-screenshot-cannot-save-to-file.md) | mcp__Claude_Browser__computer screenshot; tasks whose deliverable is an image file |
| low | 1 | unknown | [The Notion MCP `update_content` command matches each `old_str` against the page's stored text, where pasted Slack links had become mention objects, so anchors copied from a local markdown copy failed with `No matches found` and the whole multi-edit batch was rejected; the retried batch also left a stray `</span>` and orphaned label fragments to clean up.](andrei.claude.notion-update-anchor-misses-link-mentions.md) | Notion MCP notion-update-page (command update_content), local markdown copy of a Notion page |

## env-friction (13)

| severity | occ | status | papercut | area |
|---|---|---|---|---|
| high | 20 | open | [In the zsh Bash tool, `echo =====LABEL` is an `=command` expansion that fails with "(eval):1: ====LABEL not found" and aborts the rest of the command line, so eight chained read commands in a row returned nothing](andrei.claude.zsh-and-bsd-shell-quirks.md) | Claude Code Bash tool (zsh), section separators in compound commands |
| medium | 7 | open | [In the agent shell `sed` is an alias for GNU `gsed`, so the macOS habit `sed -i '' 's/a/b/' file` takes `''` as the script and the substitution as a file name: the file is left unchanged (silently when stderr is discarded), and with `-e` the edit applies but the command still exits 2.](andrei.claude.agent-shell-inherits-interactive-aliases.md) | user shell rc files (`alias sed=gsed`), Bash tool shell snapshot, in-place edits with sed -i |
| medium | 6 | open | [Told that `sleep` is blocked and to use an until-loop, a review orchestrator ran `until [ -s <background task output> ]` in the foreground and stayed blocked for the full 600 s timeout while its two lens subagents' completion notices queued behind it](andrei.claude.auto-backgrounded-bash-polling-and-cwd.md) | Bash tool sleep block and hint, auto-backgrounded commands, 600 s foreground timeout, subagent task notifications |
| medium | 5 | documented-still-hit | [A `cd` in one of several parallel Bash calls carries over to its siblings, so the sibling that relied on the earlier directory ran `git grep`, `uv run` and `git add && git commit` in the wrong repository](andrei.claude.bash-cwd-persists-in-scratchpad-but-resets-in-worktrees.md) | Claude Code Bash tool, parallel tool calls, workspace holding several git repos |
| medium | 4 | open | [`python3` resolves to a mise shim, so in a fresh worktree (or any checkout whose `mise.toml` was never trusted) every shimmed command, even a heredoc edit, fails with 'Config files ... are not trusted' until someone runs `mise trust`; subagents start in the parent's cwd and hit it in repos they are not working on](andrei.claude.mise-untrusted-config-breaks-shims-in-new-checkouts.md) | mise shims (`~/.local/share/mise/shims/python3` first on PATH), per-checkout `mise.toml`, `git worktree add`, subagent starting cwd |
| medium | 2 | documented-still-hit | [In a fresh Metabase worktree `core.hooksPath=.husky/_` points at a directory only `husky install` creates in the main checkout, so commits run no pre-commit hook at all, lint-staged never runs `mage cljfmt-files`, and unformatted Clojure reached CI's cljfmt gate](andrei.claude.husky-precommit-needs-node-modules-in-worktree.md) | git `core.hooksPath=.husky/_` (relative, shared config), husky, lint-staged.config.cjs (`./bin/mage cljfmt-files` for *.clj), git worktrees, CI `backend-tests / Cljfmt` |
| medium | 2 | open | [In a fresh metabase worktree, `bun run jest <spec>` and the documented `bun run test-unit-keep-cljs` fail with a moduleNameMapper 'Could not locate module cljs/...' error because `target/cljs_dev` does not exist; the agent copied another checkout's compiled CLJS to get tests running](andrei.claude.jest-needs-built-cljs-in-fresh-worktree.md) | `package.json` (`test-unit` vs `test-unit-keep-cljs`), jest `moduleNameMapper` for `cljs/*` → `target/cljs_dev`, `.claude/skills/_shared/typescript-commands.md`, git worktrees |
| medium | 1 | fixed | [`mise activate` in .zshrc only updates PATH from prompt and cd hooks, which never fire in the Bash tool, so bare `java` and `clojure` resolved to the system JDK 21 while the repo and CI use Temurin 25, and tests and Eastwood silently ran on the wrong JDK](andrei.claude.bash-tool-skips-mise-activate-system-jdk.md) | ~/.zshrc mise activation, Bash tool shell, ./bin/test-agent, `clojure -X:…:eastwood/test` |
| medium | 1 | open | [Recreating a test Postgres container hung for the full 300 s tool timeout because `docker pull` blocked in the macOS keychain credential helper; only a raw Engine API pull through the socket worked](andrei.claude.docker-credential-helper-hangs-pull.md) | Docker CLI on OrbStack (credential helper), `docker run` of an image not present locally; local uploads Postgres container for Slackbot testing |
| medium | 1 | open | [Three parallel subagents of one session each wrote their mutation-testing harness to the shared scratchpad as `mutate.py`, so one found a sibling's script in place of its own and another executed a sibling's harness believing it was its own](andrei.claude.parallel-subagents-share-scratchpad.md) | Claude Code session scratchpad shared by concurrent subagents (<scratchpad>/mutate.py and other generically named working files) |
| medium | 1 | unknown | [When the local dev instance comes up on a fresh app DB it sits at the first-run setup wizard, which needs an admin password agents may not enter, so a live-app screenshot task silently degraded into 16 minutes of hand-built HTML mockups](andrei.claude.dev-instance-setup-wizard-blocks-agents.md) | local dev instance, first-run setup flow, agents' no-account-creation rule |
| low | 2 | documented-still-hit | [Switching the metabase checkout under a running `bun dev-ee --hot` leaves localhost failing on namespaces the old JVM half-loads (here an invalid-schema macroexpansion in comments/api.clj) until someone restarts it](andrei.claude.dev-ee-breaks-when-checkout-moves-under-running-jvm.md) | `bun dev-ee` (`clojure -M:run:ee:dev:dev-start --hot`), dev/src/dev/reload.clj, src/metabase/comments/api.clj |
| low | 1 | open | [A loop variable named `path` in the zsh Bash tool replaced $PATH, so `gh`, `jq` and `ls` inside a GitHub pagination loop all failed with command not found](andrei.claude.zsh-path-variable-clobbers-path.md) | Bash tool zsh (`path` array tied to PATH), shell loops in review subagents |

## agent-behaviour (4)

| severity | occ | status | papercut | area |
|---|---|---|---|---|
| medium | 2 | open | [The coordinating agent relayed subagents' unverified findings to the user as ready-to-file tickets; two of three were factually wrong and had already been written into a doc and PR text](andrei.claude.asserts-unverified-claims-as-fact.md) | multi-agent orchestration; follow-up ticket drafts; docs and PR descriptions written by subagents |
| medium | 1 | open | [An orchestrating agent waited for its own background subagents with fixed-length foreground `until [ $(date +%s) -ge $end ]; do sleep 5; done` loops of 5 to 10 minutes, and because completion notifications are only delivered after the Bash call returns, finished subagents sat unread for up to 8.5 minutes each.](andrei.claude.fixed-sleep-loops-delay-subagent-notifications.md) | Bash tool, background Agent task notifications, Monitor tool |
| medium | 1 | open | [Before opening a stacked PR, the agent checked its local base branch against `origin/<base>` without fetching it, compared a stale ref with itself, and opened a PR that showed 19 unrelated commits](andrei.claude.base-check-against-unfetched-remote-ref.md) | git remote-tracking refs, stacked PR base branches, `git rev-parse <base> origin/<base>`, `git push --force-with-lease` |
| medium | 1 | open | [For a quick question about debugging a hung Metabot chat, the lead spawned an open-ended code-research subagent, then ended its turn with "Full ideas list coming once the agent reports" although it already had the answer, and the user waited 16 minutes before stopping it.](andrei.claude.unbounded-research-subagent-delays-answer.md) | Agent tool (background general-purpose subagent), TaskStop, turn structure |
