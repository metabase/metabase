.evals_branch pins the metabase/evals branch that eval-run dispatches check out. The label trigger reads the pin
from the PR's base branch; a manual dispatch can override it with the evals_branch input. See
.github/workflows/evals-dispatch.yml for details.
