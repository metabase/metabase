# Agent A — matrix runner

Session: `metabase-sqlite-semantic-search-8a`

Entries up to 11:00 were backfilled at 11:00; the times on them are approximate.

## 2026-09-23 09:35 — Orientation + decisions with Voytek (backfilled)
- **What**: Read the A brief, shared context, plan and contracts. Checked the live stack. Metabase on :3002 and nREPL on :50605 are the same JVM (pid 35969). pgvector is up, and the `harness` DB did not exist yet.
- **Why/Decisions**: D owns the schema and writer, and A calls D's writer. The smoke scenarios stay small and unlabelled. We use one dedicated non-admin user, not `create-test-environment!`, to keep the corpus unchanged. Using the live JVM is fine, but the end result must be self-contained, or explicit about its preconditions.
- **How**: Proposed the writer signatures to D, who accepted them and added `record-scenarios!`.

## 2026-09-23 09:50 — Clojure in-process adapter (DEAD END, deleted)
- **What**: Wrote `dev/src/dev/harness/adapter.clj`, with the `SearchAdapter` protocol plus in-process and HTTP adapters. Deleted it at ~10:20, and unloaded it from the JVM.
- **Why deleted**: Voytek switched A to "TypeScript, HTTP-only, outside-in", matching the team-wide direction. The stub engine (which would have been a Clojure multimethod inside Metabase) was dropped for the same reason. appdb, in-place and semantic already make three real columns, so the stub's purpose is covered.
- **Findings worth keeping** (they still apply to anyone measuring in-process):
  - The brief said to call `search.engine/results`. Voytek chose `search.impl/search` instead: raw engine results skip permission filtering and the in-memory ranking (`impl.clj:512-539`), so they would violate fairness rule 4.
  - **Silent mislabelling, twice.** (1) `parse-engine` (`impl.clj:242`) falls back to the default engine when the requested one is unknown or unsupported. (2) The semantic engine reads the current user from a *dynamic binding at query time*. Without it, the permission filter dropped all 19 raw hits, and `semantic_search/core.clj:112` "supplemented" with appdb, so appdb rows were reported as semantic. The TS adapter guards against (1) by checking `body.engine` on every response. (2) can't happen over HTTP.
  - The baseline itself mixes in appdb rows at zero semantic distance when it has fewer results than `semantic-search-min-results-threshold`. That's legitimate baseline behaviour, but the "semantic" column is not purely vector.
- **How verified (before deletion)**: in-process == HTTP ranked ids, 9/9 (3 engines × 3 queries).

## 2026-09-23 10:00 — dev.search-perf does not compile (reported to B)
- **What**: `dev/src/dev/search_perf.clj:1008` references `search.ingestion/spec-index-query`, which no longer exists. So `create-test-environment!` and `search-context-for-user` are unusable on this branch.
- **How**: `(require 'dev.search-perf :reload)` in the live REPL. Messaged B. Not fixed, because it isn't in A's tree.

## 2026-09-23 10:20 — Contracts §2, §3, §5 amended
- **What**: `01-contracts.md`. §2: the adapter is a TS interface, HTTP-only; `score` is the sum of scorer contributions; stage timings and `raw_count` are NULL. §3: scenarios are JSON in camelCase. §5: each rule is marked verified or declared from outside. Told B, C and D.
- **Why**: Following the outside-in direction, several facts are not observable over HTTP. `additional-search-engines` is `:visibility :internal`, and the API refuses to write it (`setting.clj:1544`). The 0.7 cutoff is a code constant. The results limit comes from an env var. The appdb index is in H2, locked by the running instance. `ee-embedding-*` properties are admin-only.

## 2026-09-23 10:45 — TS runner package: config, adapter, setup
- **What**: `runner/package.json`, `tsconfig.json`, `runner.config.json`, `src/config.ts`, `src/adapter.ts`, `src/setup.ts`.
  - `adapter.ts`: `MetabaseSession` (lazy login) + `httpAdapter(engine, userSession, adminSession)`. It rejects responses whose `engine` differs from the one requested. The admin session is used only for provenance: the embedder, since `ee-embedding-*` is admin-only.
  - `setup.ts`: idempotent. Creates or repairs the non-admin harness user through the admin API. Does nothing when a corpus manifest is given (B's `apply.ts` creates that user).
  - `config.ts`: `runner.config.json` plus flags. A B manifest overrides baseUrl, corpusId and the user.
- **Decisions (Voytek)**: engines default to semantic, appdb and in-place. Thin runner plus a separate `pipeline.ts`, which **boots its own clean instance** (B says golden needs a clean app DB). The manifest user wins, and setup is the fallback. Smoke rows go into the shared `harness` DB under corpusId `runner-smoke`.
- **How verified**: `tsc --noEmit` is clean. Ran `setup` twice against :3002: it created `harness@metabase.local` (id 2) the first time and was a no-op the second. Adapter smoke test as that user: confirmed non-superuser; all 3 engines ready; every query is deterministic when repeated; scores are in descending order; an unknown engine gives a clear HTTP 400; `describe()` reports `ollama/all-minilm`, 384.
- **Not verified yet**: no preflight, no matrix run, no DB writes.
- **Side effect on :3002**: user `harness@metabase.local` / `harness-runner-1` now exists there.
- **Observation (not tuned)**: for the typo query `custmer revenu`, in-place returns 9 results and semantic/appdb return 0.

## 2026-09-23 11:00 — Status
- **Done**: adapter, setup, config.
- **Next**: `preflight.ts` (the §5 checks, pgvector corpus probe), `run.ts` (matrix, warmup, timing, D's writer, C's `toMetricRows`, recording E's `embeddingText` + guard), `smoke-scenarios.json` in `runner/` (kept out of `scenarios/` so B's glob stays clean). Then `pipeline.ts`: boot a clean instance → warehouse.sql → B's apply.ts → wait for indexing → resolve.ts → preflight → run (+ switching E's variants).
- **Open**: D's shared npm workspace proposal is accepted; A edits `runner/` itself. `smoke-scenarios.json` does not exist yet.

## 2026-09-23 11:10 — Joined D's shared npm workspace
- **What**: `runner/tsconfig.json` now extends `../tsconfig.base.json`. Dropped `runner/package.json` devDependencies, which moved to the root; `pg` stays.
- **How**: `tsc --noEmit` is clean. It still resolves through the old `runner/node_modules`.
- **Not done**: the root `npm install`. D asked us to wait for C. `runner/node_modules` and `runner/package-lock.json` get removed after D's root install. *(Superseded by 11:20: done.)*

## 2026-09-23 11:20 — Workspace migration finished
- **What**: After D's root install, deleted `runner/node_modules` and `runner/package-lock.json` and re-ran `npm install` at `hackathon/harness/`.
- **How verified**: `pg` resolves from the root `node_modules`, `tsc --noEmit` in `runner/` is clean, and `node src/setup.ts` still runs (no-op: the user already exists).
- **Blockers**: none. Next are `preflight.ts`, `run.ts` and `smoke-scenarios.json`.

## 2026-09-23 14:55 — preflight.ts, run.ts, first smoke run
- **What**: `runner/src/preflight.ts`, `runner/src/run.ts`, `runner/smoke-scenarios.json` (6 unlabelled queries; kept out of `scenarios/` so B's glob stays clean).
  - preflight *verifies*: the query user is non-superuser; every engine is served by itself (with a hint to set MB_ADDITIONAL_SEARCH_ENGINES); the embedder (admin session); corpus sets via probes (pgvector: the active table from `index_metadata` ⋈ `index_control.active_id`; or an ids-file). Every probed engine must match every other, and must contain every manifest entity (extras are recorded as built-ins). It *declares*: rule 3, and the unprobed engines (appdb, in-place). It records E's `embeddingText` (a 204/empty response means "baseline").
  - run: preflight gate (`--force` → `publishable:false` in notes). Per scenario, each engine gets its warmups; timed iterations then **round-robin across engines**, so drift hits every column equally. One `recordQueryResults` batch per scenario. Errors are recorded, never aborting the run. Checks the embedding-text variant again at the end. Calls C's `toMetricRows` only when the scenarios carry labels, because C treats `expected: []` as "should return nothing".
- **How verified**: `tsc` is clean. Preflight on :3002: ok, `ollama/all-minilm`/384, baseline, semantic corpus 134 (it was 78 this morning; someone added content). **Negative test**: an ids-file probe with a different set → `ok:false`, exit 1, violation names both engines + example ids. `run.ts` with that config refuses, exit 1, and writes no run. **Smoke run** `20260923-145103-fd5cd1`: 3×6×5 = 90 rows, 0 errors, 5.4s. DB check: run finished, publishable, baseline; 30 rows/engine, iterations 0–4; 6 scenarios in `harness_scenario`; `returned` keeps rank order with name and score.
- **Smoke observations (unlabelled, :3002, not tuned)**: p50 appdb 16ms, semantic 22ms, in-place 45ms (appdb max 248ms, one outlier). Semantic returns 0 for both paraphrases (the 0.7 cutoff again) and 0 for the typo. in-place returns 10/10/9 on those. Only semantic returns anything for `PRODUCT_ID` (9). Cross-lingual returns 0 everywhere.
- **Not verified**: the metrics path (the smoke scenarios have no labels). That's next, with golden.

## 2026-09-23 15:00 — First golden run (:3003)
- **What**: `runner/golden.config.json` (B's manifest and resolved scenarios; pgvector probe on `mb_ss_golden`). Run `20260923-145150-29002c`: 3 engines × 56 scenarios × 5 iterations, plus 2 warmups each.
- **How verified**: preflight ok, and all 235 manifest entities are indexed. 840 observations, 0 errors, 2,676 metric rows. The `empty-04` permission-leak check returned 0 leaked items on every engine. Results are in the `harness` DB under this run_id.
- **Not verified**: I did not check C's metric values by hand.

## 2026-09-23 15:30 — End-to-end ownership, F's gap list, pipeline.ts
- **What**: Voytek made A the owner of end-to-end delivery. I asked F for the gap list and acted on it: asked D to key scenario labels per run (Voytek's decision; entity ids differ per instance) and to fix the dashboard defaults (the corpus filter was on "fixture", scale on 100); asked B to fix the corpus-gen typecheck (`apply.ts:157`) and add it to the workspace.
- **Declared settings**: the results limit and the min-results threshold are `:visibility :internal`, absent even from the admin `/api/setting` list. Run notes now record the code defaults as declared: threshold 100, limit 1000, cutoff 0.7. **Finding**: with the threshold at 100, the semantic engine backfills with appdb whenever fewer than 100 results remain, i.e. on almost every golden query. The "semantic" column is mostly semantic+appdb.
- **pipeline.ts**: one invocation = one fresh instance (own H2 dir `local/pipeline/<name>/`, own pgvector DB `mb_pl_<name>`, own warehouse DB, default port 3010), headless admin via a config file, `clojure -M:run:drivers:ee`. Then B's `apply.ts`/`resolve.ts` with `--out` into the instance dir (B's :3003 files untouched), a wait on `/api/ee/semantic-search/status`, and per embedding-text variant: PUT the setting + `POST /api/search/re-init` + wait, then `runMatrix`. Scale corpora reuse the golden queries, unlabelled. It stops the JVM at the end unless `--keep`.
- **How verified**: `tsc` is clean. The precondition check refuses without MB_PREMIUM_EMBEDDING_TOKEN. **Not yet run end to end**: that needs the token exported in the calling shell. The pipeline deliberately doesn't fetch it itself.
- **Open**: Libor/Paolo branches (F escalated). The D and B requests above are pending.

## 2026-09-23 15:45 — pipeline: staging license env, 1Password opt-in, --pure-vector
- **Why**: Voytek pointed at the Notion page "How to try out Enterprise features locally". Dev tokens are *staging* tokens, which need `METASTORE_DEV_SERVER_URL=https://token-check.staging.metabase.com` and `MB_RUN_MODE=dev`. `local/semantic-search.env` sets the URL, but pipeline.ts didn't read that file, so a fresh instance would have validated against production.
- **What**: `pipeline.ts` sets both env vars. New opt-in `--token-from-1password` reads the team dev token with `op read` (the same source as the local scripts). The value goes only into the Metabase child's env: never printed, never written. New `--pure-vector` (Voytek: yes): boots with `MB_SEMANTIC_SEARCH_MIN_RESULTS_THRESHOLD=0`, a separate instance dir `<name>_pure`, and records the engine as `semantic-pure`, with the threshold 0 noted as "env at boot". `config.ts` gained `engineLabels` and a `semanticMinResultsThreshold` override; `run.ts` uses both.
- **How verified**: `tsc` is clean. With no token and no flag, it refuses with a clear message. **Still not run end to end.**

## 2026-09-23 15:20 — recordScenarios fix, token file, dashboard defaults to golden
- **recordScenarios**: D re-keyed `harness_scenario` by `(run_id, scenario_id)` (Voytek's decision). `run.ts` now calls `recordScenarios(runId, scenarios)` after `startRun`, and passes `embedder` to `startRun`. Verified: smoke run `20260923-151041-9b1d7d` has its 6 scenario rows under its own run_id, and `harness_run.embedder` = `ollama/all-minilm`.
- **License token**: 1Password needs an interactive sign-in (`metabaseinc` account not signed in here). Voytek put the token in `local/.mb-license-token` (gitignored via `/local`, `.gitignore:71`; mode 600). `pipeline.ts` source order: env var → that file → `--token-from-1password` (now passes `--account metabaseinc`, overridable with `OP_ACCOUNT`). I checked the file exists and is non-empty; I did not read it.
- **Dashboard defaults** (F ruled they're A's): in `results/src/dashboard-cards.ts`, corpus → `northwind-golden-v1`, scale default removed (golden runs have NULL scale), scenario → `concept-01`; also the stale "fixture" comment and the unused `CORPUS_ID` import. Ran `npm run dashboard` (rebuilt `localhost:3002/dashboard/12`), then `npm run serdes -- export`.
- **How verified**: `npm run check`: 0 errors, 22/24 cards return rows with the defaults. The 2 empty cards are the embedding-text Δ cards, which stay empty until a non-baseline run exists.
- **In flight**: golden `pipeline.ts` run on :3010 (started 15:13).

## 2026-09-23 15:50 — Pipeline works end to end; BL-01/02/03/05 done
- **Pipeline boot failures, fixed**: (1) the token file first held extra text, so config-file bootstrap failed with "requires a Premium token with :config-text-file". Voytek fixed the file (64 hex; I checked shape only). (2) **Deadlock in pipeline.ts**: the JVM's stdout was piped through Node while `apply.ts` ran under `execFileSync`, which blocks the event loop. The pipe filled after ~75 card POSTs, and every request thread blocked on log4j's `OutputStreamManager` lock. Confirmed with `jstack`: the holder was stuck in `FileOutputStream.writeBytes` via `ConsoleAppender`. Fix: the JVM writes its log straight to a file descriptor. Also added SIGTERM handling (F item 15).
- **End-to-end pipeline run**: `node src/pipeline.ts --corpus golden --port 3010` on a fresh instance: boot ~1 min, apply, index 250/250 in ~1 min, resolve, then run `20260923-153516-5394c0`: 840 observations, 0 errors, 2,676 metric rows, JVM stopped (port free afterwards). About 2m40s total.
- **BL-01** (Voytek chose (b)): `sql/02-views.sql` has new `harness_valid_run` and `harness_latest_engine_run`; observations and scenario metrics join on (run_id, engine); agreement stays within a run. Applied twice in a transaction, clean. The golden engines now come from the pipeline run.
- **BL-02**: a run that fails the variant check is left unfinished. **BL-03**: `harness_valid_run` excludes `publishable = false`. **BL-05**: `semantic-pure` is in `ENGINES`; fixtures got their own `FIXTURE_ENGINES`.
- **How verified**: `npm run typecheck` (root) is clean. `npm run check`: 0 errors, 2 empty (the variant Δ cards, as before). Dashboard rebuilt and serdes-exported.
- **Mistake**: I ran `node src/fixtures.ts --help` to see its usage. It has no `--help` flag, so it regenerated the fixtures: it deletes every `corpus_id='fixture'` run and rewrites 10. The generator is seeded and uses the same engines, so the fake data is equivalent, but **the fixture run_ids changed** (now `20260923-15374[78]-*`). Real runs are untouched (2 golden, 2 smoke).
- **Not verified**: `--pure-vector` has not run yet, so the semantic-pure column is untested with real data.

## 2026-09-23 16:00 — BL-01/02/03/05 verified by F
- F verified and accepted all four; statuses set to done in BACKLOG.md. Next: batch 2 (BL-11, then BL-13), each synced with F first.

## 2026-09-23 16:10 — BL-11: embedding dimensions follow the model (review)
- **What**: `runner/src/pipeline.ts`. New `embeddingDimensions(model)` reads `<arch>.embedding_length` from Ollama `POST /api/show`. `--embed-dims` no longer defaults to 384: it's derived, and an explicit value that disagrees fails fast. Clear failures when Ollama is down or the model isn't pulled. Synced with F before starting.
- **How verified**: `tsc` is clean. `embeddingDimensions`: all-minilm → 384, snowflake-arctic-embed2 → 1024. `--embed-model snowflake-arctic-embed2 --embed-dims 384` → "does not match … produces 1024-dimensional embeddings". `--embed-model nope-model` → "not available … run: ollama pull nope-model". Both fail in preconditions: no instance dir or DB is created.
- **Not verified**: an actual arctic boot. That stays with BL-12.

## 2026-09-23 16:20 — Pure-vector run; BL-13 gate + timing; items released
- **Pure-vector run** (Voytek's decision): `pipeline.ts --corpus golden --port 3011 --pure-vector` → run `20260923-154059-044bd9`: 840 obs, 0 errors, 2,666 metric rows, JVM stopped. Notes record threshold 0 ("env at boot") and the label semantic→semantic-pure. `harness_latest_engine_run` now shows `semantic` (from 5394c0) and `semantic-pure` (from 044bd9) side by side.
- **Released to H/backlog**: BL-14, 15, 18, 19, 20 (F suggested it; A is the bottleneck).
- **BL-13 prep** (approved by F, with two additions): `variantContentGate` in pipeline.ts counts, in the *active* index, docs with a collection whose content has E's `collection: ` line. It requires ≥ 90% for non-baseline variants and exactly 0 for baseline. It runs before every run. Re-init-to-indexed time is recorded in notes as `reinitToIndexedMs`, next to `embeddingTextCheck` (new `extraNotes` in config/run). Tested on the existing baseline index: baseline passes (0/210), and checking it as context correctly fails.
- **In flight**: BL-13 run on :3012, `--variants baseline,context`.

## 2026-09-23 16:35 — BL-13: variant switch works end to end (review)
- **What**: `pipeline.ts --corpus golden --port 3012 --variants baseline,context` on a fresh instance. Baseline run `20260923-154430-7f2163`, then PUT variant=context + `POST /api/search/re-init` + wait → context run `20260923-154552-0f1b87`. Both 840 obs, 0 errors (2,676 / 2,672 metric rows). The JVM stopped and :3012 is free.
- **Re-init → indexed**: 45.2 s for 250 docs (all-minilm), recorded as `notes.reinitToIndexedMs = 45227`.
- **Content gate fix**: the first version passed context at exactly its 0.90 threshold (189/210). Inspecting the active index showed all 21 unmarked docs are `collection` docs, which E's variant embeds by name only, by design. The gate now excludes collections and requires ≥ 0.98 (baseline: exactly 0). Re-checked: context index 189/189 = 1.00 PASS; baseline index (pure-vector DB) 0/189 PASS; baseline index checked as context FAIL. The context run's notes record the earlier gate's numbers (0.9 over 210), which are the same data under the old denominator.
- **Keyword engines unchanged across variants** (E's sanity): appdb and in-place have identical ranked `model:id` lists in 56/56 scenarios each (same order). My first query wrongly reported 24/56 for appdb because `jsonb_agg` without `ORDER BY` rank compared arbitrary element orders.
- **Dashboard**: `npm run check` → 0 errors, **0 empty cards**. The 4 embedding-text cards now return rows (364 / 7 / 25 / 2).

## 2026-09-23 16:55 — BL-10: checklist for engine authors in contract §1 (review)
- **What**: `01-contracts.md` §1 gained a "Checklist for engine authors" (11 items), a corrected result-row table (`:score` is never shown; `:all-scores` is effectively required), a note that scores are sums of contributions (no 0..1), the §4 `returned` example corrected, and the "Rules" line fixed (the limit is an internal setting, not a constant).
- **Verified against code before writing**: `known-engines` = keys of `supported-engine?` methods (engine.clj:121); `search/init.clj` requires each engine ns; `check-permissions-for-model :default`/`:document` return true unless archived (impl.clj:60-71). **Engines must filter collection permissions themselves** (the most important item). Ranking uses `:score` via the default `search.engine/score`, while `serialize` shows only `:scores` from `:all-scores`. Stored golden data confirms: in-place scores are all 0.00, appdb 6.8–108, semantic 7.2–119.
- **Not verified**: item 4's exact field list per model (derived from reading, not from an engine that lacked them).

## 2026-09-23 17:00 — BL-12: first arctic golden run (review); BL-10 verified
- **What**: `pipeline.ts --corpus golden --port 3013 --embed-model snowflake-arctic-embed2` (dims derived → 1024, BL-11). Run `20260923-155004-12e37a`: 840 obs, 0 errors, 2,704 metric rows. Embedding-text gate baseline 0/189. JVM stopped; :3013 free.
- **How verified**: `harness_run.embedder = ollama/snowflake-arctic-embed2`, max `dimensions` = 1024, finished, publishable. The pgvector `index_metadata` shows `index_ollama_snowflake_arctic_2_1024_…` with 1024 dims. `harness_latest_engine_run` has both embedders for golden/baseline. `npm run check` gives 0 errors, 0 empty; "Where quality variance comes from" rows=4 and "Swap the embedder vs swap the engine" rows=4.
- **Note**: 250 docs indexed in ~10 s with arctic (B measured 81 docs/s, consistent).
- **BL-10**: verified by F, marked done; F is relaying §1 to Voytek for Libor/Paolo.

## 2026-09-23 16:20 — Scale tiers, BL-04, BL-19 checks
- **Scale tiers** (F's order #1): `pipeline.ts --corpus scale-1000 --port 3014` then `scale-10000 --port 3015`. The first attempt failed on my bug (`scenarios/src/golden.json` is `{scenarios: [...]}`, not an array); fixed, and `corpusFile` added so `corpusHash` is filled. Runs `20260923-155756-94d8c0` (1k, 2,656 docs) and `20260923-160501-03ff76` (10k, ~15k docs with a collection): 780 obs each, 0 errors, unlabelled (latency only). H's BL-18 notes are all filled on both.
- **Latency p50/p95 ms** (golden ~250 docs / 1k / 10k): appdb 14/22, 15/23, 94/275; in-place 37/58, 47/106, 207/549; semantic 34/50, 50/159, **454/1,422**. The instances use the brute-force (exact) vector strategy, so semantic grows linearly with the index.
- **Dashboard gap (G's card)**: the latency-vs-size cards are scoped to one corpus, and each scale tier has its own corpus_id, so they can't draw a size curve (with scale-10000 selected they show only that tier). Reported to G/F.
- **BL-04**: fixture runs deleted in one transaction after 10k finished (the harness DB shares the container with the pgvector store being measured). check: 0 errors / 0 empty. Verified by F.
- **BL-19 checks for H/F**: (b) throwaway `--engines appdb --iterations 1` on :3016 → `notes.instance = golden_all_minilm_1612037c0e`, "dropped mb_pl_…/wh_pl_…", 0 matching DBs afterwards. I deleted that throwaway run `20260923-161326-dd7047` (my test data) so it wouldn't become the latest golden appdb run. (a) SIGTERM mid-run (scenario 8/56): node exit 143, "stopping Metabase", JVM gone and :3016 free within 10 s, DBs `mb_pl_/wh_pl_golden_all_minilm_16135106f6` kept (no "failed: kept" line, as H predicted). The interrupted run `20260923-161515-552b1e` has finished_at NULL, so it's excluded from the views.

## 2026-09-23 16:25 — Arctic context run (F's item 2)
- **What**: `pipeline.ts --corpus golden --port 3017 --embed-model snowflake-arctic-embed2 --variants context` → run `20260923-161915-402da0`: 840 obs, 0 errors, 2,950 metric rows. Re-init to indexed 50.3 s; gate context 189/189 = 1.00. BL-19 auto-drop worked (instance DBs gone; :3017 free).
- **How verified**: harness_run has embedder arctic, embedding_text context, finished, publishable. `harness_latest_engine_run` for golden now covers minilm×{baseline, context}, arctic×{baseline, context} and semantic-pure (minilm baseline). `npm run check`: 0 errors, 0 empty.
- **Rule noted from F**: ask F before deleting any rows from `harness`.

## 2026-09-23 16:30 — text_strategy + Agent I's pipeline flags (F's item 3; review)
- **What** (all additive):
  - `sql/01-schema.sql`: `harness_run.text_strategy text NOT NULL DEFAULT 'none'`.
  - `shared/types.ts`: `RunInfo.textStrategy`; `results/src/writer.ts` inserts it.
  - `sql/02-views.sql`: `text_strategy` beside `embedding_text` in every key/partition/select (22 lines), plus the agreement join.
  - `runner/src/config.ts` / `run.ts`: `textStrategy` → startRun.
  - `runner/src/pipeline.ts`: `--corpus golden|sql|scale-N` (sql: artifacts/sql from Agent I, labels scenarios/src/sql.json; labelled corpora now resolve from `scenarios/src/<corpus>.json`), `--corpus-file` (applied and hashed), `--text-strategy` (default none). The sql, corpus-file and labels presence checks run before anything is created.
- **How verified**: schema + views applied twice in a transaction, clean; the 12 existing runs defaulted to 'none'. Root typecheck clean. `npm run check`: 0 errors, 0 empty. Failure paths (`--corpus sql` without artifacts, a missing `--corpus-file`, `--corpus bogus`) fail with clear messages and create no instance dir (8 before/after). Positive test into a **scratch results DB** (`harness_scratch`, created for this and dropped after, so no shared rows written or deleted): golden, appdb, 1 iteration, `--corpus-file` = a copy of golden corpus.json, `--text-strategy smoke-test` → run `20260923-162737-133143` with text_strategy=smoke-test, corpusHash = the copy's sha256 (2f6f8d1c…), and the path recorded; BL-19 dropped the instance DBs. Shared `harness` has 0 runs with text_strategy ≠ 'none'.
- **Open**: G's cards don't filter or pair on `text_strategy` yet (told G). Until they do, a corpus with several strategies would show mixed rows.

## 2026-09-23 16:40 — SQL-corpus baselines (I's northwind-sql-v1), minilm + arctic
- **What**: `pipeline.ts --corpus sql --port 3018 --keep` (minilm) → run `20260923-163103-6ee999`; kept for I's MCP harvest (40/40 GUI cards, I confirmed), then stopped and its 2 instance DBs dropped (count 0 after). Then `--corpus sql --port 3019 --embed-model snowflake-arctic-embed2` → run `20260923-163607-383d40` (DBs auto-dropped). Both 900 obs, 0 errors (2,505 / 2,529 metric rows). The runs were sequential so they didn't compete for Ollama, and I's qwen batch was held until both finished.
- **First live test of I's paths**: apply.ts "Applied 145 entities for northwind-sql-v1 in ~4.5s", no card errors (64 native, 28 MBQL); resolve.ts resolved 60/60. Gate baseline 0/121.
- **Headline (n=60)**: nDCG@10 semantic 0.359 (minilm) / 0.402 (arctic), in-place 0.337, appdb 0.250. recall@10 semantic 0.413 / 0.485, in-place 0.385, appdb 0.228. Zero-result: appdb 0.717, in-place 0.05, semantic 0.05 / 0.017. Keyword engines identical across embedders (as expected).
- **Dashboard** (corpus=northwind-sql-v1): 0 errors, 8 empty, all explained: 3 embedding-text cards (no context run on sql), 3 drill-down (default scenario concept-01 is a golden id), permission leaks and false-positive rate (sql.json has 0 empty-expected and 0 expectedAbsent scenarios).

## 2026-09-23 16:45 — BL-08: `pipeline.ts --repo <path>` (review)
- **What**: `--repo` (default: this worktree) is the cwd for `clojure -M:run:drivers:ee`; preconditions require a `deps.edn`. `RunnerConfig.repo`; run.ts provenance (`repo`, `gitDescribe --dirty`, `git_sha`, `branch`) now uses `git -C <repo>`, and `harnessRepo` / `harnessGitDescribe` are recorded separately. F's addition: `embeddingTextSupported` (does `/api/setting` list `search-embedding-text-variant`) is recorded on every run, and the pipeline **refuses** non-baseline `--variants` when it's false (a branch without E's change would otherwise silently measure baseline). The harness code, corpus-gen, token file and instance dirs stay in this worktree.
- **How verified**: tsc clean. `--repo /nope` and `--repo /tmp` fail with "not a Metabase checkout (no deps.edn)", with no instance dir created (11/11). Positive test into a scratch results DB `harness_scratch` (created, then dropped; no shared rows): `--repo <this worktree>` golden/appdb/1 iteration → run `20260923-164147-06bc61`: git_sha 188c8f412f7, branch sqlite-semantic-search, notes.repo = worktree path, gitDescribe `…-g188c8f412f7-dirty`, harnessRepo = same path, embeddingTextSupported true; instance DBs dropped. I was told about the Ollama use beforehand.
- **Not verified**: the variant refusal on a real repo without E's setting (needs such a checkout, e.g. Libor's branch). It's covered by code only.

## 2026-09-23 16:55 — SQL queue running; sqlite-vec1 plan agreed with J
- **Queue** (scratchpad `sql-queue.sh`, sequential, stops on first failure, port 3021): controls (context-sql,context × minilm, arctic), then I's 8 strategy runs, then 2 pure-vector sql baselines. minilm controls done: context-sql `20260923-164504-a41556`, context `20260923-164625-8dcef1` (gate 121/121, re-init ~45 s each).
- **J's vec1 native build** (16:47:53–16:48:03 UTC) overlapped only the arctic control's apply and initial indexing (16:47:58–16:48:12). Its timed queries started 16:49:08, so no latency was affected.
- **sqlite-vec1 design agreed with J** (branches.md "plan v2, agreed with A"): `--sqlite-vec1` (per-instance `semantic.sqlite`, max distance 0.7, `--sqlite-max-distance` override → label suffix `@<d>`), labels sqlite-vec1 / sqlite-vec1-pure, no pgvector DB, the gate recorded n/a. BL-09 readiness = docs = vectors, stable for 2 polls, then preflight's `sqlite` corpus probe requires every manifest entity (catches silently skipped embed batches). J proved the count query on a real vec1 store (incl. doc-without-vector → 51|50). Implementation waits until the queue ends, since each queue step loads runner/src fresh.

## 2026-09-23 17:25 — SQL queue done; BL-37 queue.ts; sqlite-vec1 wired (BL-09/34); I's kept run
- **SQL queue done**: 14 runs, 0 failures (4 controls, 8 strategies, 2 pure-vector sql baselines `20260923-171125-4bb0f6` / `20260923-171321-e25cf2`, threshold 0). Build-overlap note added to the 2 arctic control runs (additive notes merge, requested by F/J).
- **BL-37 `runner/src/queue.ts`** (new; F approved the plan plus hold max-duration): jobs.jsonl / events.jsonl / logs / paused flag; kinds quality (N slots, default 2) / latency (exclusive) / hold (exclusive no-op; release or auto-release after --max-min, default 20, with a warning); strict FIFO with no overtaking of exclusive jobs; the port pool 3021–3040 skips listening ports; adopt/orphan on restart; `HARNESS_QUEUE_DIR` override for tests. Job windows are mirrored fail-soft into the new table `harness.harness_job` (01-schema.sql, additive; applied twice clean), for G's concurrency view. **Dry-run verified**: orphan detection, 2-slot concurrency with q3 waiting, latency exclusive, no overtaking, hold auto-release after 6 s with a warning, pause blocks starts, resume, release. Not yet verified: a real 2-job batch and the indexing-slowdown measurement.
- **sqlite-vec1 wired** (runner/src/sqlite.ts + pipeline/run/preflight/config, ENGINES + sqlite-vec1-pure; fixtures exclude both -pure): `--sqlite-vec1`, `--sqlite-max-distance` (label @d when ≠ 0.7); repo guard (sqlite.clj + vec1.dylib, sha256 recorded); no pgvector DB or URL; boot proof "Opened SQLite semantic search store at <path>" polled ≤ 2 min; **BL-09 readiness** = docs = vectors + manifest ⊆ search_doc, stable 2 polls (pinned /usr/bin/sqlite3 3.51); store meta embedder check; `sqlite` corpus probe; notes store / sqliteMaxDistance / vec1DylibSha256 / sqlite3 / storeMeta / readiness / engineSha (all runs). **BL-34 guard on every run**: semantic responses checked (fallback = row without semantic-distance, for all; vector-only prefix/suffix + pure rule for sqlite only, per J's catch that pgvector is hybrid) plus a metabase.log scan; any hit leaves the run unfinished; the verdict goes in notes.fallbackGuard.
- **Live evidence**: (1) `--sqlite-vec1` on this worktree refused (no sqlite.clj). (2) Libor's branch booted with `--variants context` → store opened "(created)", readiness passed (250 = 250), then **the variant refusal fired live** ("needs the search-embedding-text-variant setting, which …/sqlite-vec1-libor does not have"); JVM stopped; the leftover warehouse DB dropped. (3) sqlite smoke into scratch DB `harness_scratch` (created, then dropped): run `20260923-172107-9ace74`, 168 obs, 0 errors; readiness 250 docs = vectors, all 235 manifest entities, 15 built-ins; guard vector-only 56/0/0; label sqlite-vec1; engineSha 571a489e13814e5599258a4ed74cdda5bacd98b9; dylib 44d210b33f39…; empty-04 leaks 0; only 16/56 scenarios have identical top-10 vs pgvector semantic (the store is really different); no hs_err logs; sqlite-vec1 min score 7.3 (non-zero). (4) The pgvector guard is live on I's kept run `20260923-172027-626b30`: 300 responses, 0 violations, 0 log errors.
- **I's kept context-sql minilm instance** on :3025 (`sql_all_minilm_1717563f59`), handed to I.

## 2026-09-23 17:40 — sqlite-vec1 run 1 (via queue.ts); plugins race; accidental run marked unpublishable
- **Accidental run** `20260923-172555-82ecd1` (my validation loop included the valid value 0.8, so a real appdb-only run on Libor's branch landed in harness): per F, NOT deleted. notes.publishable=false plus excludedReason. harness_valid_run excludes it, so golden appdb falls back to 7f2163; check 0/0.
- **J's review fixes**: `--sqlite-max-distance` validated (finite, 0 < d ≤ 2; "abc", "", "0", "3" all fail); fails without --sqlite-vec1; --sqlite-vec1 refuses non-baseline --variants before boot; the unfinished-run message no longer blames only preflight.
- **Run 1, first attempt FAILED at boot** (jobs 172746-c1ea / 172746-2812): two JVMs booting at once from the same --repo raced on `<cwd>/plugins` (instance-analytics extraction): "Collection … was not found" / "…/usage_analytics". Not a guard; no run rows. **Fix**: `MB_PLUGINS_DIR=<instance dir>/plugins` per instance (plugins/impl.clj:21). The failed instances' warehouse DBs were dropped.
- **Run 1 retry OK, via the queue (first real 2-slot batch)**: as-is `20260923-173209-5c468e` (sqlite-vec1), pure `20260923-173138-6fd341` (sqlite-vec1-pure). Both finished, publishable, 250 docs (15 built-ins), engineSha 571a489e…, max distance 0.7; guard vector-only 280 responses / 0 violations / 0 log lines each (pure: every row semantic-distance > 0). empty-04 leaks 0. The same-repo concurrent boot worked, so the plugins fix is verified.
- **Golden × minilm, n = 56**: nDCG@10 semantic 0.601 / semantic-pure 0.597 / sqlite-vec1 0.581 / sqlite-vec1-pure 0.577; recall@10 0.595 / 0.589 / 0.597 / 0.590; MRR 0.728 / 0.723 / 0.676 / 0.672; zero-result 0.058 all. sqlite-vec1 matches pgvector recall and ranks lower (MRR). p50 28.6 / 18.7 ms for sqlite, but measured concurrently (the overlap view excludes it).
- **Concurrency, indicative only**: apply+index solo 21 s vs 21 / 20 s concurrent (no slowdown); boot 69 s solo vs 90 / 60 s. F's real measurement is on the next pgvector pair.
- **Queue windows for H**: BL-35 (--vector-only), then BL-33 (--query-prefix), each with the queue paused.

## 2026-09-23 17:50 — sqlite-vec1 scale tiers (latency, exclusive); job_id on runs; daemon restart
- **Scale tiers via the queue as exclusive latency jobs** (minilm, 0.7, as-is): 1k `20260923-173628-4350df` (2,656 docs), 10k `20260923-174357-3004e3` (26,416 docs); guard vector-only 260/0/0 on both.
- **p50/p95 ms, semantic column**: 1k pgvector 49.5/159.3 vs sqlite-vec1 35.9/130.1; 10k pgvector 454.0/1,421.6 vs **sqlite-vec1 222.3/1,083.9** (avg results equal: 7.8 at 1k, 8.6 at 10k). Caveat: at 10k the unchanged engines were also faster than in the pgvector 10k run (appdb 94.3→74.7, in-place 206.7→183.1, i.e. 0.79×/0.89×), so the machine state differed; sqlite's 0.49× is well beyond that. At 1k appdb/in-place were identical across runs.
- **job_id**: `harness_run.job_id` (additive; schema applied twice), stamped at startRun from HARNESS_JOB_ID, which queue.ts passes to each child. This fixes G's BL-38 own-job self-overlap. Queue paused during the edit (only the exclusive scale-10000 job was running), daemon restarted with the new code after it ended (no jobs to adopt), resumed. H's BL-28 and BL-29 edits landed in the same window.
- **Queued latency re-times** (F): sqlite golden as-is + pure, exclusive, because run 1's pure latency was contaminated by the as-is job's apply/embedding (J's finding).

## 2026-09-23 18:00 — CORRECTION: clean 10k pair; sqlite-vec1 is ~24% faster at p50, not 2×
- **pgvector scale-10000 re-time** (exclusive latency job, inserted right after H's BL-33 pair at F's request): run `20260923-175722-e82939`, guard fallback 260/0/0, not overlapped. **semantic 292.1/865.7 ms** (p50/p95), much faster than the original 16:05 run (454.0/1,421.6); the machine was busier then.
- **Fair pair at 10k** (both exclusive, not overlapped): pgvector e82939 vs sqlite 3004e3. Unchanged engines (sqlite run / pgvector run): appdb ×1.11/×1.39, in-place ×1.12/×1.04 (p50/p95), so the sqlite run's machine was ~11% slower at p50, and p95 is noisy. Semantic column: **sqlite-vec1 222.3/1,083.9 vs pgvector 292.1/865.7 → p50 ×0.76 (≈×0.68 adjusted), p95 ×1.25** (tail worse; inconclusive given the noise). My 17:50 "2× faster" claim came from comparing against the contaminated 16:05 pgvector run: wrong, superseded here.
- **Index strategy at 10k**: both exact. pgvector brute-force (no HNSW; indexSize.includes = vectors + tsvector+gin: the hybrid keyword arm, 117 MB table+indexes). sqlite-vec1 flat and exhaustive (sqlite.clj:167 "Flat nearest-neighbour index … exhaustive"), store 156 MB (file + wal). The pgvector semantic column is hybrid (a keyword arm per query), so part of the gap is hybrid overhead; store-vs-store needs pgvector --vector-only (BL-35) at 10k.

## 2026-09-23 19:15 — Store-vs-store at 10k (A-B-A), network outages, lucene
- **Network outages**: two drops made fresh boots fail (18:04 token check "Network is unreachable"; 18:38 token-check timeout + DNS; 19:02 lucene first boot "UnknownHostException repo1.maven.org"). No bad rows. Fixes: a boot retry in pipeline.ts (token-check failures only, 60 s, max 3, notes.bootAttempts; F-approved; not yet fired live); lucene deps pre-fetched (`clojure -P` in lucene-paolo). The failed boots' leftover DBs were dropped.
- **Queue ops**: F reordered twice (I's 6 quality jobs ahead of latency, OK: 8 runs; then lucene first). Done by pause → atomic rewrite of pending order → resume; running jobs were never touched. B's stats-real hold slotted per F.
- **Store-vs-store, scale-10000, minilm, vector-only on both sides, all exclusive, none overlapped, guards 260/0**: pgvector --vector-only A1 `20260923-182327-7d0d23` 305.6/1,014.8; A3 `…184533-8c5914` 277.7/655.1; **B sqlite-vec1 pure `…185610-54a159` 197.8/920.6**; A4 `…190845-9f9e5b` 267.5/1,014.1 (p50/p95 ms; avg results 8.63 in all). A3↔A4 p50 differ 3.7% (< 10%, stable); A1 drifted 12% earlier. **sqlite p50 = ×0.73 of the adjacent pgvector mean (272.6)**; unchanged engines in B vs the A3/A4 mean: appdb ×0.89, in-place ×1.01. p95 inconclusive (pgvector's own p95 varies 655 ↔ 1,014). Both exact (pgvector brute-force, sqlite flat).
- **--lucene wired** (J's plan + review; ENGINES + lucene-pure; BL-42 live vector arm recorded on all runs, required ≥ 0.95 for lucene incl. null; lucene fails on "Keyword arm of semantic search failed"; notes.appDb "h2" on all runs; embed-batch failures counted). The smoke retry is running.

## 2026-09-23 20:45 — Lucene end to end; overlap probe; final clean latency set; orphan-JVM fix
- **Lucene**: worktree lucene-paolo @917611d56a (set up by J via HTTPS + gh; my shell's SSH key is denied). `--lucene` per J's plan + review (appDb h2 note on all runs; lucene fails on the keyword-arm-failure log line; BL-42 live arm required ≥ 0.95 incl. null; embed-batch failures counted). First smoke failed on Maven DNS, so deps are pre-fetched (`clojure -P`). Smoke `20260923-191515-da3f42` = lucene golden as-is (F): nDCG/recall/MRR 0.579/0.594/0.668. The first pure attempt hung at 249/250: J root-caused it as a lucene startup race (init! populates only an empty space, core.clj:60; early event embeds win; collection 1 "Trash" waits for the hourly repair). My /api/search/debug reading ("all present") was wrong: it reads the appdb index. **Fix**: every lucene run POSTs /api/search/re-init (init! :force-reset? true; force-reindex would be a no-op since semantic's reindex! is unsupported) → notes.forcedReindex. Pure retry `20260923-200842-0f90cf`: 250/250 in 10 s, same quality. HNSW params recorded (Lucene defaults: M 16, beamWidth 100, cosine, k = limit, score floor 0.65 = cosine distance 0.7).
- **Overlap probe** (--limit/--purpose flags; runs unpublishable): pgvector-vec `…192801-990967` vs sqlite pure `…193557-9dff53` → J: Jaccard 1.000 over 26,648 items. The candidate sets are identical; top-10 differences are ranking only.
- **waitForIndexing stall detector**: 5 min without progress → fail.
- **Orphan JVM bug, fixed**: the signal handlers were installed after boot, so SIGTERM during boot orphaned the JVM (ppid 1, kept the port). Now installed before boot, stopping the booting JVM. Verified by a SIGTERM test and live.
- **Final clean latency** (exclusive, not overlapped, guards 0): 10k hybrid p50/p95: pgvector 292.1/865.7, **lucene `…203908-4e3f15` 237.9/952.6**, sqlite 222.3/1,083.9; lucene's unchanged engines ×0.84/×1.07 vs pgvector's run, so ~15–20% faster p50; p95 noise. Index size at 10k: lucene 50 MB, pgvector 117 MB, sqlite 156 MB. Golden: pgvector 34.4/49.6, **sqlite as-is `…202630-5b5dc1` 25.6/39.6**, sqlite pure `…202833-e6756a` 18.4/35.5 (unchanged engines identical: a clean comparison).
- **Queue operations**: several reorders at F's request (demo priority, holds for J's/H's/B's windows); two latency jobs stopped during boot (no rows) when a demo JVM was still busy. The queue is empty now.
