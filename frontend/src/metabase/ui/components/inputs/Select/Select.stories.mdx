import { Canvas, Story, Meta } from "@storybook/addon-docs";
import { Select } from "metabase/ui";

export const args = {
  size: "md",
  placeholder: "No semantic type",
};

export const sampleData = {
  list: [
    { value: 1, label: "Entity key" },
    { value: 2, label: "Foreign key" },
    { value: 3, label: "Category" },
    { value: 4, label: "Description" },
  ],
  groups: [
    { value: 1, label: "Entity key", group: "Overall row" },
    { value: 2, label: "Foreign key", group: "Overall row" },
    { value: 3, label: "Category", group: "Common" },
    { value: 4, label: "Description", group: "Common" },
  ],
};

export const argTypes = {
  size: {
    options: ["xs", "md"],
    control: { type: "inline-radio" },
  },
  placeholder: {
    control: { type: "text" },
  },
};

<Meta
  title="Inputs/Select"
  component={Select}
  args={args}
  argTypes={argTypes}
/>

# Select

Our themed wrapper around [Mantine Select](https://mantine.dev/core/select/).

## Docs

- [Figma File](https://www.figma.com/file/21uCY0czmCfb6QBjRce0I8/Input-%2F-Select?type=design&node-id=1-96&mode=design&t=Mk8RP6HsneuWzHtr-0)
- [Mantine Select Docs](https://mantine.dev/core/select/)

## Examples

export const Default = args => <Select {...args} data={sampleData.list} />;

<Canvas>
  <Story name="Default">{Default}</Story>
</Canvas>

### Grouped items

export const GroupedItems = args => (
  <Select {...args} data={sampleData.groups} initiallyOpened />
);

<Canvas>
  <Story name="Grouped items">{GroupedItems}</Story>
</Canvas>
