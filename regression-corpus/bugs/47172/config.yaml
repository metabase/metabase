issue: 47172
issue_url: https://github.com/metabase/metabase/issues/47172
fix_commits: ["b75577b5292889032f2bb3fc77593efb8f4da882"]
fix_pr: 47175
fix_subject: "Fix DateTimeRange substitution for open ranges (#47175)"
area: qp/parameters/dates (was driver/common/parameters)
fe_be_boundary: false

mutation:
  kind: semantic
  patch_file: reconstruction.patch
  base_sha: "56879decb0aac2ad61b5bc5737ebd4afc56e5f81"
  applies_cleanly: true      # RECONSTRUCTION patch applies; original fix diff does NOT (conflict)
  file_relocated: true
  reconstructed_by: worktree_subagent
  reconstruction_notes: >
    Removed maybe-adjust-open-range (+ its call) in date-str->datetime-range, un-adjusting open-range boundaries. File relocated driver/common/parameters/dates.clj -> query_processor/parameters/dates.clj. Chose the dates.clj hunk (governs boundary VALUES = assertion diff) over the substitution.clj hunk (governs SQL shape = would throw = error).

validation:
  oracle:
    kind: be_unit
    test: "metabase.driver.sql.parameters.substitute-test/expand-field-filters-for-datetime-field-test (+ date-field-test)"
    clean_baseline: pass
    reconstructed_result: "4/43 fail (open-range datetime bounds off by one temporal unit)"
  repro_confirmed: true
  confidence: high

failure_shape:
  observed: true
  source: observed_unit
  symptom: >
    One-sided datetime field filters (~date / date~) emit correct single-bound SQL but with a boundary value off by one unit (start not +1, end not -1).
  repro_failure: >
    expected param 2016-07-02T00:00Z, actual 2016-07-01T00:00Z (4 assertions).
  not_setup_crash: true

coverage:
  be_unit: {status: kill, killed_by: ["metabase.driver.sql.parameters.substitute-test/expand-field-filters-for-datetime-field-test (+ date-field-test)"], clean_baseline: pass}
  fe_unit: {status: untested, killed_by: []}
  notes: "Semantic-reconstruction batch-2. Conflict target reconstructed into current code + killed by shipped BE test => e2e repro cullable."
