{
  "description": "Pivot intent. `:rows` and `:columns` are sequences of UUIDs that reference `:lib/uuid` values on breakouts of the\n  stage that carries this clause. `:show-row-totals` and `:show-column-totals` default to `true` when absent.",
  "properties": {
    "columns": {
      "items": {
        "$ref": "./metabase.lib.schema.common.uuid.json"
      },
      "type": "array"
    },
    "rows": {
      "items": {
        "$ref": "./metabase.lib.schema.common.uuid.json"
      },
      "type": "array"
    },
    "show-column-totals": {
      "type": "boolean"
    },
    "show-row-totals": {
      "type": "boolean"
    }
  },
  "required": [
    "rows",
    "columns"
  ],
  "type": "object"
}
