{
  "anyOf": [
    {},
    {
      "properties": {
        "body": {
          "properties": {
            "error_message": {
              "oneOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ]
            },
            "finished_at": {
              "oneOf": [
                {},
                {
                  "type": "null"
                }
              ]
            },
            "id": {
              "description": "value must be an integer greater than zero.",
              "minimum": 1,
              "type": "integer"
            },
            "started_at": {
              "oneOf": [
                {},
                {
                  "type": "null"
                }
              ]
            },
            "status": {
              "type": "string"
            }
          },
          "required": [
            "id",
            "status"
          ],
          "type": "object"
        },
        "status": {
          "const": 409
        }
      },
      "required": [
        "status",
        "body"
      ],
      "type": "object"
    }
  ],
  "description": "Schema for `GET /query/:id`. On success the body is a streamed dataset (api/csv/json/xlsx),\n   so we describe it as `:any`. On a not-yet-done query we return a 409 with a status payload."
}
