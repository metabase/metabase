(ns release.validate)

(defn validate-release []
  ;; notify "Prepared v$VERSION ($METABASE_REPO) release! Validating..."
  ;; PREPARE=1 validate
  ;; notify "Validation complete. Don't forget to publish the release!"
  )
