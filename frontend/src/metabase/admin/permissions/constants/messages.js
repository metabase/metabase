import { t } from "ttag";

export const UNABLE_TO_CHANGE_ADMIN_PERMISSIONS = t`Cannot change the data access for Administrators`;
export const DATA_ACCESS_IS_REQUIRED = t`Data access must be allowed`;
