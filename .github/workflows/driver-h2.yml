# H2 driver tests (EE + OSS). See driver-postgres.yml for the template rationale.
name: Driver Tests - H2

on:
  push:
    branches:
      - "master"
      - "release-**"
  pull_request:
    types: [opened, synchronize, reopened]
  merge_group:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref && github.ref || github.run_id }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write
  checks: write
  pull-requests: read

jobs:
  decide:
    uses: ./.github/workflows/_driver-decide.yml
    with:
      driver: h2
      workflow-name: ${{ github.workflow }}
    secrets: inherit

  test:
    needs: decide
    if: >-
      !cancelled() &&
      needs.decide.result == 'success' &&
      needs.decide.outputs.conductor-status != 'skip' &&
      (needs.decide.outputs.should-run == 'true' || needs.decide.outputs.quarantine-conflict == 'true')
    runs-on: ${{ vars.DEFAULT_RUNNER_KEY }}
    timeout-minutes: 40
    env:
      CI: 'true'
    name: H2
    steps:
      - uses: actions/checkout@v6
      - name: Test H2 driver
        id: test-driver
        uses: ./.github/actions/test-driver
        with:
          junit-name: 'be-tests-h2'
          test-args: >-
            :only-tags [:mb/driver-tests]
            :exclude-tags [:mb/transforms-python-test]
      - name: Upload Test Results
        uses: ./.github/actions/upload-test-results
        if: always()
        continue-on-error: ${{ needs.decide.outputs.conductor-status == 'info' }}
        with:
          input-path: ./target/junit/
          output-name: be-tests-h2
          bucket: ${{ vars.AWS_S3_TEST_RESULTS_BUCKET }}
          role-to-assume: ${{ secrets.CI_TEST_RESULTS_IAM_ROLE }}
          aws-region: ${{ vars.AWS_REGION }}
          trunk-api-token: ${{ secrets.TRUNK_API_TOKEN }}
          previous-step-outcome: ${{ steps.test-driver.outputs.test-outcome }}
      - name: Report result to conductor
        if: always()
        uses: ./.github/actions/conductor-report
        with:
          secret: ${{ secrets.CI_CONDUCTOR_WEBHOOK_SECRET }}
          status: ${{ needs.decide.outputs.conductor-status }}
          outcome: ${{ steps.test-driver.outputs.test-outcome || job.status }}

  test-oss:
    needs: decide
    if: >-
      !cancelled() &&
      needs.decide.result == 'success' &&
      needs.decide.outputs.conductor-status != 'skip' &&
      (needs.decide.outputs.should-run == 'true' || needs.decide.outputs.quarantine-conflict == 'true')
    runs-on: ${{ vars.DEFAULT_RUNNER_KEY }}
    timeout-minutes: 40
    env:
      CI: 'true'
    name: H2 (OSS)
    steps:
      - uses: actions/checkout@v6
      - name: Test H2 driver (OSS)
        id: test-driver
        uses: ./.github/actions/test-driver
        with:
          junit-name: 'be-tests-h2-oss'
          edition: 'oss'
          test-args: >-
            :only-tags [:mb/driver-tests]
            :exclude-tags [:mb/transforms-python-test]
      - name: Upload Test Results
        uses: ./.github/actions/upload-test-results
        if: always()
        continue-on-error: ${{ needs.decide.outputs.conductor-status == 'info' }}
        with:
          input-path: ./target/junit/
          output-name: be-tests-h2-oss
          bucket: ${{ vars.AWS_S3_TEST_RESULTS_BUCKET }}
          role-to-assume: ${{ secrets.CI_TEST_RESULTS_IAM_ROLE }}
          aws-region: ${{ vars.AWS_REGION }}
          trunk-api-token: ${{ secrets.TRUNK_API_TOKEN }}
          previous-step-outcome: ${{ steps.test-driver.outputs.test-outcome }}
      - name: Report result to conductor
        if: always()
        uses: ./.github/actions/conductor-report
        with:
          secret: ${{ secrets.CI_CONDUCTOR_WEBHOOK_SECRET }}
          status: ${{ needs.decide.outputs.conductor-status }}
          outcome: ${{ steps.test-driver.outputs.test-outcome || job.status }}

  result:
    name: H2 result
    needs: [decide, test, test-oss]
    if: always() && !cancelled()
    runs-on: ${{ vars.DEFAULT_RUNNER_KEY }}
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/driver-gate
        with:
          decide-result: ${{ needs.decide.result }}
          test-results: ${{ needs.test.result }} ${{ needs.test-oss.result }}
          conductor-status: ${{ needs.decide.outputs.conductor-status }}
