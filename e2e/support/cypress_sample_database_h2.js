/**
 *  This JSON file gets recreated every time Cypress starts.
 *  See: `e2e/snapshot-creators/default.cy.snap.js`
 *
 *  - It had to be added to `.gitignore`.
 *  - It contains extracted metadata (table ids and field ids) from the H2-backed copy of the
 *    sample data that lives alongside the canonical SQLite Sample Database in the default snapshot.
 */

//
export { default as H2_SAMPLE_DATABASE } from "./cypress_sample_database_h2.json";
