{
  "get": {
    "description": "Return the captured eval trace as JSONL (one span per line) for `session-id`. Superuser-only;\n   served only when `MB_AI_EVAL_CAPTURE` is enabled.",
    "operationId": "get-api-eval-trace-session-id",
    "parameters": [
      {
        "in": "path",
        "name": "session-id",
        "required": true,
        "schema": {
          "maxLength": 200,
          "minLength": 1,
          "type": "string"
        }
      }
    ],
    "responses": {
      "2XX": {
        "content": {
          "application/json": {
            "schema": {}
          }
        },
        "description": "Successful response"
      },
      "4XX": {
        "description": "Client error response"
      },
      "5XX": {
        "description": "Server error response"
      }
    },
    "summary": "GET /api/eval-trace/{session-id}",
    "tags": [
      "/api/eval-trace"
    ]
  }
}
